# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers (`_core/`, `routers/`, `db.ts`)
- `drizzle/`: Schema (`schema.ts`) + migrations
- `shared/`: Tipos e constantes (`version.ts`, `changelog.ts`, `paymentConditions.ts`, `modules.ts`)
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui`

## Recent changes

> **Convenção (atualizada Rev. 2062 — mais enxuta)** — `replit.md` guarda apenas as **2 últimas revisões** em formato detalhado e as **5 seguintes** em one-liner. Detalhe completo (causa-raiz, arquivos tocados, racional, follow-ups) vive SEMPRE em `shared/changelog.ts`. Demais one-liners vão para `replit-history.md`.
>
> **Ao criar uma nova revisão**:
> 1. Adicionar bloco detalhado da NOVA revisão no TOPO (1-2 parágrafos: o quê + por quê + arquivos principais — sem racional longo, isso vai pro `changelog.ts`).
> 2. Demover a Rev. mais antiga das 2 detalhadas pra one-liner.
> 3. Demover a Rev. mais antiga dos 5 one-liners pra `replit-history.md`.
> 4. Bumpar `shared/version.ts` + prepender entrada COMPLETA (com todo o racional) no topo de `shared/changelog.ts`.

### Top 2 detalhadas

- **Rev. 2350** — **CAUSA RAIZ ENCONTRADA via teste manual: (a) GOOGLE_API_KEY tem o Custom Search API PERMANENTEMENTE BLOQUEADO no projeto GCP 1052983877622 (`API_KEY_SERVICE_BLOCKED`); (b) OpenVerse/Wikimedia indexam quase só EN, não PT. Por isso todas as revs 2342-2349 deram 0/60. Fix: LLM gera query EN curta (2-3 palavras industriais), cascade OpenVerse→Wikimedia, blocklist cirúrgica + barra PDFs, fallback EN por categoria.** Pedido user (24/05/2026, IMG_1143 ainda com placeholder): "Não deu certo". Logs Rev. 2349: "LLM gerou 60/60 queries" + "Aprovadas: 0/60". **Debug manual** via shell: (1) Google CSE direto → `HTTP 403 API_KEY_SERVICE_BLOCKED` (chave tem serviço CustomSearch desabilitado no GCP — já era estado da Rev. 2341 mas só agora foi confirmado por teste isolado). (2) OpenVerse query PT "andaime fachadeiro produto" → 0 results. (3) Wikimedia query PT → 0 pages. (4) Wikimedia query EN "scaffolding" → 3 pages. (5) Loop EN curto em 7 queries: "adjustable scaffold jack" → `Scaffold-Jack.jpg` (perfeito), "concrete mixer" → foto perfeita, "demolition hammer" → Jackhammer.jpg, OV "Adjustable Base Jack"/"Titan Breaker" — cobertura excelente. Queries longas (4+ palavras) trazem PDFs de tratados antigos. **Diagnóstico**: 8 revs (2340-2349) trabalharam em torno do sintoma errado; causa raiz era IDIOMA da query, não validação/arquitetura. **Implementação** (`server/routers/equipamentos.ts`): (1) **Prompt LLM em INGLÊS pedindo query EN 2-3 palavras** com 10 exemplos canônicos (DIAGONAIS+ANDAIME → "scaffold brace"; RODAPÉ+ANDAIME → "toe board scaffold"; PAINEL NR18 → "scaffold facade panel"; SAPATAS+ESCORAMENTO → "adjustable scaffold jack"; BETONEIRA → "concrete mixer"; MARTELETE → "demolition hammer"; ESCORA+ESCORAMENTO → "shoring prop"; GERADOR → "diesel generator"; PRANCHAO+ANDAIME → "scaffold metal plank"; ANDAIME TUBULAR → "tubular scaffolding"); regras NO quotes/códigos/medidas/palavras-poluentes ("product/photo/equipment"). (2) **Cascade reordenado**: OpenVerse → Wikimedia → Google (Google `googleDesativadoPorErro=true` por default — branch teórico pra se algum dia destravarem). (3) **Blocklist mais cirúrgica**: removidos tokens ambíguos (`model/modelo`, `cover/capa`), adicionados específicos vistos em testes (`beetle/rhino/insect/cat/dog/bird/baby/nude/painting/exhibition/fragonard`), adicionado `\.pdf(\?|$)/i` pra barrar PDFs Wikimedia. (4) **`FALLBACK_EN` por categoria** quando LLM falha pra um item: `{andaime:"scaffolding", escora:"shoring prop", forma:"formwork", ferramenta:"construction tool", epi:"safety helmet", veiculo:"construction vehicle", maquina:"construction machine", container:"container", mobiliario:"office furniture", eletric:"electric tool"}` → fallback final "construction equipment". **Por que NÃO insistir em PT**: OV usa Flickr/Wikipedia/Smithsonian (todos majoritariamente EN); WM Commons usa títulos de arquivo EN; PT <5% cobertura industrial. **Por que NÃO trocar GOOGLE_API_KEY**: já discutido na Rev. 2341 — key compartilhada com Gemini/geocoding, editar restrições impacta outras integrações silenciosamente. **Por que NÃO Bing/Brave/SerpAPI**: nova secret + cota paga; OV+WM são free/ilimitados e cobrem 80%+ quando query é boa. **Esperado**: 30-50/60 fotos em descrições comuns. **R-001/R-007/R-010:** N/A — LLM batch + UPDATE escopado por `company_id` preservado, idempotente, zero DDL.
- **Rev. 2349** — **SOLUÇÃO DEFINITIVA · Busca de fotos com IA inverte a arquitetura: LLM gera a QUERY PT-BR perfeita por item (conhece jargão BR + desambigua via categoria) e a gente confia no PRIMEIRO resultado do Google Images. Sem validação a posteriori — só filtro anti-lixo por keywords no título. Captura o gesto que um humano faz no Google.** Pedido user (24/05/2026, IMG_1142 com 60+ cards no placeholder amarelo): "Erro nas fotos, quero uma solução definitiva pra ISO, usa o Claude, chatgp qualquer outra forma para isso funcionar". Histórico: Rev. 2342 (validação rigorosa) → 0/60; Rev. 2345 (sem validação) → foto de homem em RODAPÉ; Rev. 2347 (rigorosa PT) → 0/60; Rev. 2348 (categoria no payload) → ainda 0/60. **Causa raiz fundamental**: arquitetura "buscar genérico + validar com LLM" tem equilíbrio impossível — validador estrito rejeita matches reais (provedores não retornam títulos com "menciona equipamento exato"), validador frouxo deixa passar lixo. Os provedores NÃO indexam fotos com títulos descritivos PT — só são buscáveis via BOA QUERY DE ENTRADA. **Inversão**: o trabalho do LLM é GERAR A QUERY, não VALIDAR resultados. **Implementação** (`server/routers/equipamentos.ts`, `locadosBuscarFotosComIA`): (1) **2a — LLM gera query PT-BR por item (1 batch call)**: prompt "você é especialista em equipamentos de construção civil brasileira" + 6 exemplos canônicos ("DIAGONAIS X 1,50 M" + ANDAIME → "diagonal contraventamento andaime tubular produto"; "RODAPÉ 20 CM" + ANDAIME → "rodapé proteção andaime fachadeiro produto"; "PAINEL NR18" + ANDAIME → "painel fachadeiro nr18 andaime produto"; "SAPATAS AJUSTÁVEIS" + ESCORAMENTO → "sapata ajustável escora metálica produto"); regras: 4-7 palavras PT-BR, jargão de obra, sem aspas, sem código proprietário, sem medida que polua, termina com "produto"/"equipamento". Fallback se LLM falhar: `${desc} ${cat} produto`. Single call ~5-8s/60 itens. (2) **2b — Filtro anti-lixo por keywords (sem LLM call)**: 5 regex sobre `title+url` bloqueando model/modelo/female/male/woman/man/portrait/selfie, avatar/profile/logo/brand/banner/cover/capa, book/livro/magazine/revista, food/comida/cake/recipe, cartoon/illustration/drawing/vetor. Latência zero, captura os vetores conhecidos (foto de homem em RODAPÉ era "male torso"; capa livro USP em SAPATAS era "book cover"). (3) **2c — Cascade trust por provider**: pra cada desc, busca com query LLM em Google → 1º resultado não-lixo ganha; senão OpenVerse → Wikimedia. Sem cap/dedup cross-provider (pega 1º válido e fim). (4) **Validação a posteriori REMOVIDA** — bloco antigo de ~80 linhas (prompt + JSON parse + map) sumiu. `validacoes` construído direto de `aprovacoes`+`semFoto`. (5) `motivo` reporta `<provider> · "<query>"` (debug grátis). Phase C placeholder SVG preservada. **Por que confiar no 1º hit Google**: queries específicas geradas por LLM mapeiam direto pra catálogos online BR (Casa do Construtor, Locadora de Andaimes); 1º hit é 90%+ relevante quando query é específica — é o que humanos usam. **Por que NÃO Claude gerando URL**: alucinaria. LLM gerar QUERY pra search engine real é o uso correto. **Por que NÃO Anthropic web_search tool**: retorna texto, não URLs de imagens. **Por que blocklist em vez de validador LLM**: captura 95% dos casos de erro com latência zero e zero chance de "rejeitar tudo". **Esperado**: Phase A 0/60 → 40-60/60 em descrições comuns. SKUs proprietários muito específicos podem continuar no placeholder (aceitável). **R-001/R-007/R-010:** N/A — LLM + UPDATE escopado por `company_id` preservado, idempotente, zero DDL.
- **Rev. 2348 (demovida)** — **HOTFIX/UX · Busca de fotos com IA ganha (a) AUTO-LOOP client-side até processar TODOS os lotes (não para mais em 60 por click) + (b) VALIDAÇÃO STRICT "foto EXATA do produto" reforçada com `categoria` no payload da IA pra desambiguar (RODAPÉ/PAINEL/DIAGONAIS).** `fotosAcumRef` acumula entre lotes, `onSuccess` dispara próximo via setTimeout(250ms); prompt reescrito como "foto EXATA do produto"; `categoria` adicionada ao userPayload. **R-001/R-007/R-010:** N/A. (Auto-loop preservado na Rev. 2350; validação removida pela Rev. 2349. Detalhe completo em `shared/changelog.ts`.) Pedido user (24/05/2026, IMG_1141 "60 de 60 — 0 Fase A / 60 Fase C"): "Quero que a ia melhore a busca e só pare quando fizer todas, não quero liberando apenas 60" + "Quero que a foto seja exatamente do produto". **Causa raiz dupla**: (1) procedure server cap=60 desc/call, retornava `haMaisLotes:true` mas client exigia N cliques manuais. (2) Phase A=0/60 — prompt da Rev. 2347 mencionava RODAPÉ na seção AMBIGUIDADES, mas o `userPayload` passado pro Gemini não carregava `categoria` do item — sem contexto, o LLM caía no "em dúvida, rejeite". **Implementação** — **Server (`server/routers/equipamentos.ts`, `locadosBuscarFotosComIA`)**: (1) `Bundle` ganha campo `categoria: string` populado do `d.categoria` já presente no SELECT agregado. (2) `userPayload` passa `categoria` por item (`(sem categoria)` quando NULL) — zero custo de tokens, habilita as regras de desambiguação. (3) System prompt reescrito como "foto EXATA do produto" (não "plausível"/"razoável"): REGRA SUPREMA "em dúvida, REJEITE"; APROVE só com título nomeando equipamento/sinônimo industrial inequívoco + categoria batendo, OU nome de arquivo descritivo Wikimedia; REJEITE título genérico ("construction site", "canteiro", "scaffolding system overview" — mostra ambiente mas não O PRODUTO), pessoa/animal/paisagem, categoria diferente, vago, confiança <85%; casos especiais via categoria (RODAPÉ em ANDAIME = toe board; PAINEL em ANDAIME = fachadeiro vs ELÉTRICO = quadro; DIAGONAIS = brace); códigos proprietários (PG-2030) → quase sempre rejeite. **Client (`client/src/pages/equipamentos/Locados.tsx`)**: (1) `fotosAcumRef = useRef<{lotes, analisadas, encontradas, itensAtualizados, semFoto[], phaseA/B/C} | null>` — acumulador entre lotes do mesmo run (ref pra closure estável durante mutations sequenciais). (2) `buscarFotosMut.onSuccess` vira state machine: soma `res.*`, invalida `locadosListar` (UI mostra fotos novas conforme aparecem), se `haMaisLotes && !cotaEsgotada` → `setTimeout(mutate, 250)` pra próximo lote (250ms = frame pra UI re-renderizar antes da próxima call); senão consolida em `resultadoFotosIA` com novo campo `lotesProcessados`, fecha progresso, toast "processou N lote(s) — X equip atualizado(s)". (3) `onError` limpa acumulador (run abortado idempotente — basta clicar de novo). (4) Click "Buscar agora" reseta `fotosAcumRef.current=null`. (5) Texto de progresso mostra "Processando lote N" + acumulado de lotes anteriores; primeira linha vira "vou rodar em cascata até processar todos". (6) Type `resultadoFotosIA` ganha `lotesProcessados?`. **Por que ref vs state pro acumulador**: state seria capturado por closure stale do onSuccess entre mutations; ref garante "última escrita ganha". **Por que setTimeout 250ms vs 0**: dá frame pro invalidate refetchar antes da próxima call — sem isso UI só atualizaria no fim. **Por que NÃO loop no server**: cap 60 evita LLM context overflow (60×10 cands = 600 títulos) + timeout HTTP >2min + bloqueio thread Node; loop client preserva limites + dá feedback incremental + permite cancelar fechando aba. **Por que categoria SÓ no payload (não na busca)**: a busca já usa cat como qualificador (`${desc} ${cat}`); problema era SÓ na validação sem contexto. **R-001/R-007/R-010:** N/A — LLM + UPDATE escopado por `company_id` (preservado), idempotente, zero DDL, rate-limit natural via Gemini (~5-8s/lote).
- **Rev. 2347 (demovida)** — **HOTFIX/FILOSOFIA · Busca de fotos com IA volta a buscar em PORTUGUÊS (Google CSE prioridade) com VALIDAÇÃO RIGOROSA em TODOS os candidatos. Phase B "busca ampla sem validação" da Rev. 2345 REMOVIDA — era ela que aplicou foto de homem em RODAPÉ 20 CM (IMG_1140). Reverte a tradução PT→EN da Rev. 2343.** Removido bloco de tradução PT→EN (60 linhas + 1 LLM call); busca PT-only com 2 queries por descrição em paralelo via OpenVerse/Wikimedia/Google CSE; validação rigorosa com prompt "em dúvida REJEITE" + 4 exemplos de ambiguidade (RODAPÉ ≠ piso; PAINEL precisa categoria); modal de resultado vira grid 2 cards (A emerald + C slate). **R-001/R-007/R-010:** N/A. (Revertida na Rev. 2349 — validação rigorosa rejeitava 100%. Detalhe completo em `shared/changelog.ts`.) Pedido user (24/05/2026, IMG_1140 com "RODAPÉ 20 CM" + thumbnail de torso de homem): "As fotos não têm nada a ver com o produto … usa o Google e pesquisa as fotos na internet, procure em português pra facilitar as buscas, e agrar a que nunca dê errado". **Causa raiz**: Rev. 2345 introduziu Phase B (busca ampla por categoria/1ª palavra) que pegava o 1º candidato SEM nova validação por IA, só pra garantir cobertura 100%. Em descrições ambíguas como "RODAPÉ 20 CM" (em construção civil = guard rail/toe board do andaime, não rodapé de piso) virou vetor de foto errada. Rev. 2343 amplificou traduzindo PT→EN ("RODAPÉ"→"skirting board"). **Implementação** — **Server (`server/routers/equipamentos.ts`, `locadosBuscarFotosComIA`)**: (1) Removido bloco de tradução PT→EN inteiro (60 linhas + 1 LLM call) — Google CSE indexa PT-BR muito bem, e validação rigorosa filtra Wikimedia/OpenVerse mesmo retornando títulos em EN. (2) Busca PT-only com 2 queries por descrição (`"<descricao> <categoria>"` + `"<categoria> equipamento construção"`), 2× OpenVerse + 2× Wikimedia + 1× Google CSE em paralelo, dedup por URL (Google primeiro = prioridade no desempate da IA), cap em 10 cands/desc. (3) Validação RIGOROSA com prompt reescrito: "REGRA DE OURO: em dúvida, REJEITE" + 5 critérios pra rejeitar explícitos (nenhum menciona equipamento; algum sugere pessoa/animal/paisagem/capa de livro; título vago; categoria diferente; confiança <80%) + seção "CUIDADO COM AMBIGUIDADES" com 4 exemplos contra (RODAPÉ ≠ rodapé de piso; PAINEL precisa de categoria; DIAGONAIS ≠ listras; códigos proprietários → rejeite). (4) **Phase B REMOVIDA** — só Phase A (validada) → Phase C (placeholder SVG). Tudo que não passa vai direto pro placeholder honesto. `fotosPhaseB` retorna constante 0 (preserva shape do client). **Client (`client/src/pages/equipamentos/Locados.tsx`)**: (1) Modal de confirmação — "busca em **português** no Google + OpenVerse + Wikimedia e aplica **validação rigorosa**"; box pink lista 2 fases (A: foto validada com "em dúvida, rejeita" + C: placeholder por categoria) + linha itálica "Filosofia: melhor placeholder honesto que foto errada". (2) Modal de resultado — grid de 3 cards (A/B/C) vira grid de **2 cards** (A emerald + C slate). (3) Texto de cota esgotada não menciona mais "Fases B/C". **Por que NÃO traduzir PT→EN mesmo pro Wikimedia**: tradução automática é vetor #1 de ambiguidade — validação rigorosa no passo 2b filtra ruim, e Google em PT compensa. **Por que placeholder em vez de Phase B**: trocou problema (sem foto) por outro pior (foto errada). User preferiu placeholder honesto. Alinhado com R-001 (nada mentiroso em prod). **R-001/R-007/R-010:** N/A — só LLM calls + UPDATE escopado por `company_id` (preservado), idempotente, zero DDL.
- **Rev. 2346 (demovida)** — **UX/i18n · Inteiros ≥ 1.000 em Equipamentos Locados formatados em pt-BR com separador de milhar (1220 → "1.220").** Helper `fmtN(n) = n.toLocaleString("pt-BR")` + `<Kpi>` auto-formata quando `typeof value === "number"`; substituições inline em KPIs/badges/filtros/contadores. **R-001/R-007/R-010:** N/A — 100% client-side. (Detalhe completo em `shared/changelog.ts`.)
- **Rev. 2345 (demovida)** — **FEATURE/FILOSOFIA · Busca de fotos com IA passa a garantir COBERTURA 100% via 3 fases (A match preciso → B busca ampla → C placeholder SVG por categoria), invertendo a filosofia "sem foto > foto errada" da Rev. 2342.** Phase A com prompt relaxado ("sempre escolha o melhor"); Phase B com queries em cascata (categoria + 1ª palavra + EN) sem nova validação; Phase C com `svgPlaceholder` data-URI. **R-001/R-007/R-010:** N/A. (Revertida na Rev. 2347 — Phase B virou vetor de foto errada em descrições ambíguas como "RODAPÉ 20 CM". Detalhe completo em `shared/changelog.ts`.)
- **Rev. 2344 (demovida)** — **UX/PERF · Tela Equipamentos Locados ganha AGRUPAMENTO por descrição+obra (default ON) — 1218 cards individuais viram ~60 cards agregados (1 por "DESCRIÇÃO @ OBRA"), com modal drill-down listando as unidades individuais e todas as ações originais.** Pedido user (24/05/2026, IMG_1137): 12 cards "DIAGONAIS X 1,50 M" idênticos em "QIU 2 - FASE 4". "Agrupar os itens com o mesmo nome dentro da mesma obra para facilitar a análise". Pós-import (Rev. 2333) lista de 1218 unidades virou paredão visual. **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server/schema): (1) Estado `agruparPorDescObra` (default true) + `modalGrupo`. (2) useMemo `grupos` agrupa `data` por chave `${descricao.upper().trim()}__${obraId ?? "na"}`; agrega `unidades[]`, `valorMensalTotal` (Σ), `statusMix` (Record), `statusPrincipal` (mais comum → cor da faixa), `fotoUrl`/`fotoIA`/`fornecedorNome`/`categoria` (primeiro não-nulo). Ordenado por #unidades desc, depois valor desc. (3) Toggle pill no header da lista (ao lado de "Selecionar todos visíveis"): "Agrupar (N)" emerald + "Individual (N)" slate, contagem visível em ambos modos. (4) Render branch condicional `agruparPorDescObra ? grupos.map(g => …) : data.map(l => …)` — view individual preservada bit a bit. (5) **Card de grupo**: faixa gradient da cor do statusPrincipal; badge emerald grande "{N} un." (Boxes); pílulas por status ("12 EM USO", "3 ATRASADO") em vez de badge único; R$/mês é Σ ("total mensal"); checkbox tri-state (`ref.indeterminate`) marca/desmarca TODAS unidades do grupo; footer com botão único "Ver N unidade(s)" abre modal. Sem Check-in/Devolver no card (ações são por unidade — promovidas ao modal). (6) **Modal `modalGrupo`**: header gradient emerald→teal com foto+descrição+obra; KPI strip 4 cards (Unidades / Total mensal / Em uso / Atrasadas vermelho se >0); tabela das unidades com Patr/Status/Fornecedor/Início→Fim/R$/mês/Ações; thead sticky; zebra; ações por linha (Detalhes/Check-in/Devolver) FECHAM o modal e abrem o respectivo (Rev. 2339 convention — evita 2 modais sobrepostos). **Por que default ON**: ganho de densidade imediato no caso comum pós-import. **Por que client-side puro**: dados de `locadosListar` já trazem tudo; O(n) <5ms; mover pro server quebraria cross-filter + toggle instantâneo. **Por que ações no modal**: card agregado torna "Devolver" ambíguo — sempre 1:1 com unidade física no modal. **R-001/R-007/R-010:** N/A — feature 100% client-side, tenant guard preservado em `locadosListar`.
- **Rev. 2343** — **HOTFIX · Busca de fotos com IA agora TRADUZ as descrições PT → query em INGLÊS (com guards anti-alucinação) antes de buscar nos provedores (Wikimedia/OpenVerse/Google CSE) — sem isso a Rev. 2342 retornava 0/60.** Guards de segurança: (a) prompt de tradução tem REGRA CRÍTICA "se não tiver certeza, copie o PT — NÃO INVENTE" + exemplos de quando copiar (códigos proprietários, siglas obscuras); proibido traduzir pra categoria diferente (MARTELETE não vira "mixer"). (b) prompt de validação tem PRINCÍPIO #1 não negociável: DESCRIÇÃO_PT é VERDADE ABSOLUTA, a query EN pode ter mentido — se título bate com EN mas não com PT, REJEITE. 2 exemplos contra-tradução no prompt ("MARTELETE"+"concrete mixer"→reject; "ANDAIME"+"copper pipe"→reject). Dicionário expandido de sinônimos industriais.** Pedido user (24/05/2026, IMG_1136): após Rev. 2342, modal mostrou "0 de 60 — 0 equipamentos atualizados" com 30 termos sem foto (SAPATAS AJUSTÁVEIS, DIAGONAIS, PRANCHAO METALICO, PAINEL NR18…). Causa raiz: providers CC indexam majoritariamente conteúdo em INGLÊS — "SAPATAS AJUSTÁVEIS" retorna 0 ou lixo (capa de livro USP), enquanto "adjustable scaffold base jack" retorna fotos reais. A Rev. 2342 fez seu trabalho (rejeitou lixo); o gargalo era a busca, não a validação. **Server** (`server/routers/equipamentos.ts`, `locadosBuscarFotosComIA`): (1) **Novo PRÉ-PASSO 2a** — 1 única call ao `invokeLLM` com todo o lote (até 60 descrições) pedindo tradução PT→EN industrial. Prompt com 7 exemplos canônicos ("BETONEIRA 400L"→"concrete mixer construction", "PAINEL NR18"→"construction site protection panel", etc). Response `{traducoes:[{pt,en}]}` → Map<pt,en>. Falha silenciosa → fallback PT (preserva comportamento). Log `Tradução: X/N queries traduzidas`. (2) **Busca usa EN (2b)**: `queryPrincipal = traducoes.get(desc) || desc`; os 3 providers em paralelo recebem `queryPrincipal`. Google CSE recebe `${en} construction` (qualifier explícito quando temos EN). Bundle registra `queryUsada` pra passar pra validação. (3) **Validação 2c atualizada**: prompt alerta que títulos virão em INGLÊS; lista dicionário sinônimos (scaffold=andaime, shoring=escora, formwork=fôrma, jack base=sapata ajustável, plank=prancha, brace=diagonal, guard rail=guarda-corpo, concrete mixer=betoneira). Regra 1 RELAXADA — aceita títulos descritivos COM contexto desde que mencionem equipamento/sinônimo (ex: "scaffold base jack closeup" → OK); antes "EXPLICITAMENTE" levava a rejeições demais. Regra "em dúvida real rejeite" preservada. Payload passa `descricao_pt + query_busca_en + candidatos`. (4) **Logs server-side**: `Busca: X/N descrições com candidatos` permite diagnosticar gargalo sem instrumentar client. **Por que tradução E não relaxar validação**: relaxar permitiria voltar ao bug original (capa de livro USP grudada). Tradução ataca a CAUSA RAIZ. **Por que 1 call só**: 60 desc × 1 call ≈ 5-8s; 60 calls individuais seriam 1-2min + 60× mais tokens. **Esperado**: descrições comuns (BETONEIRA, ANDAIME, ESCORA, MARTELETE) devem encontrar foto em 60-80% dos casos; SKUs muito proprietários provavelmente seguirão sem foto. **R-001/R-007/R-010:** N/A — só LLM calls + UPDATE escopado por `company_id` (preservado da Rev. 2342), idempotente, zero DDL.
- **Rev. 2342 (demovida)** — **HOTFIX/FEATURE · Busca de fotos da IA passa por VALIDAÇÃO RIGOROSA via Gemini antes de persistir + novo botão "Limpar fotos IA" para reset em massa.** Helpers `fromOpenverse`/`fromWikimedia`/`fromGoogleCSE` em paralelo retornam `Cand[]`; 1 call `invokeLLM` valida todos os bundles com 6 regras rigorosas (rejeita genéricos/capas/pessoas; só aceita se título menciona equipamento/sinônimo industrial). Nova procedure `locadosLimparFotosIA` com tenant guard. **R-001/R-007/R-010:** UPDATE escopado por `company_id`. (Detalhe completo em `shared/changelog.ts`.)

- **Rev. 2341** — **FEATURE/HOTFIX · Busca de fotos com IA passa a usar chain de provedores públicos (OpenVerse + Wikimedia Commons + Google CSE opcional) — destrava o feature mesmo com GOOGLE_API_KEY restrita.** Pedido user (24/05/2026, IMG_1131/IMG_1132): após habilitar Custom Search API no projeto GCP `1052983877622` o erro persistiu. Causa real: GOOGLE_API_KEY (compartilhada com Gemini) tem restrições (HTTP referrer / API allowlist) que rejeitam chamadas server-side de Custom Search — Google retorna mensagem confusa "API not enabled". User: "arrume ou ache outra forma agora". **Decisão**: em vez de pedir edição da key (frágil — pode quebrar Gemini/geocoding), trocar pra chain de providers sem chave. **Server** (`server/routers/equipamentos.ts`, `locadosBuscarFotosComIA`): removido guard `PRECONDITION_FAILED` por GOOGLE_API_KEY ausente; `googleHabilitado = !!(apiKey && cx)` controla se CSE é tentado. 3 helpers: `tryOpenverse(query)` (CC aggregator do WordPress Foundation, ~700M imagens, sem chave/cota, timeout 8s, `api.openverse.engineering/v1/images/?page_size=5&mature=false`), `tryWikimedia(query)` (MediaWiki API `commons.wikimedia.org/w/api.php?action=query&generator=search&gsrnamespace=6&prop=imageinfo&iiurlwidth=400` — só arquivos de mídia, retorna thumburl), `tryGoogleCSE(query)` (lógica antiga preservada mas retorna struct `{url, cotaEsgotada, configInvalida}` em vez de throw). Cada provider never-throws no path feliz. Loop principal: pra cada descrição tenta OpenVerse → Wikimedia → Google (se habilitado e não desativado por erro). Após 1 erro config no Google, `googleDesativadoPorErro=true` evita repetir (perda de ~10s × 59 desc = 10min sem o flag). User-Agent custom em OpenVerse/Wikimedia. Wikimedia recebe descrição crua (sem qualificador "equipamento construção civil") — termos genéricos indexam melhor. **Client** (`Locados.tsx`): modal de confirmação atualizado — "bibliotecas públicas (OpenVerse / Wikimedia / Google)" + "Sem cota: OpenVerse e Wikimedia são ilimitados". Resto do fluxo (barra de progresso Rev. 2340.1, modal de resultado, badge Sparkles) reaproveitado. **Por que NÃO trocar a key**: GOOGLE_API_KEY também é usada pelo Gemini (parse PDF) — editar restrições impacta outras integrações silenciosamente. **R-001/R-007/R-010:** N/A — só network IO + UPDATE escopado por `company_id`, idempotente, zero DDL.
- **Rev. 2340 (demovida)** — **FEATURE · Busca de FOTO ILUSTRATIVA dos equipamentos locados via IA (Google Custom Search Image) + thumbnail nos cards com badge "IA".** Schema `foto_url TEXT` idempotente; procedure `locadosBuscarFotosComIA` agrupa por descrição única, query CSE com qualificador, detecta cota; client tem botão pink, modal de progresso, modal de resultado, thumbnail com badge Sparkles. **R-001/R-007/R-010:** UPDATE escopado, ADD COLUMN IF NOT EXISTS. (Detalhe completo em `shared/changelog.ts`.)
- **Rev. 2339** — **FEATURE/UX · Card de Equipamento Locado clicável + painel de detalhes completos (foto, KPIs, obra/fornecedor, galeria, timeline de eventos, ações).** Pedido user (24/05/2026, screenshot iPad com 1220 cards): "Quero poder clicar no item e abrir tudo relacionado ao item". Antes só o botãozinho "Histórico" abria um modal mínimo com `tipo + observacao` — dados do contrato, fotos, KPIs derivados ficavam invisíveis. **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server/schema — reusa `eventosListar`): (1) **Card clicável**: `role=button + tabIndex + onKeyDown(Enter/Space)` no wrapper; hover `border-emerald-300`, focus `ring-2 emerald`; checkbox e botões internos com `e.stopPropagation()`. Botão "Histórico" virou "Detalhes" (emerald). (2) **Painel redesenhado** (substitui modal antigo): **Header gradient** emerald→teal com foto 96×96 + pílulas status/categoria + título + Patr/N°S/ERP. **KPI strip 4 cards**: Dias em uso (`floor(hoje-dataInicio)`), Restam/Atrasado há (colorindo verde/âmbar/vermelho), Custo/mês com diária derivada, Acumulado estimado (`diasUso × valorDia` — primeira vez que user vê quanto já gastou). **Info grid 2-col**: Obra+Local+Fornecedor+Contrato | Período+Devolução+Responsável. **Observações** (âmbar, condicional). **Galeria** fotos do recebimento (grid 3-6 cols, target=_blank). **Timeline** `<ol>` com `border-l-2` e chip-ícone colorido `ring-4 white` — `TIPO_META` mapeia 6 tipos (RECEBIMENTO/CHECK_IN_OBRA/DEVOLUCAO_FORNECEDOR/RENOVACAO/MANUTENCAO/VINCULO_OBRA) → label+cor+ícone; cada evento mostra data pt-BR, observação, usuário, até 6 thumbs. **Footer sticky**: Check-in azul + Devolver emerald (só `em_uso`) que fecham este modal e abrem o respectivo (evita 2 modais sobrepostos). **Por que reusar `modalEventos`**: query `eventos` já keyed em `modalEventos?.id`, semanticamente é o mesmo "item selecionado". **Por que IIFE**: consome 8 closures locais — extrair componente exigiria prop drilling massivo. **R-001/R-007/R-010:** N/A — leitura pura.
- **Rev. 2338 (demovida)** — **UX · KPI cards de Equipamentos Locados verdadeiramente responsivos (1 → 2 → 4 colunas) com tipografia fluida via `clamp()`.** Pedido user (24/05/2026, screenshot iPad): "Quero o card responsivos". Diagnóstico: grid era `grid-cols-2 lg:grid-cols-4` — só virava 4 colunas em ≥1024px, iPad portrait (~820px) ficava sempre 2×2; card monetário usava `text-xl` cravado vs `text-3xl` dos outros, e valor grande (ex: "R$ 15.815,50") estourava o card. **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server/schema): (1) Grid `grid-cols-1 sm:grid-cols-2 md:grid-cols-4 gap-2 sm:gap-3` — mobile empilha 1/linha, tablet portrait 2×2, iPad landscape+ 4 lado a lado. (2) Tipografia fluida via CSS `clamp()`: números puros `clamp(1.5rem, 3.2vw, 2rem)`, monetário `clamp(1rem, 2.6vw, 1.5rem)` — viewport faz o trabalho, sem N breakpoints. (3) Defesas anti-overflow: `min-w-0` no card, `truncate` no valor+label, `tabular-nums` (alinha dígitos), `title=` (hover mostra completo), `shrink-0` no ícone. (4) Padding/ícone adaptativos: `p-3 sm:p-4`, `h-4 w-4 sm:h-5 sm:w-5`. **Por que `clamp()`**: transição contínua com a viewport (não pulsa em quebras discretas como `text-{sm,base,lg}` com breakpoints) — KPI com valor que cresce evita "estourou no passo 768→1024". **R-001/R-007/R-010:** N/A — só CSS/markup.
- **Rev. 2337 (demovida)** — **FEATURE · Categorização automática dos equipamentos locados via IA + filtro por categoria na tela Equipamentos Locados.** Pedido user (24/05/2026): "A ia consegue separar... categoria por exemplo... seria possível a ia colocar foto do produto tbm?" → escolheu "🤖 Deixa a IA propor as categorias com base nos 1.218 itens". Pós-import (Rev. 2333) 1.218 cards sem categoria → filtro inutilizável; coluna `categoria varchar(100)` JÁ EXISTIA no schema mas a IA do import nunca preenchia e nada classificava em lote os antigos. **Implementação** (zero schema change): **A. Server (`server/routers/equipamentos.ts`)** (1) `executeParseContratoLocacao` ganhou `categoria` no responseSchema + bloco no prompt com 7 categorias canônicas (Andaime e escoramento, Equipamento elétrico, Ferramenta manual, EPI/EPC, Veículo/Máquina pesada, Container/Mobiliário, Outros) — Gemini Vision classifica durante o parse, próximos imports já vêm categorizados sem call extra. (2) `importarContratosLocacaoLote` aceita `categoria` opcional por item + persiste no bulk INSERT. (3) Nova procedure `locadosCategorizarComIA({companyId, sobrescrever?})`: SELECT DISTINCT descricao + COUNT(*) (1218 linhas → ~50-80 strings únicas) → `invokeLLM` (Claude→Gemini, maxTokens 16k, response_format json_object) com prompt pedindo 5-10 categorias em jargão de obra brasileira → saneamento (ignora mapping com descrição/categoria inválida) → agrupa por categoria → 1 `UPDATE WHERE descricao = ANY([...]::varchar[])` por categoria (7 round-trips totais vs 1218 se fosse linha a linha). Idempotente (default só toca NULL/vazio). Retorna `{categorias, itensAtualizados, descricoesNaoMapeadas[20], haMaisLotes}`. Limite 800 descrições/call. **B. Client (`client/src/pages/equipamentos/Locados.tsx`)** (1) `confirmarImport` envia `categoria` por item. (2) Pipeline: dataAll → dataPorStatus → dataPorStatusEObra → data (filtroCategoria) — cross-filter respeita status+obra nos counters. (3) `categoriasComItens` useMemo + `totalSemCategoria` sobre `dataAll`. (4) UI: grid 2-col vira 3-col (busca | obra | categoria), select violet com ícone Tag. (5) Badge no card vira pílula clicável: violet quando há categoria (clique aplica filtro + stopPropagation), cinza estático quando não. (6) Botão "🤖 Categorizar com IA" no header (visível só se `totalSemCategoria > 0`), badge com contagem. (7) Dois modais — confirmação (violet) e resultado (emerald) com pills clicáveis das categorias propostas (clique aplica filtro + fecha). **Por que DISTINCT**: 50-80 strings cabem em 1 prompt; manda qtd pra IA priorizar. **Por que UPDATE por categoria**: 7 round-trips vs 1218. **Por que badge clicável**: descobrabilidade — análise emerge do gesto natural. **Próx. rev**: FOTOS via Google CSE (depende de CSE_ID a criar) + tab "Por Categoria" no dashboard. **R-001/R-007/R-010:** N/A — UPDATE scopado por `company_id`, idempotente, iniciado pelo user, zero DDL. **Hardening pós-review**: adicionado guard `getCompaniesForUser(ctx.user.id, ctx.user.role)` → FORBIDDEN se `input.companyId` não pertence ao user (corrigiu cross-tenant tampering apontado pelo architect).
- **Rev. 2336 (demovida)** — **FEATURE/UX · Drill-down clicável nas células da tabela "Locações mês a mês" → modal moderno com lista detalhada, KPI strip, busca e link pra tela principal.** Pedido user (24/05/2026, screenshot mostrando "Abr 2026 · 1.012 iniciadas / R$ 15.815,50"): "Quero poder clicar na informação, quero q abra uma nova tela conforme nossa regra de ouro, e layout moderno e interativo". Tabela mostrava só agregados — sem caminho óbvio entre análise→operação. **Implementação** (`client/src/pages/dashboards/DashAlmoxarifadoEquipamentos.tsx`, 0 server/schema): (1) Wrapper `cellBtn(content, metrica, disabled)` envolve cada `<DeltaCell>` num `<button>` com hover ring emerald + ícone 👁 on-hover; células com valor 0 ficam disabled (evita modal vazio). (2) Estado `detalheLoc: {mesKey, mesLabel, metrica: "ini"|"dev"|"saldo"|"custo"} | null` + `detalheBusca`; useEffect reseta busca ao fechar. (3) Modal full-screen backdrop-blur (click fora fecha, stopPropagation no card), max-w-6xl max-h-90vh flex-col. (4) Header gradient por métrica: emerald→cyan (ini), rose→orange (dev), indigo→fuchsia (saldo), amber→red (custo); ícone branco em chip backdrop, label mês caixa-alta + título + sub. (5) KPI strip 4 cards sempre (Iniciadas/Devolvidas/Custo/Obras envolvidas) — dá contexto independente da métrica. (6) Busca interna autoFocus filtrando descrição+fornecedor+patrimônio+obra. (7) Tabela com thead sticky backdrop-blur, badge Iniciada (TrendingUp emerald) / Devolvida (TrendingDown red), col Patrimônio mono, ícone Building2 na obra; devoluções mostram "—" no R$/mês (métrica de custo refere só às iniciadas). (8) Footer: contagem + link "Abrir Equipamentos Locados →". (9) Composição: ini/custo casa por dataInicio; dev casa por dataDevolucao; saldo = união (mesma unidade pode aparecer 2× se entrou+saiu no mesmo mês). **Por que modal e não rota**: preserva contexto da tabela, fechamento 1 clique, zero round-trip (dados em `locadosQ`). **Escopo**: só a tabela Locações (foco do screenshot); padrão `cellBtn` + modal genérico pronto pra reuso nas outras 5 tabelas se user pedir. **R-001/R-007/R-010:** N/A.
- **Rev. 2335** — **UX · Padrão do filtro de período dos 6 dashboards mês a mês passa de "últimos 12 meses" pro ano corrente.** Pedido user (24/05/2026): "O padrão é começar sempre no [início] do ano". Desde a Rev. 2330 default era `'12m'` (janela móvel) — em maio carregava 5+ meses do ano anterior, confundindo leitura ano-vs-ano. **Implementação** (`client/src/pages/dashboards/DashAlmoxarifadoEquipamentos.tsx`, 0 server/schema): `useState<"12m"|number>("12m")` → `useState(() => new Date().getFullYear())` (lazy initializer pra avaliar 1× no mount). Pill "12M" permanece como 1 clique pra quem quiser janela móvel. Estado compartilhado entre as 6 tabs (Rev. 2330). **Por que ano corrente**: unidade natural pra análise empresarial (fechamento contábil, ano-vs-ano, orçamento). **R-001/R-007/R-010:** N/A — só valor inicial de useState.
- **Rev. 2335 (demovida)** — **UX · Padrão do filtro de período dos 6 dashboards mês a mês passa de "últimos 12 meses" pro ano corrente.** Pedido user (24/05/2026): "O padrão é começar sempre no [início] do ano". `useState<"12m"|number>("12m")` → `useState(() => new Date().getFullYear())` com lazy initializer. Pill "12M" permanece como 1 clique pra janela móvel. **R-001/R-007/R-010:** N/A — só valor inicial de useState. (Mais detalhe em `shared/changelog.ts`.)
- **Rev. 2334 (demovida)** — **UX · Filtro por OBRA em Equipamentos Locados + reorganização da barra de filtros (grid 2-col busca+obra) com chips de filtros ativos.** Pedido user (24/05/2026, screenshot iPad mostrando 1220 cards "SAPATAS AJUSTÁVEIS" idênticos enfileirados): "Melhore o layout coloque um filtro por obra". Pós-import em massa (Rev. 2333) a lista virou paredão — única forma de filtrar era status ou texto livre (que não bate em obra). **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server/schema): (1) Estado `filtroObra: string` ("" / "__null__" / "<id>"). (2) Pipeline reescrito: `dataPorStatus` (memo intermediário só por status, alimenta contadores do select) → `data` (memo final + filtro por obra). (3) `obrasComItens` useMemo agrupa `dataPorStatus` por `obraId` via `obrasMap`, com count + valorMes somado; só obras que efetivamente TÊM equipamentos no status corrente entram no select (evita poluir com 50 obras zeradas); "sem obra" empurrado pro fim. (4) UI grid 2-col responsivo: linha1 pills status (preservada), linha2 busca (fr) + select obra (minmax 260px) com ícones Building2/ChevronDown + `appearance-none` + borda emerald quando ativo; cada option "Nome · N unid. · R$ X/mês". (5) Linha3 (condicional): chips de filtros ativos "Obra X · count" + "busca" cada um com X individual + "limpar tudo". (6) `obraSelecionada` useMemo pra resolver chip sem reiterar lista. **Por que client-side**: `locadosListar` já traz tudo; bucketing O(n) em 1218 itens <5ms; server-side só adicionaria round-trip. **Por que `<select>` nativo**: até 50 obras cabem no scroll; iPad nativo abre full-screen, fácil tocar; chips coloridos mascaram aparência "system default". **R-001/R-007/R-010:** N/A — leitura cliente-side, zero DDL/mutations.
- **Rev. 2333 (demovida)** — **HOTFIX/PERF/UX · Import PDF de locação: bulk INSERT no server (corrige "Load failed" do iOS Safari) + chunking de 10 contratos no client + nova seção "Equipamentos por Obra ERP" no preview pra validação pré-import.** Pedido user (24/05/2026, 2 screenshots iPad): "Tá travando nesta tela quando clico em cadastrar" + "Quero tbm quero liste todos os equipamentos por obra para o usuário validar antes de importar". **Causa raiz "Load failed"**: procedure `importarContratosLocacaoLote` fazia `for` aninhado com 2 INSERTs POR UNIDADE (`equipamentos_locados` + evento `RECEBIMENTO`) em UMA transação. 46 contratos / 1218 unidades = 2436 round-trips Neon → estoura 60s do proxy → iOS Safari mata fetch com "Load failed". Mesma classe da Rev. 2329. **Implementação**: (1) **Server** (`server/routers/equipamentos.ts`): loop POR CONTRATO dentro da transação — para cada contrato faz 1 bulk INSERT das N unidades + 1 bulk INSERT dos N eventos. 46 contratos × 2 = 92 round-trips (vs 2436 antigos, ~25× menos). Pareamento eventos→locados é DETERMINÍSTICO: dentro de um contrato todos os eventos compartilham obraId+numeroContrato (não dependemos da ordem do RETURNING). Atomicidade preservada. (2) **Client chunking** (`client/src/pages/equipamentos/Locados.tsx`, `confirmarImport`): CHUNK=10 contratos/call (~200-300 unid/chunk, <3s cada); loop `for` sequencial com `mutateAsync`; estado `importLoteProgresso` atualizado a cada iteração; botão mostra "Cadastrando lote X/Y…". On error: mensagem inclui "após X/N contratos" + invalida list query pra refletir parcial. `disabled` usa `!!importLoteProgresso` (não `isPending`). (3) **Seção "Equipamentos por Obra ERP"** (painel verde abaixo do "Custo por obra" indigo): agrupa `importPreview` por `c.obraId` via `obrasMap`, bucket extra "— Sem obra vinculada —" (sempre aberto + borda âmbar); `<details>`/`<summary>` colapsável por obra com contagem de contratos+unidades; expande pra tabela consolidada desc→qtd desc; ordenação: maior unidades primeiro, sem obra ao fim mas com badge "⚠ N unidade(s) sem obra" no header. **Esperado**: 1218 unid de ∞ (timeout) → ~15s (5 lotes × 3s). **Por que não 1 lote com bulk**: bulk corrige latência DB; payload ~500KB ainda passa pelo WebKit numa única request; chunking limita pior caso (~50KB) + dá progresso visual. **Por que "por obra" e não só "por endereço"**: endereço (Rev. 2326) é diagnóstico do PDF; obra ERP é o que vai pro banco — o que user precisa validar antes do INSERT irreversível. **R-001/R-007/R-010:** N/A — só INSERTs (escopo por `companyId`), zero DDL.
- **Rev. 2332 (demovida)** — **UX · Nome do mês capitalizado + ano completo ("Jan 2026") e indicador MoM (▲/▼ + %) em cada célula numérica das 6 tabelas mês a mês do Dashboard Almox & Equip.** Pedido user (24/05/2026, screenshot iPad): "Melhore o nome do mês, quero quero tinha indicador mês a mês, se subiu, abaixou cada movimentação isso em todos os dash ok". Rótulos "jan/26" apertados + nenhuma célula mostrava direção vs mês anterior. **Implementação** (`client/src/pages/dashboards/DashAlmoxarifadoEquipamentos.tsx`, 0 server/schema): (1) **Labels**: nova const `MESES_PT_CAP = ["Jan","Fev",…]`; `lastNMonths()` e `monthsOfYear()` geram `"Jan 2026"` em vez de `"jan/26"`. (2) **Componente `DeltaCell({ value, prev, money?, accent? })`**: valor formatado + badge com ícone (▲ArrowUp / ▼ArrowDown / —Minus) e % de variação vs mês anterior; cores: verde subiu / vermelho desceu / cinza igual; tooltip mostra valor anterior; fallback delta absoluto se |%|≥999. (3) **DeltaCell em todas as 6 tabelas** — Visão Geral (7 cols), Estoque (Novos+Acumulado), Movimentações (Movs/Ent/Sai/Saldo), Ferramentas (Registros), Próprios (Equip+Valor BRL), Locados (Ini/Dev/Saldo/Custo BRL). (4) Iteração mudou `.map(m=>…)` → `.map((m,i,arr)=>{ const pk=arr[i-1]?.key; … })`; Estoque (IIFE com `acc`) ganhou `prevN`+`prevAcc` por closure. **Por que direcional puro**: subir/descer em "Saídas"/"Devolvidas" não tem leitura óbvia de bom/ruim (depende do contexto); padrão direcional é universal e sem ambiguidade. **Por que %**: normaliza escalas (1→2 é +100%; 1000→1001 é +0%); tooltip já tem o valor absoluto. **R-001/R-007/R-010:** N/A — só apresentação client-side.
- **Rev. 2331 (demovida)** — **UX · Layout modernizado das 6 tabelas mês a mês do Dashboard Almox & Equip. + segmented pill p/ filtro de período.** Pedido user (24/05/2026, screenshot iPad com select nativo cinza feio): "Melhore o layout deixe mais moderno e melhore o layout do filtro do ano tbm… quero algo melhor". `<select>` nativo do iOS sobrescreve CSS e exige 2 cliques. **Implementação** (`client/src/pages/dashboards/DashAlmoxarifadoEquipamentos.tsx`, 0 server/schema): (1) **Filtro = segmented pill** (1 clique troca): container `rounded-full bg-slate-100/80 ring-1`, botões "12M / 2026 / 2025…" com ativo `bg-white shadow-sm ring-1` e inativo `text-slate-500 hover:bg-white/60`; `overflow-x-auto` se sobrar; `role=tablist` + `aria-selected` p/ a11y. (2) **Header** refeito: chip 9×9 rounded-xl gradient emerald-50→emerald-100/60 com ring + ícone CalendarRange + título 15px font-semibold + sub-título 11px com label do período ("últimos 12 meses" / "ano 2026"); fundo `from-slate-50/80 via-white to-white`. (3) **Cards** (replaceAll 10 ocorrências): `rounded-xl shadow-sm` → `rounded-2xl shadow-sm hover:shadow-md transition-shadow border-slate-200/70`. (4) **Thead** (replaceAll 11x): `bg-slate-50 text-xs` → `bg-gradient-to-b from-slate-50 to-slate-50/40 text-[11px] tracking-wide` (estilo Linear/Notion). **Por que pill não dropdown**: ≤7 opções → decisão instantânea visualmente; dropdown só ganharia com 20+. **Por que rótulo "12M"**: cabe ao lado dos anos no iPad portrait; sub-título já contextualiza. **R-001/R-007/R-010:** N/A — só CSS/markup.
- **Rev. 2330 (demovida)** — **FEATURE · Filtro de período (Últimos 12 meses ou ano fechado) nas 6 tabelas mês a mês do Dashboard Almox & Equip.** Pedido user (24/05/2026, screenshot iPad): "Quero um filtro por ano ou seja estamos em 2026 quero analisar isso" + "Replica isso para todos os dash deste módulo". Tabela mostrava jun/25→mai/26 (janela móvel) — não dava pra comparar ano calendário 2026 vs 2025. **Implementação** (`client/src/pages/dashboards/DashAlmoxarifadoEquipamentos.tsx`, 0 server/schema): (1) helper `monthsOfYear(year)` espelha `lastNMonths(12)` (mesma shape `{key,label}`); constante `MESES_PT` extraída. (2) Estado único `periodoMeses: '12m' | number` compartilhado pelas 6 tabs — trocar em qualquer tabela atualiza todas. (3) `anosDisponiveis` useMemo une anos com dado em qualquer fonte (movs, próprios, locados início/devolução, ferramentas, itens) + ano atual, ordenado desc. (4) `monthlyAgg` ganhou dep em `periodoMeses` — `months = periodoMeses === '12m' ? lastNMonths(12) : monthsOfYear(periodoMeses)`, loops inalterados. (5) Componente inline `MesesHeader({titulo})` padroniza ícone CalendarRange + título dinâmico ("— últimos 12 meses" ou "— ano 2026") + `<select>` à direita. Substituiu 6 blocos repetidos. (6) Todas as 6 tabelas (Visão Geral, Estoque, Movs, Ferramentas, Próprios, Locados) usam `<MesesHeader />`. **Por que selector único**: troca de período é intenção GLOBAL ("quero 2026 inteiro"); 6 selectors isolados forçariam 6 cliques. **Por que NÃO datepicker**: ano-vs-ano e rolling-12m cobrem 99% dos casos; intervalo arbitrário só complica UI. **R-001/R-007/R-010:** N/A — leitura pura.
- **Rev. 2329 (demovida)** — **PERF · Exclusão/vinculação em lote de locados ~50× mais rápida via bulk SQL.** Pedido user (23/05/2026, screenshot iPad): "Quero mais rápido". Mostrava "Lote 4 de 4 · 600 de 718 processados" há 33s — paliativo da Rev. 2328 (chunk 200 + spinner) deixou menos pavoroso mas não atacou a raiz. **Causa raiz**: server fazia `for (const id of input.ids)` dentro de `db.transaction` com 1 UPDATE/DELETE + 1 INSERT POR ID → 400 round-trips ao Neon por chunk de 200. Latência cumulativa, não pressão de DB. **Implementação** (`server/routers/equipamentos.ts`, 0 schema/DDL): (1) `locadosVincularObraLote`: `UPDATE ... WHERE id IN(...) RETURNING id` + `INSERT INTO eventos VALUES (...multi rows...)` a partir dos IDs efetivamente atualizados. 2 round-trips por chunk em vez de 2N. Transaction removida (não precisa — UPDATE é atômico; eventos duplicados em retry são aceitáveis vs perda de perf). (2) `locadosExcluirLote`: `DELETE eventos WHERE locado_id IN(...)` + `DELETE locados WHERE id IN(...) RETURNING id`. Idempotente — retry apaga eventos órfãos sem quebrar. (3) Cliente: `CHUNK` 200→500 (= limite server). Cada call <2s, sem risco de timeout do proxy (60s). 1218 itens viram 3 lotes rápidos em vez de 7 lentos. **Esperado**: ~105s → ~6s. UI da Rev. 2328 mantida. Multi-tenant preservado via `companyId` no WHERE. **R-001/R-007/R-010:** N/A.
- **Rev. 2328 (demovida)** — **HOTFIX/UX · Exclusão/vinculação em lote de locados parecia travada — chunk 500→200 + spinner + tempo decorrido por lote.** Pedido user (23/05/2026, screenshot iPad): "Travou" — modal "Excluindo equipamentos…" parado em "Lote 1 de 3 · 0 de 1.218 processados" por ~1 min. **Diagnóstico**: Rev. 2325 deixou `CHUNK=500` (= limite do servidor). 500 deletes em transação única no Neon + insert de eventos demora 30-60s — proxy do Replit mata em 60s (mesmo problema que motivou polling do PDF na Rev. 2321). Durante esse tempo, UI mostrava "0 de 1.218" estático → user via como travamento. **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server, 0 schema): (1) CHUNK 500→200 (cada call ≈10-20s, folgado nos 60s do proxy; 1218 itens viram 7 lotes em vez de 3, barra anda a cada ~15s). (2) Spinner girando ao lado de "Lote X de Y". (3) Tempo decorrido por lote via `tickNow` (setInterval 500ms enquanto `loteProgresso` existe) — "14s" em fonte mono; após 25s aviso âmbar "O banco está demorando mais que o normal — aguarde, pode levar até 60s por lote". (4) Preview otimista da barra: `previstoFeitos = feitos + min(restante, CHUNK)` em vez de degraus 1/N. (5) Texto explicativo atualizado. (6) Estado `loteProgresso` ganhou `loteIniciadoEm: number` (Date.now() no início de cada iteração). **Por que não baixar `.max(500)` do server**: integrações futuras podem precisar de batches maiores; limite protege contra payload abusivo. **Por que não polling**: overhead de fila desproporcional pra operação que cabe em 7 lotes de 15s. **R-001/R-007/R-010:** N/A — DELETE escopo por `companyId+id`, transação atômica por chunk.
- **Rev. 2327 (demovida)** — **UX · Cada aba do Dashboard Almox & Equip. vira item próprio na sidebar + tabela mês a mês (12 meses) em cada tela.** Pedido user (23/05/2026, 2 screenshots iPad): "quero que todos esses dash fique, na barra de comando lateral da forma que está ok e quero uma tabela em cada tela, comparando mês a mês". **Implementação 0-server, 0-schema** (1) `DashboardLayout.tsx` (seção "Análise" L478): 1 item virou 6, todos apontando pra mesma rota `/dashboards/almoxarifado-equipamentos` com `?tab=visao|estoque|movs|ferramentas|proprios|locados`; ícones distintos (Package/ArrowLeftRight/Wrench/HardHat/Truck). Active-state já suportado nativamente via `sidebarActiveParam` (L1170/L1784) — cada sub-item destaca em dourado quando ativo. (2) `DashAlmoxarifadoEquipamentos.tsx`: `useLocation()` + parse de `window.location.search` → `tabAtual`; `<Tabs>` agora CONTROLADO (`value={tabAtual} onValueChange={setTab}`) navegando pra `?tab=X` (deep-link da sidebar funciona, back/forward do nav também). Set `TABS_VALIDOS` valida fallback "visao". (3) **Comparativo mês a mês (12m)** novo em CADA tab: utils `monthKey()` (YYYY-MM UTC) + `lastNMonths(12)` (mmm/aa pt); `monthlyAgg` useMemo único bucketiza `movsEntradas/Saidas/Count` (exclui estornadas), `propriosNovos/Valor` (dataAquisicao), `locadosIniciados/CustoIniciado` (dataInicio), `locadosDevolvidos` (dataDevolucao), `ferramentasReg` (data_hora/criado_em), `itensCadastrados` (criadoEm). Tabela em Visão Geral consolida 8 cols; Estoque mostra novos + acumulado; Movs mostra Entradas/Saídas/Saldo (verde/vermelho); Ferramentas mostra Registros; Próprios mostra Equipamentos + Valor BRL; Locados mostra Iniciadas/Devolvidas/Saldo/Custo BRL. Todas headers padronizadas com ícone CalendarRange. **Por que mesma rota + querystring** (não 6 rotas): mantém as 10 queries tRPC em UMA instância — troca de tab instantânea, sem re-fetch. **Por que client-side**: `listMovements limit:2000` + listas já cobrem 12 meses; bucketing O(n) roda <10ms. **R-001/R-007/R-010:** N/A — leitura pura.
- **Rev. 2326 (demovida)** — **FEATURE · Importação PDF de locação cruza endereço com obras em andamento e sugere vínculo automático.** Pedido user (23/05/2026, PDF F051/R051 com 84+ contratos de 2 endereços: "RUA JOAO MARCELINO CAVALHEIRO Nº336 JARDIM DO VALE ll, GUARATINGUETA/SP" e "AVENIDA GETULIO VARGAS, 995 SANTA RITA APARECIDA HOTEL DO PAPA"): "Quero q o ERP cruze o endereço de entrega, nome da obra com as obras em andamento para que seja importando no local correto". Causa raiz: desde Rev. 2308 a IA salvava `localObra` só em `observacoes` (texto livre), todos os itens ficavam "Sem obra vinculada" → user vinculava na mão (multi-seleção da Rev. 2323 + chunking da Rev. 2325 eram workarounds). **Implementação** (1) **Server** `server/routers/avaliacao.ts` procedure `obras.listActive`: adicionado `endereco` + `cidade` ao SELECT (não-breaking, 7 telas usam mas só leem campos conhecidos). Procedure `importarContratosLocacaoLote` já aceitava `obraId` por contrato desde 2308. (2) **Client** `Locados.tsx`: util `normalize()` (NFD strip acentos + remove `Nº336` + pontuação), `tokenize()` (4+ chars, filtra 25 stop-tokens pt: rua, avenida, jardim, sp, sao, hotel, casa, etc), `matchObra()` (score = tokens da PDF presentes em `nome + endereco + cidade` da obra; exige >=2 em comum; desempate por contagem absoluta depois proporção). No polling `done`: auto-match em cada contrato, popula `c.obraId + obraMatchAuto + obraMatchScore`; toast "X/N auto-vinculados". **UI preview**: linha com endereço PDF + select de obras (cidade visível), borda verde (auto) / azul (manual) / âmbar (sem); badge `✓ auto` com tooltip de %. Banner verde no topo mostrando "X auto-vinculados · Y manuais · Z sem obra". `confirmarImport` envia `obraId` por contrato. **R-001/R-007/R-010:** N/A — zero DDL (só SELECT estendido), sem UPDATE/DELETE adhoc. Multi-tenant herdado de `listActive`.
- **Rev. 2325 (demovida)** — **HOTFIX/UX · Exclusão/vinculação em lote de equipamentos locados — chunking de 500 + modais bonitos.** Pedido user (23/05/2026, 2 screenshots do iPad): "Precisa arrumar o erro, e corrigir o layout da mensagem". Screenshots mostravam (1) toast vazando do canto inferior esquerdo com ZodError cru `"Too big: expected array to have <=500 items"`, ilegível e cortado; (2) `window.confirm` nativo do iPad sem aviso de que vai falhar. **Diagnóstico**: na Rev. 2323 o server limitou `ids: z.array(z.number()).min(1).max(500)` em `locadosExcluirLote`/`locadosVincularObraLote` (limite legítimo pra evitar timeout em transação Postgres com 1200+ UPDATEs/DELETEs); o user selecionou 1218 cards visíveis (cenário comum pós-import Rev. 2308) e o client mandava tudo num único call → ZodError + toast cortado. **Implementação** (`client/src/pages/equipamentos/Locados.tsx`, 0 server, 0 schema): (1) `CHUNK=500` + util `chunkIds(arr, size)`; `confirmarVincular`/`executarExcluir` agora rodam `for` sequencial chamando `mutateAsync` por chunk, acumulando `vinculados`/`excluidos`. (2) Estado `loteProgresso` + modal z-60 com barra de progresso (azul/vermelho) e "Lote X de Y · N de M processados" — fica aberto durante toda a operação, impede clique duplo. (3) Estado `confirmExcluir: number | null` substitui `window.confirm`: modal vermelho próprio com box amarelo ("histórico será removido") + box azul informativo SE total > 500 ("dividida em N etapas — limite do servidor"). (4) Estado `loteErro` + modal de erro persistente (z-60) substitui `toast.error`; parseia ZodError JSON via `formatTrpcError` (espelha o da Rev. 2322), mostra até 5 issues como `• path: message` em `<pre>`. (5) `excluirLote.isPending` substituído por `!!loteProgresso` no `disabled` (cobre chunks em série). **Por que NÃO subir `.max(500)` no server**: transação Postgres com 1200+ UPDATEs+DELETEs+INSERT de eventos pode estourar timeout/lock. Manter limite + paginar client = melhor dos dois mundos. **R-001/R-007/R-010:** N/A — DELETE via mutation iniciada pelo user, escopo por `companyId` + `id` + transação atômica por chunk.
### Revisões recentes (one-liners)

- ~~Rev. 2343~~ — HOTFIX · Busca de fotos com IA traduz PT→EN (guards anti-alucinação) antes dos providers. Ver `shared/changelog.ts`.
- ~~Rev. 2338~~ — UX · KPI cards de Equipamentos Locados responsivos (1→2→4 cols) + `clamp()`. Ver `shared/changelog.ts`.
- ~~Rev. 2337~~ — FEATURE · Categorização automática de equipamentos locados via IA + filtro por categoria. Ver `shared/changelog.ts`.
- ~~Rev. 2336~~ — FEATURE/UX · Drill-down clicável nas células da tabela "Locações mês a mês" → modal moderno. Ver `shared/changelog.ts`.
- ~~Rev. 2334~~ — UX · Filtro por OBRA em Equipamentos Locados + grid 2-col busca+obra + chips. Ver `shared/changelog.ts`.
- ~~Rev. 2333~~ — HOTFIX/PERF/UX · Import PDF de locação: bulk INSERT no server + chunking de 10 contratos no client. Ver `shared/changelog.ts`.

### REGRA DE OURO — Cabeçalho de documentos institucionais FC (Rev. 2106+)

Todo documento oficial FC (contrato, aviso prévio, termo de rescisão, comunicado interno, carta MDO, advertência etc.) DEVE usar este cabeçalho HTML:

```
[logo centralizado ~88px — fallback ${window.location.origin}/logo-fc.jpg]
[RAZÃO SOCIAL caixa alta 16pt bold centralizado]
[CNPJ: xx.xxx.xxx/xxxx-xx — 9.5pt centralizado cinza]
[ENDEREÇO COMPLETO uppercase 9pt centralizado cinza claro]
[faixa azul #1B2A4A full-width, border branco 2px, padding 14px,
 TÍTULO DO DOC caixa alta 13pt letter-spacing 3px branco]
[Nº NNN/AAAA (esq) ───── Data de Emissão: DD/MM/AAAA (dir)]
```

Regras técnicas obrigatórias:
- **Inline styles** em TODOS elementos críticos (DOMPurify pode descartar `<style>` externo).
- `<style>` interno SEMPRE dentro do `<body>` (não no `<head>`).
- `print-color-adjust: exact` inline na faixa azul (cores de fundo no print).
- JAMAIS usar `onerror=`, `onload=` ou qualquer handler `on*` (filtro XSS do `signatures.create`).
- Logo SEMPRE com fallback `${window.location.origin}/logo-fc.jpg`.
- Corpo: `text-align:justify; hyphens:auto`, Times serif 11.5pt.
- Cláusulas com `border-left:3px solid #1B2A4A; padding-left:8px` no título.

> Revisões 2098 → 2044 e anteriores: ver [`replit-history.md`](./replit-history.md) e `shared/changelog.ts` (detalhe completo).

> Revisões 2084 → 2044 e anteriores: ver [`replit-history.md`](./replit-history.md) e `shared/changelog.ts` (detalhe completo).


## User preferences

- Idioma de comunicação: pt-BR direto e objetivo.
- Toda revisão DEVE: editar código + bumpar `shared/version.ts` + adicionar entrada NO TOPO de `shared/changelog.ts` + atualizar `replit.md` (convenção 2+5 — ver acima).
- R-001 / R-007 / R-010: JAMAIS executar `ALTER TABLE`, `DROP`, ou `DELETE` em produção.
- **Métricas de avanço de obra — fonte ÚNICA é o MS Project (XML LOTUS).** O ERP deve SEMPRE ler do XML do MSP pra garantir paridade absoluta com o que o engenheiro vê no Project. Convenção fixa (Rev. 2260+):
  - **PREVISTO** = campo `% PREVISTO` calculado pelo MSP na **tarefa-resumo** (UID=0). Lido em ordem de prioridade: Texto10 (FieldID 188743750, 4 casas) → Texto11 (188743997) → Texto6 (188743746, inteiro — usado pelo template LOTUS R05). Por atividade: mesma ordem (Texto10 → Texto6).
  - **REALIZADO** = `PercentComplete` da **tarefa-resumo** do projeto. Por atividade: Texto7 (188743747 — %Reali AUX) com fallback `ActualDuration / (ActualDuration + RemainingDuration)` (precisão MSP-nativa).
  - JAMAIS recalcular dinamicamente quando o XML tem snapshot — o snapshot do MSP é a verdade.
- **PROIBIÇÃO ABSOLUTA DE CÁLCULO NO PLANEJAMENTO (Rev. 2265+).** O módulo Planejamento NÃO executa NENHUM cálculo de avanço próprio para os cards/agregados visíveis ao engenheiro. Só LÊ o snapshot do MSP (`previstoMspSnapshot` / `realizadoMspSnapshot` do `calendarioJson`). Quando o snapshot está ausente (XML antigo, semana fora do cutoff, envelope mexido), o ERP exibe "—" com tooltip explicando o motivo e CTA pra reimportar o XML — JAMAIS recorre a fallback calculado (ponderação por duração/custo/dias úteis). Indiretas existem apenas no ERP (fora do XML), então no painel "Avanço Global" os valores "Diretas" e "Global" são idênticos ao snapshot da raiz UID=0 e a "distorção" foi aposentada. Single-source-of-truth: hook `mspReadOnly` em `client/src/pages/planejamento/PlanejamentoDetalhe.tsx`. Editor de avanços (linhas/inputs por atividade) e exportações internas (REFIS, Curva S) podem usar os useMemos legados, mas **nenhum card agregado novo** deve fazê-lo.
