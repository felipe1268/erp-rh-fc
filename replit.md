# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per módulo
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (`shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> **Convenção OBRIGATÓRIA (não negociável)** — este arquivo guarda APENAS as últimas **5 revisões** em formato detalhado (o quê + por quê + arquivos tocados). As 10 revisões seguintes aparecem como one-liner com título curto. Demais revisões vão para `replit-history.md`.
>
> **Ao criar uma nova revisão**:
> 1. Adicionar o bloco detalhado da NOVA revisão no TOPO.
> 2. Pegar o bloco mais ANTIGO dos 5 detalhados e converter pra one-liner curto na seção "Revisões recentes".
> 3. Se a seção "Revisões recentes" passar de 10 itens, mover o mais antigo pra `replit-history.md`.
> 4. NUNCA deletar parcialmente um bloco. Linhas órfãs sem prefixo `- ` são bug.
> 5. NUNCA usar marcadores HTML do tipo `<!-- DETALHES -->` (banidos na Rev. 1958).
>
> O histórico completo (causa-raiz, stack traces, arquivos tocados, comentários longos) vive em `shared/changelog.ts`. Esta convenção é validada visualmente: cada linha do bloco deve começar com `- ` (hífen+espaço).

### Top 5 detalhadas

- **Rev. 2056**: **SST · Integração · 2 ajustes pedidos pelo usuário em sequência: (A) reprovado volta AUTOMATICAMENTE pra Pendentes (sem precisar de botão "Liberar"); (B) configuração de integração agora tem botão de editar (título, nota mínima, validade, ativo).** **(A) Reprovado → Pendentes automático**: usuário escolheu "AUTOMÁTICO — assim que reprovar, o nome já aparece em Pendentes pra refazer (a aba Reprovados vira só consulta/histórico)". **Investigação**: na verdade JÁ FUNCIONA assim no backend — `listarPendentesAuto` (L753) monta `lastApproved` filtrando `status='aprovado'` apenas (L773); registros `reprovado` NÃO entram nem em `lastMap` (default "nunca_fez") nem em `inProgressSet` (só pega `pendente`/`em_andamento`). ZERO mudança de servidor. **Solução só na UI da ReprovadosTab (Rev. 2055)**: botão "Liberar" REMOVIDO (era redundante — confundiria RH sugerindo ação obrigatória); AlertDialog âmbar REMOVIDO; mutation `excluirRegistros` REMOVIDA (registro fica vivo pra preservar métrica de tentativas); nova coluna "Status" com badge âmbar "Pendente · refazer" (ícone Clock) — sinaliza visualmente que JÁ está na fila; banner do topo reescrito: "voltam AUTOMATICAMENTE para Pendentes para refazer — não é preciso liberar manualmente"; Raio-X preservado; colSpan empty-state 9→10. **(B) Editar configuração**: pedido direto (IMG_0943): "Quero poder editar a validade". Backend `atualizarConfig` (L239) JÁ EXISTIA — só faltava UI. **Solução em 1 arquivo (zero schema, zero novo procedure)** — `client/src/pages/sst/IntegracaoSST.tsx` ConfigTab: novo state `editingConfig` + hook `atualizarConfig` (toast onSuccess/onError); botão `Edit` azul ao lado do `Trash2` (com `e.stopPropagation()` pra não toggle a expansão — também consertou o bug latente do Trash2); novo `<Dialog>` modal reusa visual do "Nova Configuração" (Título / Nota Mínima 1-100% / Validade 1-60 meses com helpers / checkbox "Configuração ativa"); banner âmbar dentro do modal: "Mudanças valem pra NOVAS integrações. Certificados já emitidos preservam a validade calculada no momento da aprovação"; botão "Salvar alterações" com ícone Save. + `shared/version.ts` → 2056. **Por que NÃO criei `liberarParaRefazer` no servidor**: na opção AUTOMÁTICO escolhida, nada precisa acontecer no servidor (já estava certo); adicionar mutation = complexidade sem propósito. **Por que NÃO toquei nos certificados já emitidos quando muda validade**: `dataValidade` é GRAVADA na linha do registro no momento da aprovação (L1432 `validade.setMonth(...)`); preservar regra/contrato é fundamental pra auditoria fiscal/RH; banner âmbar deixa explícito. **R-001/R-007/R-010 OK**: ZERO ALTER/DROP/SQL direto; `atualizarConfig` faz UPDATE cross-tenant (assertCompanyAccess + WHERE companyId). **Preservado**: Rev. 2055 (estrutura aba Reprovados) preservada — só mudou conteúdo; Rev. 2054 (filtro férias) INTACTA; Rev. 2049 (AprovadosTab) INTACTA; `excluirRegistros` continua disponível pra HistoricoTab. **Follow-up**: (1) auditoria "quem editou config X e quando" (hoje sem trilha); (2) recalcular `dataValidade` opcionalmente pros certificados já emitidos quando RH muda validade (opt-in com warning); (3) bloqueio configurável após N reprovações (escalar pra TST presencial em vez de auto-pendentes infinito); (4) histórico de mudanças de config; (5) importar config de outra empresa (setup novo tenant).
- **Rev. 2055**: **SST · Integração · Nova aba "Reprovados" no menu (entre Aprovados e Histórico), espelhando o padrão da aba Aprovados (Rev. 2049) mas adaptada pro fluxo de reprovação.** Pedido direto do usuário (IMG_0942): "Liste os reprovados também". Antes, reprovados ficavam diluídos no Histórico junto com pendentes/aprovados/vencidos exigindo filtro manual no Select de status — aba dedicada dá visibilidade direta de quem precisa refazer. **Solução em 1 arquivo (zero schema, zero novo procedure)**: `client/src/pages/sst/IntegracaoSST.tsx` — (1) Tabs config L70: nova entrada `reprovados` entre `aprovados` e `historico`, ícone `XCircle` (já importado L21), desc "Colaboradores que não atingiram a nota mínima · precisam refazer"; (2) TabsContent L138: `<ReprovadosTab companyId={companyId}/>`; (3) Novo componente `ReprovadosTab` (~170 LOC) inserido entre AprovadosTab e AssinarTstDialog. Reusa `trpc.integracaoSST.listarRegistros({status:"reprovado"})` (procedure existente já aceita filtro) e `excluirRegistros` (Rev. 2044 — soft-delete via deletedAt; colaborador volta automaticamente pra Pendentes). **UI/UX**: banner vermelho explicativo "Reprovados NÃO recebem certificado e precisam refazer"; header com contador + busca por nome/CPF/obra; tabela 9 colunas (Colaborador · CPF · Função · Obra · Nota badge vermelho · Mínima · Tentativas badge outline · Reprovação data · Ações); 2 ações por linha: (a) **"Liberar"** botão âmbar outline com `RefreshCw` (apaga via excluirRegistros após AlertDialog âmbar — devolve pra Pendentes); (b) **"Raio-X"** Link wouter pra `/raio-x/{employeeId}`. Estados padrão: loading (Loader2), erro com retry (AlertTriangle + botão Tentar novamente), vazio motivacional ("Nenhum colaborador reprovado. Bom sinal!"). + `shared/version.ts` → 2055. **Por que NÃO inclui botões de certificado/assinatura/raio-x-de-certificado**: por definição reprovado NÃO TEM certificado emitido (regra do motor Rev. 2046+); mostrar esses botões seria mentir pro RH. **Por que ação "Liberar" e não "Aprovar manualmente"**: liberar é pedagogicamente correto — colaborador refaz, vê onde errou, reforça aprendizado; aprovação manual burlaria o sistema de avaliação e o vídeo de integração (NR-1.7). Se RH quiser exceção, ainda pode editar via Histórico. **Por que reusar `excluirRegistros` em vez de criar `liberarParaRefazer`**: efeito é idêntico (filtro IS NULL em `deletedAt` faz o registro sumir; colaborador volta a aparecer em Pendentes via fluxo Rev. 2044); toast diferenciado ("liberada(s)" em vez de "excluído(s)") deixa semântica clara; ZERO nova procedure = ZERO superfície de ataque + ZERO chance de drift. **R-001/R-007/R-010 OK**: ZERO ALTER/DROP/SQL direto; `excluirRegistros` faz soft-delete cross-tenant (já validado Rev. 2044/2045/2052). **Preservado**: Rev. 2054 (filtro férias no ranking) INTACTA; AprovadosTab INTACTA (apenas adicionei componente novo no mesmo arquivo); HistoricoTab INTACTO (continua mostrando reprovados via filtro "reprovado" — agora redundante mas inofensivo); KPI Card "Reprovados" do DashboardTab (L166) continua mostrando contador. **Follow-up**: (1) exibir motivo detalhado da reprovação (quais perguntas errou — exige join com sst_integracao_respostas); (2) bulk-liberar (selecionar várias reprovações de uma vez, padrão Rev. 2044); (3) e-mail/WhatsApp automático pro colaborador reprovado com link direto pra refazer; (4) histórico de tentativas anteriores (hoje some quando libera — talvez manter como `dataAnularReprovacao` em vez de soft-delete); (5) bloqueio configurável após N reprovações (escalar pra RH/TST presencial em vez de auto-liberar infinitamente).
- **Rev. 2054**: **Fechamento de Ponto · Ranking "Menos Dias Trabalhados" agora EXCLUI colaboradores que estavam em gozo de férias no período.** Pedido direto do usuário (IMG_0941): "Quem tava em gozo de férias, não deveria estar na lista de menos dias trabalhados, não é injusto colocar este funcionário nesta lista?" — sim, é injusto. Quem está de férias por definição NÃO está trabalhando; rankeá-lo como pouco produtivo é erro conceitual e gera viés contra o colaborador. **Caso concreto no print**: MARCELO DE LIMA FELISBERTO (PEDREIRO II) aparecia no top-7 com 3 dias trabalhados + badge "Férias" + "Não justificada" — confusão visual e injustiça evidente. **Solução em 2 arquivos (zero schema, zero contrato quebrado)**: (A) `server/routers/fechamentoPonto.ts` — `getSummary` novo bloco "Rev. 2054 Férias no período" antes do fetch de termination notices. SELECT em `vacationPeriods` (status NOT IN cancelada/pendente, deletedAt IS NULL, cross-tenant via `companyFilter`). Pra cada férias overlapping os bounds do ciclo (`input.dataInicio`/`dataFim` quando presentes, senão fallback pro mês calendário), conta dias overlap em até 3 fracionamentos (`dataInicio`/`dataFim`, `periodo2Inicio`/`periodo2Fim`, `periodo3Inicio`/`periodo3Fim`) usando aritmética de timestamps em UTC-meio-dia (evita drift de DST). Mapa `feriasByEmp[employeeId] = {emFerias, diasFerias}`. 2 props novas no return: `emFerias: boolean` + `diasFerias: number` (opt-out — clientes antigos ignoram silenciosamente). (B) `client/src/pages/FechamentoPonto.tsx` — useMemo `rankings`: (1) propaga `emFerias`/`diasFerias` no objeto data spread (pareado com cipaStatus/fotoUrl da Rev. 2015); (2) `allFaltosos` ganha `&& !e.emFerias` no filtro — colaboradores em férias somem do ranking E do CSV/PDF/contadores (filteredRankingRows deriva de allFaltosos). Pontuais/Atrasados/Extras NÃO mudam — quem está de férias geralmente não tem atrasos nem HE registrados, então o filtro implícito já cuida (`e.atrasos > 0` / `e.horasExtras > 0`). + `shared/version.ts` → 2054. **Por que excluir e não esconder com badge "🏖 Férias"**: o usuário foi explícito ("não deveria ESTAR na lista"). Manter com badge mantém o viés visual (TST/RH ainda lê como "esse cara trabalhou pouco") e polui contador de faltas no rodapé. A coluna "Férias" continua existindo no resumo geral (linha 351 da tela principal) — quem quiser ver dias de gozo continua tendo. **Por que detectar overlap (e não exigir gozo no dia exato)**: férias fracionadas podem cair em parte só do ciclo (ex: 11 dias só na primeira metade do mês); ainda é injusto cobrar produtividade normal — quem usou metade do ciclo em férias por definição trabalhou menos. Se virar problema, endurecer pra "≥ N dias úteis" via threshold no filtro. **R-001/R-007/R-010 OK**: ZERO ALTER/DROP/DELETE. Read-only SELECT em `vacationPeriods`. ZERO nova dependência. ZERO schema. **Preservado**: Rev. 2053/2052/2051/2050 INTACTAS. Modal de detalhe de falta da Rev. 2051 INTACTO (já enxergava férias como categoria separada — agora consistente). CSV/PDF do ranking (Rev. 2031) deriva de filteredRankingRows → herda filtro de graça. **Follow-up**: (1) badge sutil "🏖 N dias de férias no ciclo" no resumo geral (não no ranking); (2) ranking dedicado "Em férias no período" (positivo, pra RH conferir gozo); (3) threshold configurável de dias mínimos de férias pra excluir (hoje 1 dia já exclui — RH pode querer "≥5 dias úteis"); (4) excluir também de pontuais quando férias cobre >50% do ciclo (base muito pequena enviesa pontualidade); (5) marcar "Afastado por INSS" com mesma lógica (auxílio doença ≠ falta — schema já tem dados).
- **Rev. 2053**: **SST · Integração · +10 perguntas sobre NRs e Segurança APPENDADAS ao banco-padrão (Regras de Ouro Rev. 2047 continuam intactas).** Pedido direto do usuário: "Quanto aos questionamentos, mantenha as que já tem, e faça mais 10 perguntas sobre a NR... e segurança". **Decisão arquitetural**: a constante `PERGUNTAS_REGRAS_OURO` em `server/routers/integracaoSST.ts` deixa de ser SÓ as 12 Regras de Ouro de conduta e passa a englobar TAMBÉM 10 perguntas técnicas de segurança — as 12 originais NÃO mudam, só append no fim do array. Nome da constante preservado pra não quebrar imports (`server/_core/index.ts` importa pra auto-migração; `salvarPerguntas` referencia em mensagens de erro). **10 temas novos cobrem**: (1) NR-6 responsabilidade do trabalhador sobre EPI (conservar/devolver — ângulo diferente do item 13 original que é sobre USAR); (2) NR-35 trabalho em altura (cinto paraquedista ANCORADO + treinamento + Análise de Risco); (3) NR-33 espaço confinado (PET + vigia + medição de atmosfera); (4) NR-10 instalações elétricas (não tocar, isolar, avisar eletricista habilitado); (5) NR-12 máquinas (treinamento + autorização + proteções/botão emergência); (6) NR-17/NR-11 ergonomia e movimentação de carga (joelhos dobrados, coluna reta, carrinho/empilhadeira); (7) Acidente de trabalho — comunicar IMEDIATAMENTE + CAT pra todo acidente mesmo pequeno; (8) NR-5 CIPA — todo trabalhador pode/deve levar dúvidas/sugestões de segurança; (9) DDS obrigatório (Diálogo Diário de Segurança); (10) reforço de EPI sob ângulo de conservação. **Linguagem simples (NR-1.7 — colaboradores semi-alfabetizados)**: 3 alternativas cada, 1 correta, sigla da NR aparece apenas na alternativa correta como reforço (não na pergunta — não exige leitura técnica). **Auto-migração da Rev. 2050 continua funcionando SEM mudança**: usa `PERGUNTAS_REGRAS_OURO.length` (não literal 12) — tenants com 12 canônicas serão detectados como stale (`canonicos=12 AND total=12 ≠ 22`) no próximo startup e re-seedados pras 22 atuais; transação atômica; respostas históricas em `sst_integracao_respostas` NÃO afetadas (sem FK rígida, design Rev. 2034). Tenants sem canônicas (módulos customizados pelo cliente via UI) continuam INTOCADOS (filtro `canonicos>0`). **UI do ModulosEditor**: botão "🔄 Atualizar Regras de Ouro" da Rev. 2047 continua funcionando — AlertDialog avisa que perguntas atuais serão substituídas pelas novas (handler `semearPerguntasPadrao({substituir:true})` sempre lê `PERGUNTAS_REGRAS_OURO` atual). **Pontuação**: cada pergunta vale 1 ponto — acertar 16 de 22 = 72.7% (acima do `notaMinima` default 70); RH pode subir `notaMinima` por módulo via `criarConfig`/`atualizarConfig` (1-100) se quiser apertar. + `shared/version.ts` → 2053. **R-001/R-007/R-010 OK**: ZERO ALTER/DROP/SQL direto; re-seed roda em SEED (não dados do usuário) em transação atômica e só em módulos `canonicos>0`. **Preservado**: Rev. 2052/2051/2050/2049/2048/2047 INTACTAS estruturalmente — só `PERGUNTAS_REGRAS_OURO` cresce de 12 → 22 itens. **Por que append no fim (e não intercalado por tema)**: ordem-1..12 das Regras de Ouro de CONDUTA é a coluna vertebral do vídeo "INTEGRAÇÃO FC ENGENHARIA" (Rev. 2047 — 20 prints mapeados pelo usuário); manter a ordem original preserva narrativa pra quem assistiu o vídeo + facilita auditoria visual de qual seed o tenant está rodando. **Follow-up**: (1) embaralhar ordem das 22 perguntas na tela pública (anti-cola); (2) categorizar por tema (conduta/segurança técnica) com filtro RH; (3) banco de perguntas reutilizável editável via UI pelo admin_master; (4) variações por função/CBO (eletricista, soldador, operador de empilhadeira).
- **Rev. 2052**: **SST · Integração · Assinatura DIGITAL do TST no certificado (FCSign canvas inline) — desenha no canvas, embute imagem PNG sobre a linha de assinatura no PDF, com nome do TST e timestamp "assinado eletronicamente em".** Pedido direto do usuário (IMG_0940): "Quero assinatura do tst online.., vamos usar o fcsing que criamos". **Investigação**: "FCSign" = sistema IntegraSign (envelopes/multi-signatário externo com token+email+ordem+audit log) — overkill pra TST interno do RH assinando muitos certificados; padrão alternativo `EpiAssinatura.tsx` (HTML5 Canvas + touch+mouse + toDataURL PNG) é mais leve e adequado. Decisão: reusar canvas signature pad inline no modal de assinatura. **Solução em 5 partes**: (A) `drizzle/schema.ts` — 3 cols novas em `sstIntegracaoRegistros`: `assinaturaTstBase64` (TEXT), `assinaturaTstNome` (VARCHAR 255), `assinaturaTstAssinadaEm` (TIMESTAMP); coluna `certificadoUrl` existente continua reservada (design Rev. 2049). (B) `server/_core/index.ts` — 3× `ALTER TABLE ADD COLUMN IF NOT EXISTS` logo após "Tabelas SST Integração garantidas" (idempotente). (C) `server/routers/integracaoSST.ts` — 2 procedures: (1) `assinarComoTst({companyId, registroId, assinaturaBase64, nomeTst})` valida prefix `data:image/png;base64,`, tamanho ≤3MB (Zod), nome ≥2 chars, cross-tenant (assertCompanyAccess + SELECT com WHERE companyId), só permite se status='aprovado', update + NOW(); console.error NÃO loga base64 cru (loga só tamanho — LGPD); (2) `removerAssinaturaTst({companyId, registroId})` set as 3 cols pra null + updatedAt NOW() cross-tenant; `listarRegistros` (Rev. 2049) já espalha `...r` então as 3 cols novas vêm de graça — ZERO mudança no procedure. (D) `certificadoIntegracaoSstPdf.ts` — interface ganha 3 params opcionais; bloco da linha de assinatura reescrito: se base64 presente desenha imagem 70mm×18mm centrada sobre a linha via `pdf.addImage(..., "PNG", ...)`; linha cinza embaixo preservada (visual formal); se nome presente: UPPERCASE em FC_NAVY bold + "Técnico de Segurança do Trabalho (TST)" abaixo + "Assinado eletronicamente em DD/MM/YYYY HH:mm" cinza menor; sem assinatura → comportamento antigo intacto (fallback). (E) `IntegracaoSST.tsx` (AprovadosTab + novo `AssinarTstDialog`) — imports +PenLine +CheckCircle2; `certParamsFromRegistro` propaga as 3 props pro PDF (botões Ver/Baixar enviam assinatura automaticamente); coluna nova "Assinatura TST" na tabela (entre Validade e Certificado): se assinado → Badge verde "✓ Assinado" + lixeira (AlertDialog de confirmação); se não → botão azul outline "Assinar" abre `AssinarTstDialog` (~150 LOC inline): useRef<HTMLCanvasElement>, canvas 2× devicePixelRatio, handlers mouse+touch (`touch-none` + preventDefault evita scroll iPad), botão Limpar, input Nome obrigatório, banner azul com colaborador+CPF+nota+data, DialogContent max-w-2xl. + `shared/version.ts` → 2052. **R-001/R-007/R-010 OK**: 3 ADD COLUMN IF NOT EXISTS aditivos; `removerAssinaturaTst` faz UPDATE SET NULL (não DELETE) e só em registros desta empresa. **Preservado**: Rev. 2051/2050/2049/2048/2047 INTACTAS; certificadoUrl col reservada continua disponível. **Por que canvas inline e não IntegraSign completo**: IntegraSign é multi-signatário externo com token URL público — pesado pra um TST interno assinando dezenas de certificados/dia; modal inline ganha de longe em UX. **Por que `removerAssinaturaTst` em vez de reassinar direto**: 2 passos com AlertDialog mostrando qual TST assinou antes evita sobrescrita acidental (auditoria visual). **Follow-up**: (1) capturar IP+UserAgent+geo (auditoria padrão IntegraSign); (2) hash SHA-256 da PNG pra detectar adulteração; (3) upload de assinatura escaneada (PNG transparente) como alternativa ao canvas; (4) role específica `sst_tst` quando virar multi-tenant; (5) bulk-sign (assinar várias aprovações de uma vez com assinatura padrão do TST).
### Revisões recentes (one-liners)

- ~~Rev. 2051~~ — Fechamento de Ponto: modais de Ranking (Pontuais / Mais HE / Menos Dias) ganham memória de cálculo clicável em TODAS as colunas + responsivo mobile (2 procedures novos `getHeDetalhe`+`getFaltaDetalhe`). Ver `shared/changelog.ts`.
- ~~Rev. 2050~~ — SST Integração: AUTO-MIGRAÇÃO no startup das 12 perguntas-padrão "Regras de Ouro" — corrige tenants com seed antigo da Rev. 2046 OU duplicatas (idempotente, cross-tenant, transação atômica). Ver `shared/changelog.ts`.
- ~~Rev. 2049~~ — SST Integração: nova aba "Aprovados" com Visualizar/Baixar Certificado + atalho pro Raio-X do colaborador. Ver `shared/changelog.ts`.
- ~~Rev. 2048~~ — SST Integração: certificado ganha logo da FC + cores da marca + headline "Parabéns!" + destaque na validade + botão "Visualizar / Imprimir" ao lado de "Baixar PDF". Ver `shared/changelog.ts`.
- ~~Rev. 2047~~ — SST Integração: 12 perguntas-padrão REESCRITAS fiéis ao vídeo (cultura + 10 Regras de Ouro) + botão "🔄 Atualizar Regras de Ouro" com AlertDialog. Ver `shared/changelog.ts`.
- ~~Rev. 2046~~ — SST Integração: botão "Carregar Regras de Ouro" + 12 perguntas-padrão semeadas no módulo (handler `semearPerguntasPadrao` cross-tenant idempotente). Ver `shared/changelog.ts`.
- ~~Rev. 2045~~ — SST Integração aba Histórico: confirmação de exclusão via AlertDialog (substitui window.confirm que mostrava domínio do Replit no popup). Ver `shared/changelog.ts`.
- ~~Rev. 2044~~ — SST Integração aba Histórico: editar/apagar registros + múltipla seleção; ao excluir, colaborador volta automaticamente para "Pendentes" (soft-delete via deletedAt). Ver `shared/changelog.ts`.
- ~~Rev. 2043~~ — SST Integração "Iniciar agora": pula passo de CPF quando RH já selecionou o colaborador (link `?cpf=...&auto=1`, parse no mount + useEffect single-fire). Ver `shared/changelog.ts`.
- ~~Rev. 2042~~ — SST Integração "Iniciar agora": CAUSA-RAIZ — SELECT usava `employees.nome` (correto é `nomeCompleto`); fix em 3 handlers + try/catch. Ver `shared/changelog.ts`.
> Revisões anteriores à 2042 (incluindo a 2041): ver [`replit-history.md`](./replit-history.md) e `shared/changelog.ts` (detalhe completo).


## User preferences

- Idioma de comunicação: pt-BR direto e objetivo.
- Toda revisão DEVE: editar código + bumpar `shared/version.ts` + adicionar entrada NO TOPO de `shared/changelog.ts` + atualizar `replit.md` (ver convenção acima — Top-5 detalhado + 10 recentes em one-liner).
- R-001 / R-007 / R-010: JAMAIS executar `ALTER TABLE`, `DROP`, ou `DELETE` em produção.
