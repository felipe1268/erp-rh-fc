# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per módulo
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (`shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> **Convenção (importante)**: este arquivo guarda APENAS as últimas **5 revisões**, em formato curto (1–3 linhas: o quê + por quê).
> Quando entrar uma nova revisão, **remova a mais antiga daqui** — o histórico completo (com causa-raiz, stack traces, nomes de arquivos, etc.) vive em `shared/changelog.ts`.
> Não duplique conteúdo entre os dois arquivos.

- **Rev. 1882**: **Gestão de Documentos · Documentos da Obra · BOTÕES DE GESTÃO DE PASTAS VISÍVEIS NO iPAD + criação inline de nova categoria**. User (16/05/2026, após Rev. 1881, screenshot iPad): "Preciso de um botão para criar subpastas, editar nome, excluir e criar novas pastas tbm". **Causa**: (a) os 5 botões já existiam mas atrás de `opacity-0 group-hover:opacity-100` — sem hover no iPad ficavam invisíveis; (b) acervo "documento" só tinha botão "Catálogo" (configurações), sem criação inline; (c) backend `createDisciplinaFicheiro` (`server/routers/gestaodocumentos.ts` L399) não aceitava `tipoAcervo` — toda pasta nova caía em "projeto". **Mudanças**: (1) Backend L399-440: input ganhou `tipoAcervo: z.enum(["projeto","documento"]).optional()`, insert grava o valor; sub-pastas padrão DWG/PDF/IFC/DOC só p/ "projeto" (documento nasce vazio). (2) Frontend `client/src/pages/gestaodocumentos/index.tsx` L1052-1060 + L1085-1093: ambas as chamadas mutateAsync passam `tipoAcervo: acervoAtivo`. (3) L1690-1711: header "documento" virou 2 botões — "Nova" (emerald, reusa `setShowNewDiscModal(true)`) + "Catálogo". (4) L1734-1794 (disciplina) + L1820-1838 (sub-pasta): removido `opacity-0 group-hover*` de 7 botões (FolderDown, FolderPlus, Pencil x2, Trash2 x2), ícones w-3→w-4 / w-2.5→w-3.5, padding p-0.5→p-1, cores fixas `text-gray-500 hover:text-{cor} hover:bg-{cor}-50` — alvos de toque ~24px (guidelines Apple). (5) L2306-2378 modal "Nova Pasta": título/ícone adaptativos (emerald p/ documento), grid de atalhos de disciplinas técnicas escondido em "documento" (categorias são livres — CTR, ART, PRO), placeholders trocam (Contratos & Aditivos / CTR vs Arquitetura / ARQ). **Preservado**: fluxo "projeto" idêntico; rename/delete usam endpoints já existentes; multi-tenant via `assertFicheiroAccess` intacto; bulkCreateDisciplinasFicheiro inalterado. Zero ALTER/DROP/DELETE. Reversível em ~9 hunks. R-001/R-007/R-010 OK.
- **Rev. 1881**: **Frotas · Dashboards SEPARADOS e DETALHADOS — NOVOS "Dash Combustível" + "Dash Pedágios" (Dash Manutenções já existia; Dashboard Frotas = visão geral)**. User (16/05/2026, após Rev. 1880): "preciso de dashboards separados e detalhados em frotas, um para manutenção, outro de pedágios, outro combustível e outra juntando tudo o mais importante". **Mudanças**: (1) Backend `server/routers/frotas.ts` L4422-4716: 2 endpoints novos após `getMaintenanceDashboard` — `getFuelDashboard` (kpi com qtd/litros/valor/desconto/veículos/motoristas/postos/preço médio-min-max via AVG(NULLIF)/consumo médio, porMes, porVeiculo c/ consumo km/L DERIVADO de (km_max-km_min)/litros + custoPorKm, porMotorista TOP30, porPosto TOP30, porTipo, topNotas TOP15 auditoria) e `getPedagiosDashboard` (kpi c/ split pedágio físico vs sem_parar — qtd+valor, porMes c/ split, porVeiculo c/ split + praças distintas, porPraca TOP30 com rodovia, porRodovia TOP20, topPassagens TOP15 c/ tag+eixos). Ambos com guard `ctx.user.companyId` (FORBIDDEN se ≠ input.companyId) + multi-tenant em todos os WHERE. (2) Frontend NOVO `CombustivelDashboard.tsx` (~370L, gradiente emerald): 8 KPIs + card âmbar de FAIXA DE PREÇO (min/médio/máx + variação%) + ComposedChart barras+2 linhas (Valor/Litros/Preço R$L) + PieChart por tipo + BarChart horizontal TOP15 postos + ranking motoristas + tabela detalhada sortável (7 colunas) c/ consumo colorido por faixa (≥10 verde, ≥6 âmbar, <6 vermelho) + mini-barra de proporção + tabela top15 maiores notas auditoria. (3) Frontend NOVO `PedagiosDashboard.tsx` (~330L, gradiente sky): 8 KPIs + ComposedChart barras EMPILHADAS pedágio+semParar+linha qtd + PieChart físico vs Sem Parar c/ legenda lateral + BarChart TOP15 praças + tabela scroll rodovias TOP20 + tabela detalhada por veículo c/ split + tabela top15 maiores passagens c/ badge categoria + tag + eixos auditoria. (4) Wire-up: App.tsx L291-292 (lazy) + L548-549 (Routes c/ RouteGuard reaproveitando permissões "combustivel"/"pedagios"); shared/modules.ts L515-516 (sidebar "Dash Combustível" Fuel + "Dash Pedágios" Receipt após Dash Manutenções); shared/modulePages.ts L679-680 (mapping). **Preservado**: PainelFrotas (Dashboard Frotas) = visão geral juntando tudo, FrotasAnalitico, ManutencoesDashboard, CRUDs intactos. Zero ALTER/DML — apenas SELECTs read-only. Reversível em ~7 hunks + 2 arquivos novos. R-001/R-007/R-010 OK.
- **Rev. 1880**: **Almoxarifado · NOVO MÓDULO "Controle de Ferramentas de Terceiros" (portaria de obra) — entrada/saída com fotos obrigatórias + rastreamento completo**. User (16/05/2026, após Rev. 1879, screenshot Almoxarifado Central): "Preciso criar um módulo para controlar o acesso de ferramentas de terceiros para garantir que não terá confusão na hora da saída, quero algo detalhado, com fotos para não ter erro, quero saber o nome de quem lançou a entrada, quem levou, quem retirou.. controle total do que entra e sai da obra". **Contexto**: Almoxarifado existente controla itens próprios; terceirizadas/locadoras/autônomos trazem ferramentas próprias — sem registro vira disputa de propriedade ao final. **Mudanças**: (1) Schema 2 tabelas novas: `ferramentas_terceiros_registros` (header c/ tipo ENTRADA|SAIDA, empresa, CNPJ, responsável nome/CPF/tel, quem entregou, quem recebeu, lancado_por_user_id+nome snapshot, registro_pai_id, foto_documento_url, status em_obra|devolvido_parcial|devolvido_total|concluido) + `ferramentas_terceiros_itens` (linha c/ descricao, marca, modelo, numero_serie, qtde, foto_url OBRIGATÓRIA, condicao, item_entrada_id, status_item na_obra|devolvido|perda|danificada). (2) SyncSchema+ L889-937: CREATE TABLE IF NOT EXISTS idempotente + 5 índices. (3) Backend novo `ferramentasTerceiros.ts` (~380L): 7 endpoints — kpis, listarRegistros (filtros+JOIN), getById, itensNaObraPorRegistro, entradasEmAberto, criarEntrada (upload de TODAS fotos ANTES de persistir DB, multi-tenant, audit), criarSaida (valida pai + itens são do pai+'na_obra', cria espelho c/ item_entrada_id, atualiza status_item original, recalcula status pai), remover (admin soft delete). Validação foto 8MB/mime server-side. Mount em routers.ts L181. (4) Frontend `FerramentasTerceiros.tsx` (~620L): KPIs (5 cards), filtros (busca/tipo/status), tabela. compressToBase64 reduz fotos iPad p/ 1024px q=0.78. 3 modais fullscreen (Regra de Ouro): ENTRADA (empresa+obra+responsável + lista dinâmica c/ foto OBRIGATÓRIA por item), SAÍDA (Select entrada pai + itens 'na_obra' clicáveis c/ foto SAÍDA + condição + status devolvido/danificado/perda), DETALHES (header + grid itens c/ foto clicável). Câmera traseira via `capture="environment"`. (5) Rota `/almoxarifado/ferramentas-terceiros` em App.tsx L507, feature em modules.ts L361 (icon Wrench), mapping em modulePages.ts L655. **Segurança**: multi-tenant em todos queries; upload ANTES de DB (rollback grátis); validação na SAÍDA garante itens pertencem ao pai+'na_obra'; admin-only remove. **Preservado**: warehouse router intacto. R-001/R-007/R-010 OK — só CREATE TABLE IF NOT EXISTS. Reversível em ~7 hunks + 2 arquivos novos.
- **Rev. 1879**: **Férias · Fluxo de Caixa Prévio — DEMARCAR o que já foi PAGO (badge ✓ PAGO + barras empilhadas + cards Pago/A Pagar + Gantt diferenciado)**. User (16/05/2026, após Rev. 1878, screenshot Fluxo 2026 com vários "VENCIDA" sem distinguir o que já saiu do caixa): "Demarque aqui o q já foi pago para facilitar a Análise". **Causa**: backend `fluxoCaixa` (avisoPrevioFerias.ts L2538) só projetava `status`, descartando `dataPagamento`. Status `concluida` (lote "Já foi pago"), `em_gozo` e `agendada` com pgto no passado representam fluxo já EXECUTADO mas apareciam iguais aos previstos. **Mudanças**: (1) Backend L2562-2587: `dbInfoMap` agora projeta `{status, dataPagamento}` + `hojeStr` p/ comparação YYYY-MM-DD determinística. (2) L2612-2643: novo `pago: boolean` por funcionário = `(status concluida|em_gozo) || (dataPagamento <= hoje)`; emite `dataPagamento` no payload. (3) L2657-2679: agregados mensais `totalPago/totalAPagar/qtdPagos/qtdAPagar`. (4) Frontend `Ferias.tsx` ~L1626-1638 card mensal: chip emerald ✓ Pago + chip amber A Pagar (só quando há diferenciação). (5) L1659-1671 mini-lista: badge `✓ PAGO` (emerald-600) com prioridade sobre VENCIDA + line-through+opacity-70 no nome. (6) L1699-1761 gráfico mensal: reescrito p/ barras EMPILHADAS emerald (pago) + amber (a pagar), tooltip com split. (7) L1781-1847 Gantt: cor `bg-emerald-600` com ✓ branco quando `entry.pago`, sobrepõe vencida/agendada; legenda "Pagas ✓" em destaque. (8) L1924-1942 dialog: 2 cards Pago vs A Pagar (sempre visíveis). (9) L1960-1982 tabela detalhada: linha emerald-50/40, nome line-through, total emerald-700, badge `✓ PAGO` + dataPagamento pt-BR. **Preservado**: separação 1º/2º+, modal fullscreen (Regra de Ouro), confirmarVencidasLote, totais gerais, navegação por ano. Schema intacto, 0 ALTER/INSERT/DELETE. Reversível em ~9 hunks. R-001/R-007/R-010 OK.
- **Rev. 1878**: **Colaborador · Termo de Isenção de Controle de Jornada (Art. 62 CLT) — gerar PDF + imprimir + upload assinado + Raio-X**. User (16/05/2026, após Rev. 1877): pediu botão p/ GERAR TERMO PDF, IMPRIMIR, UPLOAD do termo assinado e exibir no RAIO-X. **Mudanças**: (1) Schema `drizzle/schema.ts` L1029-1032 + SyncSchema+ L864-867: 3 colunas em employees — `cargo_confianca_termo_url/nome_arquivo TEXT` + `cargo_confianca_termo_assinado_em TIMESTAMP` (idempotente). (2) Backend `server/routers.ts` L749-797: mutations `uploadTermoArt62({employeeId,companyId,fileBase64,mimeType,fileName})` (pdf/jpg/png, ≤10MB, key `art62-termos/{cId}/{empId}/termo-art62-{ts}.{ext}` via storagePut, UPDATE multi-tenant, audit `UPLOAD_TERMO_ART62`) e `removerTermoArt62`. (3) Frontend Colaboradores L372-547 + L2362-2415: mutations + `imprimirTermoArt62` que gera HTML completo em nova aba (window.print), com texto adaptativo por inciso (5 cláusulas — fundamento legal, efeitos, remuneração+grat, reversibilidade, boa-fé + box de observação + assinaturas empregador/empregado/2 testemunhas). UI: botão 'Gerar/Imprimir Termo' (gated por inciso selecionado) + input file 'Upload do Termo Assinado' (gated por editingId) + linha verde com link/data quando há termo + botão X remover. (4) RaioXFuncionario L965-1019: novo card índigo 'Isenção de Controle de Jornada' (visível só se cargoConfianca=1) com badge do inciso, data desde, gratificação% (II), descrição da regra, observação, link 'Ver Termo Assinado' ou aviso âmbar. `db.select().from(employees)` em controleDocumentos.ts L1305 já devolve tudo — sem mudança no backend RaioX. **Preservado**: fechamentoPonto, folha, HE, Espelho Rev. 1877. Reversível em ~6 hunks. R-001/R-007/R-010 OK.
