# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per módulo
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (`shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> **Convenção (importante)**: este arquivo guarda APENAS as últimas **5 revisões**, em formato curto (1–3 linhas: o quê + por quê).
> Quando entrar uma nova revisão, **remova a mais antiga daqui** — o histórico completo (com causa-raiz, stack traces, nomes de arquivos, etc.) vive em `shared/changelog.ts`.
> Não duplique conteúdo entre os dois arquivos.

- **Rev. 1944**: **RH · Dash Aviso Prévio · Modal drill-down REDESENHADO** (Vencendo 7d/30d/Total/Status). User (16/05/2026, screenshot modal "Avisos vencendo em até 7 dias (1)"): "melhore este layout conforme nossas regras de ouro..". **Mudança** (`DashAvisoPrevio.tsx`): (a) novo `drillDownAvisosOrdenados` ordena venc7/venc30 por `dataFim ASC` (mais urgente no topo); (b) helpers `tipoChipColor` (Trab=azul/Ind=vermelho) e `diasAteVencer`; (c) Dialog reescrito: **header colorido por urgência** (vermelho/âmbar/slate) com ícone badge + contagem pill + subtítulo contextual; **cards** com avatar inicial + nome clicável (→ Raio-X) + chips tipo/dias/redução + período com ícone + badge urgência ("Vence hoje/amanhã/em Xd/Atrasado Xd") + badge status; **empty state** elaborado; **footer sticky** com contagem + total destacado. Reaproveita helpers existentes. version → 1944. **Resultado**: triagem em 1 olhada — quem vence primeiro (vermelho), quem tem redução (chip roxo), Trab vs Ind por cor, total grande no rodapé. **Preservado**: Rev. 1942 (KPIs clicáveis), Rev. 1943 (Lei 12.506), Rev. 1941 foto, Rev. 1936 CIPA. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
- **Rev. 1943**: **RH · Aviso Prévio + CDM · CORREÇÃO JURÍDICA — Lei 12.506/2011 +3d/ano aplica nas DUAS modalidades (Trabalhado E Indenizado)**. User (16/05/2026, screenshots Trab R$ 1.131k vs Ind R$ 1.440k): "tem um erro conceitual aqui.. o juridico aconselhou que a lei não fala que so acrescenta 3 dias por ano, para quando o aviso for indenizado.. tem varios entendimentos que deve ser considerado 3 dias a cada ano, para as duas situações". **Confirmado**: Lei 12.506 Art. 1º Parágrafo único fala "ao aviso prévio... serão acrescidos 3 dias por ano" sem distinguir tipo — corrente majoritária TST (Súm. 441 + doutrina) aplica às DUAS. Antes usávamos 30 fixos pra trabalhado (MTE NT 184/2012 revogada em 2014, minoritária) → risco de condenação. User escolheu opção A. **Mudança server**: (a) `avisoPrevioFerias.ts` L918→927: `diasAvisoTrab = 30` → `calcularDiasAvisoTotal(anosServico)`; (b) `dashboards.ts` L2510→2516: remove ternário → `calcularDiasAvisoTotal(anosBase)` direto. **Mudança client**: (c) `DashAvisoPrevio.tsx` (3 hunks): copy header CDM, tooltip Trabalhado, rodapé legal; (d) `AvisoPrevio.tsx` (4 hunks): `diasAviso` descritivo, label "30 fixos" só pra pedido empregado, ternário preview, **painel Base Legal REESCRITO** com texto literal Art. 1º + "lex non distinguit" + tabela DUAS modalidades iguais (30/33/42/60/75/90) + histórico NT 184 + exceção CLT 487 §2º + Súm. 441 TST. version → 1943. **Resultado**: CDM Trab≈Ind, módulo individual aplica corrente majoritária por padrão, disclaimer legal completo auditável. **Preservado**: rescisaoCalc interno (L358-362 já estava correto), todos cálculos, drill-down Rev. 1942, foto Rev. 1941, CIPA Rev. 1936. Zero ALTER/DROP/DELETE. Reversível em 9 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1942~~ (removida do top-5 pela Rev. 1943 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · KPIs do topo (Total / Vencendo 7d / Vencendo 30d) agora CLICÁVEIS — drill-down lista QUAIS avisos compõem o número**. User (16/05/2026, screenshot cards "Vencendo em 7 dias = 1" e "30d = 3"): "QUERO QUE TODOS OS CARDS SEJAM RESPONSIVOS PARA FACILITAR A VIDA NO DIA A DIA.. E ANALISAR DA ONDE VEM A INFORMAÇÃO..". **Motivação**: cards só mostravam o número agregado — RH precisava saber QUEM são os funcionários pra agir. Em Andamento/Concluídos/Cancelados já eram clicáveis. **Mudança** (`DashAvisoPrevio.tsx`): (a) Card "Total" ganha onClick → drill-down `finTotal` (já existia, só faltava trigger); (b) Vencendo 7d/30d → novos types `venc7`/`venc30`; (c) novo bloco no `drillDownAvisos` aplica MESMA regra do server (`dashboards.ts` L2864-2865) — `status='em_andamento'` AND `dataFim BETWEEN hoje AND hoje+N` (paridade absoluta com o número); (d) dialog title com ícones próprios + contagem entre parênteses; (e) sub-label "· clique para ver" pra afordância. version → 1942. **Resultado**: clique em qualquer KPI abre modal com nome+período+valor+status — triagem em 1 clique. **Preservado**: drill-down já existente, Raio-X Rev. 1935, foto Rev. 1941, tag CIPA Rev. 1936, redim. colunas Rev. 1939, sticky Rev. 1924. Zero backend/DB. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1941~~ (removida do top-5 pela Rev. 1942 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · Foto do funcionário ao lado do nome + modal de ampliação ao clicar**. User (16/05/2026, 2 msgs): "COLOCA A FOTO DO FUNCIONARIO AO LADO DO NOME.." + "QUANDO EU CLICAR NA FOTO, QUERO QUE AUMENTE O TAMANHO PARA PODER VER MELHOR..". **Mudança server** (`dashboards.ts` getDashCustoDemissaoMassa): SELECT ganha `fotoUrl: employees.fotoUrl`. **Mudança client** (`DashAvisoPrevio.tsx`): (a) novo state `fotoAmpliada`; (b) avatar 28×28 round à esquerda do nome (dentro do flex Rev. 1936), `object-cover object-top` p/ enquadrar rosto; (c) fallback bolinha cinza com inicial p/ funcionários sem foto; (d) click no avatar → modal `fixed inset-0 bg-black/80 z-[100]` com imagem `max-h-[80vh] object-contain` + nome em card + ✕ canto; (e) clique fora fecha (`cursor-zoom-out`). version → 1941. **Resultado**: identificação visual instantânea na tabela; clique amplia pra reconhecimento confiável. **Preservado**: tag CIPA Rev. 1936, Raio-X Rev. 1935, tempo a/m/d Rev. 1934, sticky Rev. 1924, sort Rev. 1909, redim. colunas Rev. 1939. Zero ALTER/DROP/DELETE. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1940~~ (removida do top-5 pela Rev. 1941 — ver `shared/changelog.ts`). **Planejamento · ProgramacaoSemanalLotus · Excel · Coluna TAREFA (D) com largura AUTO-AJUSTÁVEL ao conteúdo mais longo**. User (16/05/2026, screenshots Sem.04 Santuário N.S. Aparecida — antes: "MOBILIZAÇÃO DE EQUIPES, EQUIPAMENTOS..." truncado; depois: texto inteiro visível): "precisia ter uma regra para a coluna TAREFA, que a largura dela é variavel em função do texto que esta dentro dela..". **Causa**: Rev. 1886/1889 hardcoded `4: 50` em `minWidths` — cortava grupos longos (~85 chars) tipo "TAPUMES METÁLICOS PARA ISOLAMENTO DAS ÁREAS DE ATUAÇÃO DE APOIO - PADRÃO SANTUÁRIO". **Mudança** (`preencherAba`): (a) Removido `4: 50` de `minWidths`; (b) novo bloco itera `buildLinhas(ats)` (cobre grupos `.toUpperCase()` + atividades), acha `maxLen`; (c) `tarefaW = clamp(Math.ceil(maxLen * 1.05) + 4, 50, 120)` — fator 1.05+padding cobre bold dos grupos; MIN 50 preserva projetos curtos, MAX 120 evita largura absurda; (d) executa por aba (1×/semana) — semanas curtas ficam compactas. version → 1940. **Resultado**: textos nunca ocultos no Excel; identico ao 2º screenshot. **Preservado**: dowToExcelCol Rev. 1926, larguras E-I Rev. 1889, cinza_fds Rev. 1893, ehUtilCal Rev. 1914, cutoff Rev. 1913, template xlsx do cliente intacto. Zero backend/DB. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- ~~Rev. 1939~~ (removida do top-5 pela Rev. 1940 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · REWRITE redimensionamento estilo Excel (window listeners nativos)**. User (16/05/2026): "esta pessima a navegabildiade para arurmar a largura da coluna.. melhore isso, use o mesmo sistema que usamos no excel.. o basico bem feito..". **Causa Rev. 1937/1938**: `CdmResizeHandle` declarado INLINE no componente pai → remount a cada `setCdmColW` no drag → `setPointerCapture` perdia referência → drag travava após 1 frame. **Mudança** (`DashAvisoPrevio.tsx`): (a) `startCdmResize(key)` registra listeners NATIVOS em `window` (pointermove/up/cancel) — sobrevivem a re-renders; (b) handle agora é JSX INLINE via `.map(['nome','funcao','obra'])` (sem subcomponente, sem remount); (c) `cdmStartCdmColWRef` p/ persistir no localStorage só no pointerUp (1 write, não a cada frame); (d) `userSelect:none` no body durante drag; (e) visual neutro Excel-like: linha vertical 1px cinza, hover âmbar-100 sutil, cursor col-resize; (f) header dividido (span clicável sort + div handle stopPropagation). version → 1939. **Resultado**: hover na borda direita → cursor ↔ → clique-segura-arrasta. Idêntico ao Excel. **Preservado**: state Rev. 1937 (defaults/min/max), th+td widths, CIPA Rev. 1936, Raio-X Rev. 1935, sticky Rev. 1924, sort Rev. 1909. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1938~~ (removida do top-5 pela Rev. 1939 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · FIX UX do redimensionamento (Rev. 1937) — handle agora visível + click não dispara sort**. User (16/05/2026, screenshot CARAMURU - HH - CA... ainda truncado): "nao ficou bom para ajudar a largura da tela, considera que precisa clicar, segurar e arraastar para modificar a largura..". **Causa**: handle Rev. 1937 era 8px com barra cinza só visível no hover (afordância ruim) + `stopPropagation` no pointerDown não impede o click sintético do React → clique no handle disparava `toggleCdmSort` do th pai. **Mudança** (`CdmResizeHandle`): (a) w-2→w-4 com `-right-2 z-30` (alvo táctil 2× maior, metade fora da célula); (b) barra interna âmbar-400 SEMPRE visível + hover âmbar-600 + active âmbar-700; (c) `onClick stopPropagation` explícito; (d) `onDoubleClick` idem + reset; (e) `title` reescrito "Clique, segure e arraste...". version → 1938. **Resultado**: barra âmbar óbvia nos 3 headers; arrasta sem disparar sort. **Preservado**: tudo de Rev. 1937 (state, handlers Pointer Events, localStorage, defaults), CIPA Rev. 1936, Raio-X Rev. 1935, sticky thead Rev. 1924. Zero backend/DB. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- **Rev. 1937**: **RH · Dash Aviso Prévio · Tabela CDM · Colunas de texto (Funcionário/Função/Obra) redimensionáveis por drag**. User (16/05/2026): "quero pode clicar e aumentar a largura da tabela para ajustar o texto..". **Mudança** (`DashAvisoPrevio.tsx`): (a) import `useRef`; (b) state `cdmColW` (nome/funcao/obra) persistido em `localStorage` key `cdm-colw-v1`, MIN=80/MAX=600; (c) handlers Pointer Events (mouse+touch+pen) com `setPointerCapture` + cursor global; (d) `onDoubleClick` reseta default; (e) componente `CdmResizeHandle` na borda direita de cada th (barra âmbar no hover, `touchAction:none`); (f) th + td correspondentes recebem `style={{width,minWidth,maxWidth}}` (necessário com `border-separate` Rev. 1924); (g) `stopPropagation` no pointerDown não conflita com toggle sort. version → 1937. **Resultado**: arrasta a borda do header (desktop/tablet/celular) — texto deixa de truncar; duplo-clique restaura; persiste. **Preservado**: tag CIPA Rev. 1936, Raio-X Rev. 1935, tempo a/m/d Rev. 1934, sticky thead Rev. 1924, sort Rev. 1909, filtros, top-3, KPIs. Zero backend/DB. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- **Rev. 1936**: **RH · Dash Aviso Prévio · Tabela CDM · Tag laranja "CIPA" ao lado do nome (estabilidade — não exclui)**. User (16/05/2026): "marque uma tag de quem faz parte da cipa e não podemos fazer aviso devido a estabilidade.. so demarca para saber quem é...". **Base legal**: CF Art. 10 II 'a' ADCT + CLT Art. 165 + Súm. 339 TST. **Mudança server** (`dashboards.ts`): novo batch query `cipa_members` filtrando `statusMembro='Ativo'` E `fimEstabilidade>=dataRef`; Map `cipaByEmp` O(N); retorno ganha `isCipa`/`cipaCargo`/`cipaFimEstabilidade`. **Mudança client** (`DashAvisoPrevio.tsx`): célula nome vira `flex gap-1.5`, tag laranja `bg-orange-100 text-[9px]` à direita do botão Raio-X com tooltip (cargo+fim+base legal). version → 1936. **Resultado**: nomes CIPA visualmente sinalizados; custo permanece na soma. **Preservado**: Raio-X Rev. 1935, tempo a/m/d Rev. 1934, idade Rev. 1931, filtros Rev. 1915/1923, queries Rev. 1911/1927, projeção, complementar Rev. 1919, sort, top-3, KPIs. Zero ALTER/DROP/DELETE. Reversível em 3 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1935~~ (removida do top-5 pela Rev. 1939 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · Clicar no nome do funcionário abre o Raio-X**. User (16/05/2026): "quando clicar no nome do funcionario, abra a tela de raiox dele... para saber quem é..". **Motivação**: contexto completo antes de decidir demissão; Raio-X já existe em Colaboradores/AvisoPrevio/Ferias/FechamentoPonto/ControleDocumentos — padronização. **Mudança** (`DashAvisoPrevio.tsx`): (a) import `RaioXFuncionario`; (b) state `raioXEmployeeId`; (c) célula do nome vira `<button>` azul com hover:underline preservando truncate/layout; (d) modal renderizado ao final. version → 1935. **Resultado**: clique no nome → modal Raio-X (dados pessoais/contratuais/histórico/documentos). Zero impacto em sort/print/scroll. **Preservado**: tempo a/m/d Rev. 1934, idade Rev. 1931, filtros Rev. 1915/1923, queries Rev. 1911/1927, projeção Rev. 1909-fix, complementar Rev. 1919, diasAvisoEstimado Rev. 1930, tipo Rev. 1921, top-3, KPIs. Zero backend/DB. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1934~~ (removida do top-5 pela Rev. 1938 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · Coluna "Tempo de empresa" agora mostra anos/meses/dias**. User (16/05/2026, screenshot mostrando "14 / 13 / 13 / 12 / 10..."): "quero anos, meses e dias..". **Motivação**: Rev. 1931 renomeou header mas valor era só anos completos (Math.floor) — RH precisa do detalhe pra estabilidade e faixa Lei 12.506. **Mudança server** (`dashboards.ts`): novo bloco calcula `tempoAnos`/`tempoMeses`/`tempoDias` (diff calendar civil dtAdm→dtRef + borrow no dia/mês). Retorno ganha os 3 campos. **Mudança client** (`DashAvisoPrevio.tsx`): célula renderiza "Xa Ym Zd" condicional (omite componentes zero à esquerda). Sort key `anosServico` preservada (base rescisão = anos completos). version → 1934. **Resultado**: "10a 2m 15d" em vez de só "10". **Preservado**: idade Rev. 1931, filtros Rev. 1915/1923, queries Rev. 1911/1927, projeção Rev. 1909-fix, complementar Rev. 1919, diasAvisoEstimado Rev. 1930, tipo Rev. 1921, sort, top-3, KPIs. Zero ALTER/DROP/DELETE. Reversível em 3 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1933~~ (removida do top-5 pela Rev. 1937 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · FIX botão "✕ Cancelar" (Rev. 1928) não persistia após Salvar**. User (16/05/2026, screenshot pós-Salvar Mosaico com FELIPE ainda nos 4 Ajudantes): "cliquei em cancelar salvei mas ele ainda continua como se não houvesse cancelado... arrume isso". **Causa**: `clearRespFromDescendants` (Rev. 1928) zerava só os descendentes mas mantinha `responsavelLotus` no GRUPO — intenção original era permitir re-replicar com mesmo nome. No Salvar, `aplicarCascataResponsavelGrupos` (L3617) re-propaga o nome do grupo aos filhos → cancel desfeito. **Mudança** (`PlanejamentoDetalhe.tsx`): `clearRespFromDescendants` agora limpa também `out[realIdx].responsavelLotus = ""` (grupo). No save, cascata vê valor vazio → `continue` (L3623) → não re-propaga. version → 1933. **Resultado**: cancel = cancel total; pra replicar de novo, digite o nome no grupo e clique "↓ Replicar" (fluxo idêntico ao 1º uso). **Preservado**: cascadeResp Rev. 1922, replicarResp Rev. 1922, marcarComoGrupo Rev. 1920/1922, onBlur Rev. 1860/1865/1892/1902, modal folhas, aplicarCascataResp Rev. 1910, semântica "grupo=cascata total" Rev. 1892. Zero backend/DB. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- ~~Rev. 1932~~ (removida do top-5 pela Rev. 1936 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · Gantt · FIX `ReferenceError: projetoId is not defined`** (regressão Rev. 1929 — tela branca ao abrir aba Gantt). User (16/05/2026, screenshot "Ocorreu um erro inesperado" + stack `at GanttCronograma`): "erro na tela". **Causa**: Rev. 1929 usou `projetoId` p/ montar key do localStorage dentro do `GanttCronograma` mas o componente não recebia essa prop. **Mudança** (`PlanejamentoDetalhe.tsx`): (a) call site L1129 ganha `projetoId={projetoId}`; (b) signature L5014 ganha `projetoId` no destructuring. version → 1932. **Resultado**: aba Gantt abre; persistência por obra Rev. 1929 funciona. **Preservado**: tudo de Rev. 1929 (LEFT_W resizable, handlers, handle, doubleClick), Rev. 1928 (clearResp), Rev. 1922 (cascadeResp). Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1931~~ (removida do top-5 pela Rev. 1935 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · Renomeia "Anos" → "Tempo de empresa" + nova coluna "Idade"**. User (16/05/2026): "melhore o texto onde ta escrito idade, coloque tempo de empresa.. e coloca outra coluna com a idade real do funcionario". (User confundiu — header era "Anos" de serviço. Renomeia p/ remover ambiguidade + adiciona idade real.) **Mudança server** (`dashboards.ts` getDashCustoDemissaoMassa): SELECT ganha `dataNascimento`; cálculo local de `idade` (anos completos até `dtRef`, não até HOJE — coerente com o resto); fallback null. Retorno ganha `dataNascimento` + `idade`. **Mudança client** (`DashAvisoPrevio.tsx`): (a) CdmSortKey ganha `'idade'`; (b) header "Anos" → "Tempo de empresa" (key `anosServico` preservada, tooltip novo); (c) nova `<th>` "Idade" (key `idade`, ordenável); (d) nova `<td>` mostra `l.idade` ou "—" itálico (sem inferir), title com data de nascimento pt-BR; (e) tfoot colSpan 10→11. version → 1931. **Resultado**: header claro + idade real ordenável. **Preservado**: filtros Rev. 1915/1923, queries Rev. 1916/1911/1927, projeção Rev. 1909-fix, vrDiario Rev. 1927, complementar Rev. 1919, diasAvisoEstimado Rev. 1930, sort Rev. 1909, tipo Rev. 1921. Zero ALTER/DROP/DELETE. Reversível em 5 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1930~~ (removida do top-5 pela Rev. 1934 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · FIX coluna "Dias Aviso" não respondia ao toggle Trabalhado/Indenizado**. User (16/05/2026, screenshot Trabalhado ativo com Anderson 60 dias / Ana 66): "os dias de aviso não deveria mudar se simularmos trabalhado ou indenizado?". **Causa**: retorno usava `previsao.diasAvisoTotal` que vem de `calcularRescisaoCompleta` (`rescisaoCalc.ts` L330) — SEMPRE calcula `calcularDiasAvisoTotal(anosServico)` = 30+3·ano Lei 12.506, ignora o `tipo`. Toggle trocava R$ (parâmetros internos OK Rev. 1921/1927) mas coluna persistia no full. **Mudança** (`dashboards.ts` L2544 getDashCustoDemissaoMassa): 1 linha — `diasAvisoTotal: previsao.diasAvisoTotal` → `diasAvisoTotal: diasAvisoEstimado` (variável local já correta na L2476: `tipo==='trabalhado' ? 30 : calcularDiasAvisoTotal(anosBase)`). version → 1930. **Resultado**: Trabalhado → 30 em TODAS as linhas (CLT Art. 487 II — fixo). Indenizado → 30/33/.../90 conforme Lei 12.506. Anderson 10 anos: Trab=30 / Ind=60. **Preservado**: lógica interna rescisaoCalc, projeção dtFimProjetada Rev. 1909-fix, vrDiario/diasTrabMes Rev. 1927, complementar Rev. 1919, filtros Rev. 1915/1923, sort Rev. 1909. Zero ALTER/DROP/DELETE. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- ~~Rev. 1929~~ (removida do top-5 pela Rev. 1933 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · Gantt/Cronograma · Coluna "Atividade / Item" redimensionável por drag (mouse + touch + pen)**. User (16/05/2026, screenshot mobile com nomes "Armadura em barra de aço CA-..." truncados): "quero poder clicar e arrastar o tamanho da coluna para ver todo o texto descrito, seja pelo mouse, celular ou tablet". **Mudança** (`PlanejamentoDetalhe.tsx`): (a) `const LEFT_W = 310` → `useState` inicializado de `localStorage` key `planej-leftw-${projetoId}` (por obra), MIN=140/MAX=720/DEFAULT=310; (b) handlers `onLeftW(Down|Move|Up|Cancel)` via **Pointer Events** unificados (cobre mouse+touch+pen), `setPointerCapture` no down + `body.style.cursor='col-resize'` durante drag + persist no Up; (c) novo handle 14×100% absolutamente posicionado em `right:-7` da corner cell (alvo táctil ≥ HIG 44pt / Material 48dp p/ gesto horizontal), `touchAction:'none'` impede pan do browser, visual barra 3×20 cinza→amber no hover, `onDoubleClick` reseta default, `role="separator" aria-orientation="vertical"`; (d) corner cell ganha `position:relative`. version → 1929. **Resultado**: arrasta o handle âmbar — desktop/tablet/celular — largura persiste por obra. Duplo-clique restaura. **Preservado**: LEFT_W em 2 locais (header+body) ambos reativos, dayPx/ROW_H/HEADER_H, sticky col+header, toggle Semana/Mês/Trimestre, N1/N2/N3, collapse. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1928~~ (removida do top-5 pela Rev. 1932 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · Botão "✕ Cancelar" no input cyan de grupos (inverso do "↓ Replicar" Rev. 1922)**. User (16/05/2026, screenshot grupo 03.03 com 7 atividades replicadas "teste"): "quero ter a opção tbm de cancelar todas de uma vez, caso eu queira cancelar". **Mudança** (`PlanejamentoDetalhe.tsx`): (a) nova função pura `clearRespFromDescendants(rows, realIdx)` espelha estrutura de `cascadeRespToDescendants` (Rev. 1922) — varre `realIdx+1` até `isGrupo`/fim, ignora `disabled`, em cada descendente seta `_respManual: false` + `responsavelLotus: ""` (não toca no grupo — permite re-replicar com mesmo nome); (b) novo helper público `cancelarRespAosDescendentes(idx)` idêntico em estrutura ao `replicarRespAosDescendentes` (resolve realIdx por id, aplica clearResp, toast contextual); (c) botão novo "✕ Cancelar" vermelho (`bg-red-100`) ao lado do "↓ Replicar" cyan, em `{a.isGrupo && (<>...</>)}`. version → 1928. **Resultado**: ciclo replicar↔cancelar em massa sem mexer linha-a-linha. **Preservado**: cascadeRespToDescendants Rev. 1922, replicarResp Rev. 1922, marcarComoGrupo Rev. 1920/1922, onBlur Rev. 1860/1865/1892/1902, modal cascadeResp folhas, save Rev. 1910. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1927~~ (removida do top-5 pela Rev. 1931 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · FIX paridade ABSOLUTA com módulo oficial (zero centavo de diferença)**. User (16/05/2026, Anderson R$ 72.191,50 ainda divergente): "OS VALORES AINDA NÃO ABATEM COM O MODULO AVISO PREVIO.. GARANTA QUE NÃO VAI TER NEM UM CENTAVO DE DIFERENÇA". **Causas remanescentes** (auditoria lado-a-lado vs `avisoPrevioFerias.ts comparativo`): (1) CDM passava `vrDiario:0` hardcoded; oficial puxa de `meal_benefit_configs` da obra do funcionário (impacto direto `vrProporcional = vrDiario × diasTrabMes` no total — rescisaoCalc L380). (2) CDM usava `diasTrabalhadosMes = dataRef.getDate()`; oficial usa `dataFimAviso.getDate() − diasFeriasAgendadasNoMes`. Para Anderson Indenizado (60d): dataFim=15/07, oficial passa 15 (Jul), CDM passava 16 (Mai). **Mudança** (`dashboards.ts` getDashCustoDemissaoMassa): SELECT ganha `companyId`; batch `obra_funcionarios` augmentado p/ capturar `obraId` (zero query extra); 2 novos batch SELECTs — meal_benefit_configs (popula `vrDiarioByObra` + `vrDiarioDefaultByCompany`) e vacation_periods status agendada/em_gozo/concluída na janela [dataRef..+90d]; novo helper `diasFeriasNoMesDaSaidaCdm` espelha avisoPrevioFerias.ts L169-207; per-emp `diasTrabMesReal` + `vrDiarioReal` (fallback obra→empresa→0) passados ao `calcularRescisaoCompleta` E `calcularRescisaoComplementar`. version → 1927. **Resultado**: total CDM idêntico ao oficial (mesmos params = função pura = mesma saída). **Preservado**: tipo seletor Rev. 1921, filtros PJ/Sócio Rev. 1915 + Recluso/Afastado Rev. 1923, vacation_periods VENCIDAS Rev. 1911 (query separada — semântica distinta), complementar Rev. 1919, sort/top-3 Rev. 1909. Zero ALTER/DROP/DELETE — 2 SELECTs novos. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1926~~ (removida do top-5 pela Rev. 1930 — ver `shared/changelog.ts`). **Planejamento · ProgramacaoSemanalLotus · FIX export Excel pintando Sáb/Dom mesmo sem atividade no ERP**. User (16/05/2026, screenshots Sem.08 19-25/jun ERP cinza vs Excel azul): "GARANTE QUE SERÁ RESPEITADO DOS DADOS QUE ESTIVER NO ERP, MANTENDO A FORMATAÇÃO DO CLIENTE". **Causa**: template do cliente tem cols fixas Seg|Ter|Qua|Qui|Sex|Sáb|Dom (J→P=10→16); código pintava sequencial (`cIdx=10+di`). Em projeto com cutoff quinta (semanas Sex→Qui), di=5 (Qua 24/jun) ia pra col O rotulada "Sábado" e sobrescrevia o cinza_fds com azul previsto. **Mudança** (`ProgramacaoSemanalLotus.tsx`): (a) novo helper `dowToExcelCol(dow)` (Sun→16, Mon→10..Sat→15); (b) preenchimento de datas L9 e (c) loop de pintura passam a usar `cIdx = dowToExcelCol(d.getDay())` — cada data vai pra col que casa com seu dia-da-semana real, independente da ordem em `dias`. version → 1926. **Resultado**: Sáb/Dom ficam cinza vazio quando o sáb/dom real não tem trabalho; semanas Sex→Qui mostram Seg-Qui (próxima semana) + Sex-Sáb-Dom corretamente alinhados. **Preservado**: cinza_fds Rev. 1893, reset BRANCO Rev. 1904, ehUtilCal Rev. 1914, diasExtras Rev. 1875, cutoff Rev. 1913, faixa azul topo+status baixo Rev. 1895, template xlsx do cliente intacto. Zero backend/DB. Reversível em 3 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1925~~ (removida do top-5 pela Rev. 1929 — ver `shared/changelog.ts`). **RH · Aviso Prévio · Base Legal expandível no formulário (CLT Art. 487-491 + Lei 12.506/2011)**. User (16/05/2026, após explicação Trabalhado=30 fixos vs Indenizado=30+3/ano até 90): "QUAL LEI FALA SOBRE ISSO, QUERO QUE DEIXE REGISTRADO NO MODULO". **Mudança** (`AvisoPrevio.tsx` entre header e body do diálogo Novo Aviso Prévio): novo painel `<details>` fundo azul (acordeon HTML nativo, sem dep externa), fechado por default. Conteúdo: 2 cards lado-a-lado (Trabalhado azul/Indenizado vermelho) com base legal de cada um (CLT Art. 487 II / Lei 12.506/2011 + CLT Art. 487 §1º) e lógica jurídica; tabela Dias de Aviso (6 linhas: ≤1 ano, 2, 5, 10 [destacado], 15, 20+ [teto 90]); resumo âmbar; links Planalto p/ fontes oficiais. version → 1925. **Resultado**: documentação legal vive dentro do módulo, auditável, escala bem (acordeon fechado = 1 linha). **Preservado**: header amber Rev. 1727, lógica cálculo, templates Rev. 1907, combobox Rev. 1906. Zero backend/DB. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- ~~Rev. 1924~~ (removida do top-5 pela Rev. 1928 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · FIX cabeçalho sticky transparente sobrepondo linhas**. User (16/05/2026, screenshot rows WILLIANS FELIPE/MARCO ANTONIO com cabeçalho embaralhado por cima): "ESTA COM ERRO DE FORMATAÇÃO DE LAYOUT.. O TEXTO ESTA SOBREPOSTO AS INFORMAÇÕES NÃO DEVE ACONTECER". **Causa**: thead sticky com `bg-muted/50` (50% opacidade) + bg aplicado em `<thead>`/`<tr>` (não funciona com sticky em Chromium — só bg em células individuais "segura" conteúdo no scroll). Tfoot sticky com mesmo bug latente. **Mudança** (`DashAvisoPrevio.tsx`): (a) table ganha `border-separate border-spacing-0`; (b) cada `<th>` recebe `bg-slate-100 border-b border-slate-300 shadow-sm` (sólido + sombra de separação); (c) `<tfoot>` bg movido p/ cada `<td>` com `bg-red-50` + shadow superior; (d) `<tbody>` rows ganham `bg-white` default (mantendo `bg-red-50/40` p/ top-3) + borders movidas de `<tr>` p/ cada `<td>`. version → 1924. **Resultado**: cabeçalho/rodapé sólidos com sombra ao rolar, sem bleed-through. **Preservado**: sort Rev. 1909, top-3 Rev. 1908, complementar Rev. 1919, ícones, colSpan, formatação, classes. Zero backend/DB. Reversível em 3 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1923~~ (removida do top-5 pela Rev. 1927 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · Excluir RECLUSOS e AFASTADOS >15 dias da provisão de caixa**. User (16/05/2026, screenshot CDM 103 ativos R$ 1.186.020,82): "TIRE DA LISTA OS AFASTADOS ACIMA DE 15 DIAS E OS RECLUSOS". **Justificativa legal**: Recluso = contrato SUSPENSO (auxílio-reclusão INSS). Afastado >15d = INSS toma conta (auxílio-doença B91/B31, CLT Art. 60 §3º). Em ambos: demitir agora é juridicamente inviável (estabilidade) e não consome caixa do empregador no curto prazo — inflavam a provisão "quanto custa demitir TODO o quadro hoje". Critério idêntico ao homeData Rev. 14716 (Central de Alertas). **Mudança server** (`dashboards.ts` `getDashCustoDemissaoMassa` activeWhere): 2 cláusulas AND novas — (a) `status <> 'Recluso'`; (b) `NOT (status='Afastado' AND (licencaDataInicio IS NULL OR licencaDataInicio <= dataRef-15d))`. Atestados curtos ≤15d não mudam status (só vão p/ espelho de ponto), então não há falso-positivo. version → 1923. **Resultado**: KPI Ativos / Custo Total / Folha caem pelos reclusos+afastados longos. **Preservado**: filtros PJ/Socio Rev. 1915, obra Rev. 1916, vacation_periods Rev. 1911, complementar Rev. 1919, seletor TIPO Rev. 1921, sort Rev. 1909, top-3, date picker. Zero ALTER/DROP/DELETE. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- ~~Rev. 1922~~ (removida do top-5 pela Rev. 1926 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · FIX/UX cascata grupo→descendentes — botão explícito "↓ Replicar" no input cyan + refactor**. User (16/05/2026, screenshots Mosaico isGrupo=true + 4 Ajudantes mesmo EAP 03.05 com checkboxes vazios, APÓS Rev. 1920): "NÃO ESTA ACONTECENDO NADA. CLICO NA ATIVIDADE PRINCIPAL DO GRUPO, MAS OS FILHOS NÃO ESTÃO SENDO AFETADAS.. ARRUME A LOGICA OU CRIE OUTRA FORMA DE RESOLVER ESTE PROBLEMA DE VEZ". **Causa-raiz Rev. 1920**: `marcarComoGrupo` SÓ rodava na transição false→true do checkbox; Mosaico já vinha do banco com isGrupo=true (caso comum pós-import MSP), então clicar no checkbox UNCHECK (sem cascata) era o efeito. **Mudança** (`PlanejamentoDetalhe.tsx`): (a) extraída função pura `cascadeRespToDescendants(rows, realIdx)`; (b) helper público `replicarRespAosDescendentes(idx)` com lookup por id + toast contextual; (c) `marcarComoGrupo` simplificada (delega à utility); (d) NOVO botão cyan "↓ Replicar" ao lado do Input cyan em linhas `isGrupo=true` (com tooltip explicativo). version → 1922. **Resultado**: user clica o botão UMA vez no grupo — toast confirma "X descendentes replicados". Funciona INDEPENDENTE de quando isGrupo foi marcado. Caminho original (checkbox+onBlur+save) preservado. **Preservado**: onBlur Rev. 1860/1865/1892/1902, `aplicarCascataResponsavelGrupos` Rev. 1910, modal cascadeResp folhas, 6 checkboxes. Zero backend/DB. Reversível em 4 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1921~~ (removida do top-5 pela Rev. 1925 — ver `shared/changelog.ts`). **RH · Dash Aviso Prévio · Tabela CDM · FIX paridade definitiva com módulo oficial — seletor TIPO (Trabalhado/Indenizado, default Trabalhado) + datas espelhadas**. User (16/05/2026, screenshots Anderson CDM 87.271 vs oficial 71.281): "SIMULEI PARA O DIA 16/05/2026 E MESMO ASSIM OS VALORES NÃO ESTÃO CONVERGINDO.. ARRUME ISSO DE VEZ". **Causa-raiz**: CDM rodava sempre `tipo='empregador_indenizado'` (60 dias aviso p/ Anderson) enquanto user simulava oficial com `tipo='empregador_trabalhado'` (30 dias). Diferentes tipos = diferentes dataFimAviso = diferentes meses_ferias/13o/FGTS = ~16k divergência. Adicional: fórmula de datas conceitualmente errada (não espelhava `calcularDataInicioAviso + diasAviso - 1` do módulo oficial). **Mudança server** (`dashboards.ts`): novo param `tipoParam` default `'empregador_trabalhado'`; `diasAvisoEstimado = tipo==='trabalhado' ? 30 : calcularDiasAvisoTotal(anos)`; `tipo` passado a `calcularRescisaoCompleta` E `calcularRescisaoComplementar` (respeitam internamente); response retorna `tipo`. Endpoint zod ganha enum. **Mudança client** (`DashAvisoPrevio.tsx`): state `cdmTipo` default trabalhado; toggle segmentado azul/vermelho no header (Trabalhado=paridade oficial / Indenizado=pior cenário); descrição reescrita. version → 1921. **Resultado**: Anderson default ~R$ 71.281,82 (paridade exata com oficial). User alterna p/ INDENIZADO p/ ver pior cenário. **Preservado**: filtro PJ/Socio Rev. 1915, obra Rev. 1916, vacation_periods Rev. 1911, complementar Rev. 1919, sort Rev. 1909, top-3, KPIs, date picker. Zero ALTER/DROP/DELETE. Reversível em 6 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1920~~ (removida do top-5 pela Rev. 1924 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · FIX `marcarComoGrupo` não cascateava em MSP denormalizado — semântica direta "todas abaixo até próximo grupo"**. User (16/05/2026, screenshot Mosaico marcada como grupo EAP 03.05 + 4 filhas "Ajudante de pedreiro" mesmo EAP 03.05, todas vazias): "NÃO ESTA FUNCIONANDO, CLIQUEI NA ATIVIDADE DE GRUPO E OS FILHOS NÃO FORAM CLICADOS AUTOMATICAMENTE". **Causa-raiz**: lógica Rev. 1918 tentava primeiro detecção estrita por prefixo EAP — em MSP denormalizado (pai e filhos mesmo EAP) o loop dava `continue` em todas as linhas, deixando `descIdxs=[]` de forma frágil. **Mudança**: rewrite da `marcarComoGrupo` com semântica DIRETA — a partir de `realIdx+1`, percorre até encontrar próximo `isGrupo` (break) ou fim; ignora `disabled` (continue); todo o resto é descendente. Removida toda detecção por prefixo EAP (paranoia desnecessária dado que user acabou de marcar a linha como grupo). Salvaguarda extra: lookup por `id` pra confirmar `realIdx` caso futura mudança no `displayAtiv` quebre paridade de índice. version → 1920. **Resultado**: Mosaico marcada → 4 filhas imediatamente recebem `_respManual=true`, checkbox cyan acende, input "Responsável manual" aparece. Cobre 100% dos casos (EAP hierárquico, flat, MSP denormalizado, sem EAP). **Preservado**: onBlur cascata Rev. 1860/1865/1892/1902, `aplicarCascataResponsavelGrupos` Rev. 1910 no Salvar, 4 demais checkboxes. Zero backend/DB. Reversível em 1 hunk. R-001/R-007/R-010 OK.
- **Rev. 1919**: **RH · Dash Aviso Prévio · Tabela CDM · FIX divergência com módulo oficial — soma RESCISÃO COMPLEMENTAR ao total (paridade TOTAL GERAL = Oficial + Complementar)**. User (16/05/2026, screenshots Anderson CDM R$ 53.256,22 vs oficial R$ 71.281,82 = 45.428 + 25.853): "ANDERSON ESTA DIVERGENTE.. SEGUINDO O MESMO CALCULO ADOTADO NO MODULO DE AVISO PREVIO.. NÃO PODE DAR DIFERENÇA". **Causa-raiz**: `getDashCustoDemissaoMassa` só chamava `calcularRescisaoCompleta` (oficial), ignorando `calcularRescisaoComplementar` (sobre `employees.valorComplemento` quando `recebeComplemento=true`) — o módulo oficial L1738 soma os dois no TOTAL GERAL. **Mudança server** (`dashboards.ts`): import + SELECT `recebeComplemento`/`valorComplemento`; novo bloco chama `calcularRescisaoComplementar` com mesmos params temporais (dataFimAviso, periodosVencidosOverride, diasTrabalhadosMes) garantindo consistência. Retorno ganha `totalOficial` + `totalComplementar`; `total` = soma dos dois. `avisoPrevioIndenizado` exibido = oficial+compl. **Mudança client** (`DashAvisoPrevio.tsx`): célula Custo Total ganha sub-linha violet `+compl R$ X` (tooltip "Oficial X + Complementar Y") quando `totalComplementar>0`; disclaimer atualizado citando paridade com módulo Aviso Prévio. version → 1919. **Resultado**: Anderson 53k → 71,3k (=detalhe oficial). Funcionários sem complemento: zero impacto. **Preservado**: filtro PJ/Socio Rev. 1915, query obra Rev. 1916, vacation_periods Rev. 1911, projeção Rev. 1909-fix, sort/ordenação Rev. 1909, top-3, KPIs. Zero ALTER/DROP/DELETE. Reversível em 5 hunks. R-001/R-007/R-010 OK.
- ~~Rev. 1918~~ (removida do top-5 pela Rev. 1923 — ver `shared/changelog.ts`). **Planejamento · PlanejamentoDetalhe · Cascata automática ao MARCAR isGrupo** (ativa `_respManual` + replica `responsavelLotus` nos descendentes). User (16/05/2026, screenshot grupo 03.03 marcado, filhas sem responsável): "QUANDO MARCAR A TAREFA QUE É A TAREFA DE GRUPO, TODAS ATIVIDADES ABAIXO DELA DEVE SER ATIVADAS AUTOMATICAMENTE E SEGUIR O MESMO NOME QUE ESTIVER NA ATIVIDADE GRUPO". Antes só cascateava no onBlur do input de responsável — se marcasse grupo DEPOIS de digitar (ou marcasse grupo vazio esperando digitar depois), filhas ficavam sem `_respManual=true` e sem nome. **Mudança** (`PlanejamentoDetalhe.tsx`): nova `marcarComoGrupo(idx, checked)` espelha detecção de descendentes do onBlur (prefix dotted/flat + guard nivel + fallback MSP denormalizado) → setar `_respManual: true` em todos + copiar `responsavelLotus` se preenchido (semântica Rev. 1892). Desmarcar grupo não desfaz cascata. Toast diferenciado. onChange do checkbox isGrupo (L4263) → `marcarComoGrupo`. version → 1918. **Resultado**: fluxo funciona em qualquer ordem (digitar→marcar OU marcar→digitar). **Preservado**: onBlur cascata Rev. 1860/1865/1892/1902, modal `cascadeResp` folhas não-grupo, `aplicarCascataResponsavelGrupos` Rev. 1910 no Salvar, 4 demais checkboxes (isMarco/isIndireta/isExterna/disabled/_respManual). Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026): "Quero o nome, função e qual obra ele está a trabalhando". **Server** (`dashboards.ts` getDashCustoDemissaoMassa): nova query batch `DISTINCT ON ("employeeId")` JOIN `obras` filtrada `isActive=1` order `dataInicio DESC` — Map `obraByEmp` populado em O(N) (sem N+1); linha ganha `funcao` + `obra`. **Client** (`DashAvisoPrevio.tsx`): `CdmSortKey` ganha `'funcao' | 'obra'`; header reordenado # | Funcionário | Função | Obra | Setor | Admissão | Anos | Dias Aviso | Salário | Aviso Indeniz. | Multa 40% | Custo Total (11 cols); body célula Função usa fallback `funcao || cargo || '-'`; célula Obra mostra `<italic>sem alocação</italic>` quando vazio; `colSpan={10}` → `colSpan={11}` no tfoot. version → 1916. **Resultado**: diretoria vê em qual obra cada custo seria aplicado — permite mental filter "se fechar obra X". **Preservado**: filtro PJ/Socio Rev. 1915, query vacation_periods Rev. 1911, projeção dataFimAviso Rev. 1909-fix, sort default, ordenação clicável Rev. 1909, destaque top-3, KPIs, disclaimer. Zero ALTER/DROP/DELETE — 1 SELECT adicional. Reversível em 5 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot tabela CDM): "Quem é PJ é sócio não entra nesta lista". PJ e Sócio não são CLT, não geram rescisão (aviso/férias/13º/multa FGTS) — inflavam a provisão de caixa com valores contabilmente errados. **Mudança** (`dashboards.ts` L2235-2244, `getDashCustoDemissaoMassa` activeWhere): adicionado `(tipoContrato IS NULL OR tipoContrato NOT IN ('PJ','Socio'))` ao AND. Critério idêntico ao L94 (KPIs RH) e `avisoPrevioFerias.ts` L2291/2463/2486/2558 — paridade total com módulo oficial. `tipoContrato IS NULL` continua tratado como CLT (default histórico). version → 1915. **Resultado**: tabela CDM mostra só CLT ativo; KPIs Funcionários/Folha caem proporcionalmente; disclaimer "+N ignorados" agora engloba também PJ/Sócios. **Preservado**: query batch vacation_periods Rev. 1911, projeção dataFimAviso Rev. 1909-fix, sort desc, ordenação clicável Rev. 1909, demais 10 seções. Zero ALTER/DROP/DELETE. Reversível em 1 hunk de 3 linhas. R-001/R-007/R-010 OK.
: respeitar 100% o calendarioJson do MSP (sáb/dom só não pintam se o MSP disser que são folga)**. User (16/05/2026, 3ª revisão da regra de FDS no mesmo dia): "Respeite o calendário que veio do project para não ter erros". Fonte de verdade = calendarioJson importado do MSP. Cada obra tem regras próprias (algumas trabalham sáb manhã). **Mudança** (`ProgramacaoSemanalLotus.tsx`): (a) L188-200 — revertido bloco `ehFds?false:...` p/ linha original `ehUtilCal = cal ? ehDiaUtil(ds, cal) : (getDay() !== 0 && !== 6)`; (b) L1894-1901 (rendering) — revertido `calMarcaUtil` p/ versão original `calMSP ? ehDiaUtil(dsIso, calMSP) : !ehFds`. Cutoff Rev. 1913 (só zera bottom) PRESERVADO. `diasExtras` Rev. 1875 PRESERVADO (override manual). version → 1914. **Resultado**: ERP segue 100% o MSP — se cronograma marca sáb útil, pinta; se folga, fica cinza. **Preservado**: pintaCinzaFds Rev. 1893 (marca visual cols 15-16 Excel, independente do calendário), cutoff Rev. 1913, reset BRANCO Rev. 1904, migração Rev. 1886, diasExtras Rev. 1875, auto-derivação Rev. 1677/1688/1785. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot Sem.18 [28/08-03/09/2026] vazia + "Pq vc retirou o previsto das demais semanas, ela deve aparecer até ao final do cronograma, o cutoff deve ser respeitado para definir a linha de corte da semana mas o resto deve ser mantido"): user mudou de ideia sobre o modelo HÍBRIDO escolhido na Rev. 1912 minutos antes. Pediu retorno ao modelo da Rev. 1905 (PLANO inteiro até fim do projeto + SNAPSHOT realizado até cutoff). **Mudança** (`ProgramacaoSemanalLotus.tsx` L267-278): `if (passouCutoff) { top=null; bottom=null }` → `if (passouCutoff) bottom = null` (1 hunk). Rev. 1912 parte A (FDS — ehUtilCal/calMarcaUtil) PRESERVADA (sáb/dom segue sempre cinza vazio ignorando calMSP, ortogonal ao cutoff). version → 1913. **Resultado**: Sem.18 volta a mostrar azul previsto em seg-sex onde há envelope; sáb/dom continuam cinza vazio; semanas futuras pós-cutoff mostram só plano sem realizado. **Preservado**: pintaCinzaFds Rev. 1893, reset BRANCO Rev. 1904, migração Rev. 1886, ehUtilCal+calMarcaUtil Rev. 1912-A, regra vermelho-passou-sem-real, auto-derivação Rev. 1677/1688/1785. Zero backend/DB/schema. Reversível em 1 hunk. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshots ENIVALDO na tabela CDM R$ 88.992,81 vs detalhe oficial Aviso Prévio R$ 32.573,67): "o valor total é 32k não 88k como mostra na tabela, verifica isso, não quero dados errados". **Causa-raiz** (em `dashboards.ts` L2270 — `getDashCustoDemissaoMassa` Rev. 1908): chamava `calcularRescisaoCompleta({tipo:'empregador_indenizado'})` SEM passar `periodosVencidosOverride`, violando comentário crítico em `rescisaoCalc.ts` L292-296: "Sempre passar `periodosVencidosOverride` com contagem real do banco (vacation_periods). Nunca usar o cálculo matemático puro". Sem override, função cai no fallback `calcularFeriasVencidas() - 1` — para Enivaldo (13 anos), retorna ~12 períodos VENCIDOS fictícios, inflando férias em ~R$ 55.722 (= diferença exata 88k-32k). **Mudança** (L2255-2280 + L2295): novo bloco com query batch SINGLE (não N+1) `SELECT "employeeId", COUNT(*) FROM vacation_periods WHERE "employeeId" IN (...) AND status NOT IN ('concluida','cancelada','em_gozo') AND "periodoAquisitivoFim" < dataRef AND "deletedAt" IS NULL GROUP BY "employeeId"`, populando `Map<id,count>`; dentro do `.map`, novo `periodosVencidosReal = vpCountByEmp.get(r.id) ?? 0` passado a `calcularRescisaoCompleta`. Critério idêntico ao `avisoPrevioFerias.ts` L494-503 (fonte de verdade). version → 1911. **Resultado**: Enivaldo 88.992,81 → ~32.573,67 (paridade com detalhe oficial). Funcionários com férias REAIS vencidas usam contagem real do banco. **Preservado**: estrutura função CDM Rev. 1908, projeção dataFimAviso Rev. 1909-fix, UI Rev. 1909, demais 10 seções dashboard. Zero ALTER/DROP/DELETE — apenas 1 SELECT adicionado. Reversível em 2 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026): "FIZ A DEMARCAÇÃO MAS AS ATIVIDADES ABAIXO DO GRUPO NÃO ESTÃO RECEBENDO O NOME QUE SERÁ DO RESPONSAVEL QUE FOI INDICADO DO GRUPO, E NÃO FICA NENHUM CARD VISIVEL QUANDO SALVAMOS O CRONOGRAMA..". **Causa-raiz** (em `PlanejamentoDetalhe.tsx` L3815 + L4358): cascata grupo→descendentes (Revs. 1860/1865/1892/1902) só rodava no `onBlur` do input — `setLinhas` é assíncrono; ao clicar "Salvar" logo após digitar, React processa blur (enqueue setLinhas) + click (lê `linhas` closure antigo) no MESMO tick → mutate envia descendentes SEM responsavelLotus → invalidate refaz GET → badges somem. **Mudança** (L3607-3662 + L3815-3826): nova função `aplicarCascataResponsavelGrupos(rows)` espelha a lógica do onBlur mas roda sincronamente sobre cópia do array (sem tocar state) — detecção estrita prefixo EAP dotted/flat + guard nivel (Rev. 1865) + fallback MSP denormalizado mesmo-EAP (Rev. 1902) + semântica grupo sobrescreve cascata total (Rev. 1892); itera TODOS os grupos (idempotente em planilhas multi-grupo). onClick do botão "Salvar" reescrito: aplica cascata → `setLinhas(atividades)` sincroniza UI ANTES do refetch (badges aparecem na hora, sem "sumiço") → `salvarMutation.mutate({ atividades })` envia versão correta por referência. version → 1910. **Resultado**: marcar grupo + digitar FELIPE + Salvar = backend persiste em TODOS os descendentes + badge cyan aparece imediatamente, sem reload. Funciona p/ EAP dotted hierárquico (`03.05.05` → `03.05.05.01`) e MSP denormalizado (todos `03.05`). **Preservado**: onBlur cascada em tempo real intacta; modal `cascadeResp` p/ folhas com sub-itens intacto; `salvarAtividades` server já aceitava `responsavelLotus`. Zero backend/DB. Reversível em 2 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026): "seção nova no dash com date picker + tabela ordenada do mais caro ao mais barato. QUETO TER POSSIBILIDADE DE MUDAR ESSA ORDEM SE EU QUISER..". **Mudança** (em `DashAvisoPrevio.tsx` L40-41, L126-150, L453-498): novos ícones ArrowUp/Down/UpDown; state `cdmSort: { key, dir }` default `{ key:'total', dir:'desc' }` (mantém Rev. 1908); função `toggleCdmSort(key)` — mesma col inverte direção, col nova default 'asc' p/ texto/data, 'desc' p/ números; `useMemo cdmLinhasOrdenadas` clona+ordena no client (Number subtract / localeCompare pt-BR — datas ISO comparam string OK). 10 `<th>` convertidos a `cursor-pointer + hover:text-blue-700` com `<SortIcon k=>` ao lado (cinza inativo, azul ativo). `tbody` itera `cdmLinhasOrdenadas`; destaque top-3 bg-red-50/40 só renderiza quando ordenação == default ('total'/'desc'). version → 1909. **Resultado**: click em qualquer coluna reordena; click no mesmo header alterna asc↔desc. **Preservado**: endpoint server, KPIs, date picker, tfoot TOTAL GERAL, disclaimer. Zero backend/DB. Reversível em 3 hunks. R-001/R-007/R-010 OK.
 User (16/05/2026): "NO DASH DE AVISO PREVIO, QUERO QUE CRIE UMA TABELA INFORMANDO DO MAIS CARO AO MAIS BARATO DE TODOS OS FUNCIONARIOS DA EMPRESA, QUAL SERIA O CUSTO PARA DAR O AVISO PREVIO APARTIR DA DATA DE HOJE, OU DE UMA DATA A SER DEFINIDA PELO USUÁRIO.. ISSO É IMPORTANTE SABER PARA TER UMA NOSSÃO DE QUANTO CUSTA PARA DEMITIR TODOS FUNCIONARIOS CASO SEJA NESCESSÁRIO É UM VALOR PRECISA ESTAR DISPONIVEL NO CAIXA..". **Mudança server** (`dashboards.ts`): novo import `calcularRescisaoCompleta` da utils existente; nova função `getDashCustoDemissaoMassa(companyId, dataReferencia?, companyIds?)` — lista funcionários ativos (status NOT IN Desligado/Lista_Negra, deletedAt IS NULL), para cada chama `calcularRescisaoCompleta` com `tipo='empregador_indenizado'` (pior cenário de caixa: paga aviso indenizado + multa 40% FGTS), ordena `b.total - a.total`, retorna grandTotal/totalFunc/media/folha; novo endpoint tRPC `dashboards.custoDemissaoMassa` com input `{ companyId, companyIds?, dataReferencia? (YYYY-MM-DD, default hoje) }`. **Mudança client** (`DashAvisoPrevio.tsx`): state `cdmData` + useQuery; nova SEÇÃO 2.B entre Previsão de Custo e Alertas — Card vermelho com ícone Flame, date picker `<Input type="date">` + botão "Hoje", 4 KPIs (Custo Total / Ativos / Média / Folha+Nx), tabela sticky 11 colunas com top-3 mais caros destacados em bg-red-50/40 + tfoot com TOTAL GERAL, disclaimer no rodapé sobre VR/descontos. version → 1908. **Resultado**: diretoria vê instantaneamente quanto sairia encerrar todo o quadro hoje (ou em qualquer data futura) + ranking individual. **Preservado**: as 10 seções originais do dash intactas; endpoints `avisoPrevio` e `avisoPrevioComparativo` inalterados; `calcularRescisaoCompleta` reusado sem modificação. Zero ALTER/DROP/DELETE. Reversível em 4 hunks + version bump. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot preview "Aviso Prévio do Empregador" com header azul gradiente + logo FC + faixa amarela): "GARANTA QUE A TELA AZUL COM O LOGO DA EMPRESA, SAIA NA IMPRESSÃO.. QUERO A TELA EXATAMENTE IGUAL..". **Causa-raiz** (em `AvisoPrevio.tsx` L583/L673 — `@media print` dos templates Trabalhado/Indenizado gerados via `window.open + document.write`): a regra `@media print` não tinha `print-color-adjust` — Chrome/Edge/Firefox/Safari DESLIGAM `background-color`/`gradient` por default na impressão (economia de tinta). Resultado: header azul gradient + faixa amarela inferior saíam 100% brancos no PDF/papel. **Mudança** (L585-595 + L675-684): `@media print` expandida com `body { -webkit-print-color-adjust: exact !important; print-color-adjust: exact !important; color-adjust: exact !important }` + mesma tripla em `.doc-header, .doc-header *`. As 3 propriedades cobrem todos os navegadores (-webkit- p/ Chromium/Safari, sem prefixo p/ Firefox, `color-adjust` legacy). Aplicado em ambos templates (Trabalhado + Indenizado). version → 1907. **Resultado**: PDF/impressão idêntica ao preview — header azul + logo branco + faixa amarela + corpo Times New Roman preto sobre branco. **Preservado**: HTML structure, escapeHtml Rev. 1727, lógica de geração de doc, opções 2h/7d (CLT Art. 488 Rev. 1727/1794), assinaturas, dimensões A4. Zero backend/DB/schema/tRPC. Reversível em 2 hunks + version bump. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot combobox "Colaborador" com search "ANA" listando ANA + ANDERSON + ANDRE + ALEXANDRO — todos com A-N-A em ordem fuzzy): "ARRUME TBM ESTE FILTRO DE PESQUISA QUANDO DIGITAR O NOME DO COLABORADOR ELE VAI FILTRANDO SOMENTE O TEXTO QUE FOR PERTINENTE.. HOJE FICA APARECENDO TUDO SEM NESCESSIDADE..". **Causa-raiz** (em `AvisoPrevio.tsx` L2243 — `<Command>` do shadcn/cmdk): sem `filter` prop, `cmdk` usa "command score" fuzzy que casa qualquer char em ordem (não-contígua). Ex: "ANA" casa com "A**N**DERSO**N** D**A**". **Mudança** (L2245-2262): novo `filter={(value, search) => norm(value).includes(norm(search)) ? 1 : 0}` onde `norm = lowercase + NFD + strip diacríticos`. Match strict por substring contígua + estável p/ acentos ("joão" = "JOAO"). version → 1906. **Resultado**: "ANA" mostra apenas itens com "ANA" como substring real (ANA BEATRIZ, JOSIANE LOPES DA SILVANA, GIOVANA), não mais o universo A-N-A fuzzy. **Preservado**: `value` multi-campo (nome+cpf+função+setor) intacto; layout dos itens (avatar/nome/função/CPF/badges Rev. 1727/1806) inalterado; lógica de seleção e card de admissão Rev. 1794 intactos. Zero backend/DB/schema/tRPC. Reversível em 1 hunk + version bump. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot UI Sem.3 [15-21/05] com header "Oficial 14/05" + grid 100% branco apesar das atividades terem previsto 04/05-22/05): "O PREVISTO DEVE APARECER TODAS AS SEMANAS DO CRONOGRAMA, ATÉ O ULTIMO DIA DO PROJETO". **Causa-raiz** (em `ProgramacaoSemanalLotus.tsx` L173-175 — `faixasCelula`): Rev. 1894 introduziu cutoff oficial (Padrão LOTUS/status-date PMBOK) mas o guard era agressivo demais — `if (ds > cutoff) return {null,null}` zerava AMBAS as faixas. Resultado: cutoff em 14/05 → Semana 3+ (15/05+) totalmente branca, sem nenhuma indicação de plano. Semântica errada: cutoff é "fotografia do REALIZADO até a data", não tem relação com o PLANEJAMENTO (que é fixo). **Mudança** (L176-190 + L266-270): removido early-return; flag `passouCutoff = ds > cutoff` calculada no início mas SEM bloquear. Lógica inteira de inPrev/inReal/passou/aderência roda normalmente — top recebe `bg-blue-800` para dia coberto pelo envelope mesmo se ds > cutoff. Post-process antes do return: `if (passouCutoff) bottom = null` — zera APENAS o realizado. version → 1905. **Resultado**: Semana 3+ mostra azul de previsto onde há envelope; plano LOTUS visível do início ao fim do projeto; cutoff continua restringindo realizado (PMBOK status-date OK). Sem cutoffIso, comportamento idêntico ao anterior. **Preservado**: Rev. 1894 cutoff semântica preservada p/ realizado; Rev. 1886 migração red→bottom intacta (antes do post-process); Rev. 1875/1893/1851 intactas; Rev. 1897/1904 reset BRANCO Excel intacto. Zero backend/DB/schema/cores. Reversível em 2 hunks + version bump. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot Excel cols J-O Sem.01 [01/05-07/05] pré-clipada a Mon-Thu): "O ERP AINDA NÃO TA RESPEITANDO A COR NA SEGUNDA CELULA EM AZUL DE PREVSITO.. E TBM NÃO ESTA RESPEITANDO A LINHA DE CORTE (CUTOFF).. RESOLVA ISSO EM DEFINITIVO..". **Causa-raiz** (em `ProgramacaoSemanalLotus.tsx` L1339-1456): convenção LOTUS Rev. 1897 = 4 céls/dia (r0=branco / r0+1=azul previsto / r0+2=status / r0+3=branco). Init L1206 usa `pattern:"none"` que NÃO REMOVE `fgColor` herdado do template em LibreOffice/Excel Online. Rev. 1897 cobriu APENAS r0/r0+3 com solid BRANCO — DEIXOU r0+1/r0+2 expostos ao vazamento. Nesta semana, `dias` clipada a 4 entradas (Mon-Thu, projetoStart=04/05); o `dias.forEach` pinta só cIdx 10-13. cIdx=14 (N=Sex sem data) ficou com cor residual em r0+1/r0+2 → (1) azul-fantasma em 2ª célula sem previsto real + (2) "paint vazando" além do cutoff visível. **Mudança** (L1404-1432, ANTES do dias.forEach): novo loop de RESET DEFINITIVO força `pattern:"solid"` + `fgColor:FFFFFFFF` em **TODAS as 4 linhas × cIdx 10-14** (Seg-Sex). O `dias.forEach` (intacto) só sobrescreve r0+1/r0+2 onde há corTop/corBot. REMOVIDO bloco defensivo Rev. 1897 pós-loop (redundante). Cols 15-16 (Sáb/Dom) preservadas pelo `pintaCinzaFds` Rev. 1893. version → 1904. **Resultado**: dias sem previsto = 100% brancos. Dias com previsto = azul (r0+1) + status (r0+2). Dias além do cutoff visível = 100% brancos. Convenção LOTUS 100% cross-viewer. **Preservado**: `faixasCelula` Rev. 1894 cutoff guard, migração Rev. 1886 red→bottom, cores Rev. 1895, init L1206, pintaCinzaFds Rev. 1893, diasExtras Rev. 1875/1893, reorder Rev. 1852. Zero backend/DB/schema. Reversível em 2 hunks + version bump. R-001/R-007/R-010 OK.
 User (16/05/2026, screenshot pág. 1 perfeita + pág. 2 com só "insumos conforme a NR-18" vazando no rodapé): "AJSUTE APRA CABER A INFORMAÇÃO DENTRO DA AREA DE IMPRESSÃO.. SEMPRE CENTRALIZADA, AJUSTADADA PARA EM TODOS OS PONTOS.. ARRUME ISSO DE FORMA DEFINITIVA.. É SUA REGRA DE OUTRO.. OK..". **Causa-raiz**: o isolamento Rev. 1899 era CSS-only com `visibility:hidden` no `body *` — `visibility:hidden` esconde mas MANTÉM as dimensões do layout. Resultado: modal fixed inset-0 + cards visíveis + sidebar + dashboard de fundo continuavam reservando altura no document body → `@page` A4 quebrava em pág. 2+ com texto residual aparecendo no rodapé. Centralização também falhava por causa do `position:absolute` (fora do flow). **Mudança** (em `PlanejamentoDetalhe.tsx` L2129+ rotina `dispararImpressao`): (A) CSS print reescrita: removidas as regras `visibility:hidden/visible`; `#atrasos-print-area` agora `position:static` + `display:block` + `margin:0 auto` (centraliza) + `width/max-width:100%` + `box-sizing:border-box`; `html, body { margin:0; padding:0; height:auto; overflow:visible }`. (B) JS de isolamento DEFINITIVO: walk de ancestrais a partir de #atrasos-print-area até <body>, em cada nível setando `display:none` inline em todos siblings (exceto SCRIPT/STYLE/LINK/META) e forçando ancestrais a `position:static / overflow:visible / height:auto / margin:0 / padding:0 / background:white`. Cada nó snapshotado via `getAttribute("style")` na `restoreList` (null quando sem style inline → `removeAttribute` no cleanup, sem `style=""` residual). Cleanup idempotente (flag `cleaned`), restaura ordem reversa, triggers `afterprint` once + safety 60s. version → 1903. **Resultado**: layout fora do print area é DELETADO → document body do tamanho exato do conteúdo → @page só quebra onde tem conteúdo real → zero página fantasma, zero vazamento, sempre centralizado. **Preservado**: densificação Rev. 1901 (grid 2-cols, paddings, fontes, DESVIO box), PrintHeader, rodapé, dedupe Rev. 1899, word-break, tela não-impressão idêntica (restauração 1:1). Zero backend/DB/schema. Reversível em 2 hunks + version bump. R-001/R-007/R-010 OK.
