# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per module
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (e.g., `shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: Defined by tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> Histórico completo em `shared/changelog.ts`. Aqui ficam apenas as últimas ~5 entregas. Para revisões anteriores (Rev. ≤ 1670), consulte o changelog.

- **Rev. 1764**: **Painel RH · Card Aviso Prévio — 'Último dia trabalhado' por funcionário**. Pedido (screenshot do card 'Avisos Prévios em Andamento'): além de Término/Prazo pgto, mostrar a data do último dia trabalhado. **Fix sem schema** — Server `homeData.ts` ~L540: novo campo `ultimoDiaTrabalhado` com regra (1) preferir `employees.dataDesligamentoEfetiva` se preenchida; (2) tipo `*_indenizado` → `dataInicio - 1` (dia da comunicação); (3) tipo `*_trabalhado` → `dataFim` (cumpriu aviso). Client `PainelRH.tsx` ~L388: nova linha 'Último dia trab.: dd/MM/aaaa' entre Término e Prazo pgto, defensiva (`{a.ultimoDiaTrabalhado && (...)}`).
- **Rev. 1763**: **SST · PGR/PCMSO/LTCAT — 'Acesso Restrito' resolvido pra Administrador SST**. Reportado: Rodnei com grupo SST=Administrador levava 'Acesso Restrito' em PGR/PCMSO. Causa em `PermissionsContext.tsx` L336-339: o localizador do módulo dono comparava `feature.route === route || basePath`, mas as features SST de programas legais (`shared/modules.ts` L210-212) têm route com query string (`/programas-sst?tab=PGR/PCMSO/LTCAT`) — nenhuma com base plain. RouteGuard de `/programas-sst` (App.tsx L380) chama sem `?tab=`, então `mod=undefined → return false` ANTES da checagem de nível admin. **Fix sem schema** em 2 pontos: (1) localizador também compara `feature.route.split('?')[0] === basePath` — admin/viewer passam imediato. (2) usuário `custom`: quando não há entrada direta no moduleRouteMap mas EXISTEM entradas tab-only pra essa base, libera se QUALQUER pageId tab-only estiver com view=true.
- **Rev. 1762**: **Aniversariantes — Funcionários desligados/Lista Negra não aparecem mais no widget**. Reportado (screenshot 'Aniversariantes — Maio'): EDIVALDO/ADEILDO/EMERSON com badge vermelho 'Lista Negra' apareciam normalmente. Causa em `server/routers/homeData.ts` L47: `STATUS_FORA = ['Desligado', 'Lista_Negra', 'Inativo']` exigia match EXATO, mas `EmpStatusBadge` aceita 2 grafias (`Lista_Negra` underscore E `Lista Negra` espaço) — registros salvos com a variante 'Lista Negra' escapavam. **Fix sem schema** em `homeData.ts`: helper `normStatus(s) = lowercase + replace /[\s_]+/g` colapsa todas variantes pra 'listanegra'; novo Set `STATUS_FORA_NORM = {desligado, listanegra, inativo}`; cinto-e-suspensório também checa flag `listaNegra === 1` (coluna SMALLINT, schema L941) — exclui mesmo se status estiver em outro valor. Afeta `aniversariantes` (mês), `aniversariosEmpresa` (anos de casa) e contadores HOJE/MES.
- **Rev. 1761**: **Permissões — TODOS os módulos expandidos com granularidade real (replicação do SST em escala)**. Pedido: 'Faça isso em todos os módulos'. Auditoria completa cruzando sidebar (`DashboardLayout.tsx`, 17 módulos) × `shared/modulePages.ts` × `shared/modules.ts` revelou 60+ rotas visíveis sem mapeamento de permissão e 40+ páginas que nunca apareciam na tela de Usuários > Permissões. **Fix sem schema change** em `modulePages.ts` (pages + ROUTE_TO_PAGEID) e `modules.ts` (features). Páginas novas por módulo: RH+3 (Painel/Hab.Obra/Comunicados/Currículos), Jurídico+6 (4 painéis + Tributário/Civil), Avaliação+7 (todas as sub-abas: Avaliar/Realizadas/Raio-X/Avaliadores/Critérios/Pesquisas/Clima), Terceiros+5 (Template/IntegraSign/Portal/Advert./Crachás), Parceiros+3, Orçamento+1, Compras+6 (Painel/Dash Obra/FD/Comissões/Databook/Config/Integrações), Almox+1, Financeiro+2 (Cronograma/Config), Operacional+2 (Diário Obra/Liberação Serv.), Proj.Doc+3 (Painel/ARTs/Config), Frotas+6 (Analítico/Dash Manut./Raio-X/Checklist/Preços Comb/Pedágios/Km), Cadastro+7 (Contas Banc./Fornec./Usuários/Config/Auditoria/Lixeira/Revisões), Medição+1, Comunicados+1, Currículos+1. Features em modules.ts ampliadas em paralelo (especialmente Cadastro: 2 → 18). **Atenção `custom`**: páginas novas começam ocultas até admin liberar — Admin Master e usuários `admin`/`viewer` veem tudo automaticamente.
- **Rev. 1760**: **SST · Permissões — módulo expandido com TODAS as áreas (Painel, ASOs, Acidentes, PGR/PCMSO/LTCAT, Integração)**. Reportado: tela de permissões SST mostrava só 3 entradas (EPI, CIPA, Dashboards) — muito aquém do que o módulo gerencia. **Fix sem schema change** em `shared/modulePages.ts` e `shared/modules.ts`: (1) MODULE_PAGE_CONFIG.sst.pages cresce de 3 → 11 (`painel_sst`, `epi`, `cipa`, `aso_documentos`, `acidentes`, `dds`, `pgr`, `pcmso`, `ltcat`, `integracao_sst`, `dashboards`); (2) ROUTE_TO_PAGEID.sst cresce de 6 → 18 rotas com granularidade real (sub-abas de EPI consolidadas, ASOs e Acidentes separados de 'epi', cada programa legal com chave própria); (3) Features do módulo em `modules.ts` cresce de 11 → 17. **Atenção pra usuários `custom`**: quem tinha 'epi' marcado vai precisar re-marcar 'acidentes' e 'aso_documentos' nas permissões. Páginas novas (Painel/PGR/PCMSO/LTCAT/Integração) ficam ocultas até admin liberar — comportamento seguro.
- **Rev. 1759**: **DDS · Permissões — feature DDS registrada no módulo SST (estava invisível pra todo usuário não-admin)**. Reportado: 'DDS — Diálogo Diário' não aparecia no sidebar SST do Rodinei (admin master via). **Causa raiz**: a página DDS foi criada (Rev. 1726) e plugada ao sidebar/router, mas nunca foi registrada em `shared/modules.ts` (features do módulo SST) nem em `shared/modulePages.ts` (`ROUTE_TO_PAGEID.sst`). Pra usuário não-admin no novo sistema, `groupCanAccessRoute('/sst/dds')` em `PermissionsContext.tsx` L336-357 não achava o módulo dono e negava por segurança ('rota dentro do módulo sem mapeamento → nega'). **Fix sem schema change** em 2 arquivos: (1) `shared/modules.ts` L203 — feature DDS adicionada ao módulo SST. (2) `shared/modulePages.ts` L80,L393 — nova `page 'dds'` em `MODULE_PAGE_CONFIG.sst.pages` + entrada `'/sst/dds': 'dds'` em `ROUTE_TO_PAGEID.sst`. Usuários `admin`/`viewer` no SST já veem automaticamente; usuários `custom` precisam que o admin marque o checkbox 'DDS' em Usuários > Permissões > SST.
- **Rev. 1758**: **Compras · `criarSolicitacao` — diagnóstico robusto de falhas no INSERT**. Reportado: toast com 'Failed query: insert into compras_solicitacoes...' truncado sem mostrar o motivo Postgres real. **Fix em `server/routers/compras.ts` ~L2598 (sem schema change)**: (1) Detecção de duplicate key por `e.code === '23505'` OU `e.constraint` (não só string match — perdíamos casos localizados). (2) `console.error` rico com code/constraint/detail/table/column/message/stack — qualquer falha aparece com TUDO que o pg-driver expõe. (3) Mensagens friendly por código: 23502 → 'Campo obrigatório vazio: X', 23503 → 'Referência inválida (FK)', 22001 → 'Texto muito longo', 22P02 → 'Tipo inválido'. Propaga como `TRPCError BAD_REQUEST` em vez do SQL cru.
- **Rev. 1757**: **DDS · `getSessao` — 'Cannot convert undefined or null to object' resolvido DE VERDADE**. O erro persistia após Rev. 1753 (que adicionou try/catch). Stack trace expôs `orderSelectedFields` falhando em `Function.entries` — drizzle quebra quando uma chave da projeção `select({...})` é `undefined`. **Causa raiz**: a Rev. 1753 referenciava `ddsSessoes.criadoEm` e `ddsSessaoFuncionarios.criadoEm`, mas no schema as colunas são `createdAt` (L8169/L8191). Refs viravam `undefined` → toda chamada `dds.getSessao` retornava 500. **Fix de 2 linhas em `server/routers/dds.ts` L1453,L1468**: `criadoEm` → `createdAt` em ambas projeções. Sem schema change. Cliente não consumia esses campos.
- **Rev. 1756**: **Raio-X do Funcionário · Foto do colaborador maior no header**. Pedido do usuário (screenshot RAIO-X de Douglas de Souza Rocha): foto circular ao lado do nome estava pequena. Fix sem schema em `client/src/components/RaioXFuncionario.tsx` L781,788,789: `w-16 h-16 sm:w-24 sm:h-24` → `w-28 h-28 sm:w-40 sm:h-40` (de 64/96px pra 112/160px). Iniciais do placeholder seguem em `text-3xl sm:text-5xl`. Lightbox/borda/zoom preservados.
- **Rev. 1755**: **Apontamentos de Campo · Modal "Detalhes do Apontamento" redesenhado**. Reportado (screenshot iPad): layout denso com scroll horizontal, hierarquia visual fraca, botões apertados. **Fix puramente visual em `client/src/pages/ApontamentosCampo.tsx` ~L816 (sem schema/server change)**: DialogContent vira `max-w-2xl max-h-[92vh] overflow-y-auto p-0 gap-0`. Header com gradiente colorido por status (emerald=resolvido, amber=pendente), avatar circular com iniciais do funcionário, nome em destaque + ícones para função/obra/data, linha de 3 chips coloridos (Status/Tipo/Prioridade). Body com mini-cabeçalhos uppercase: 'Horário do ponto' vira grid 2x4 com cards coloridos (entradas emerald, saídas rose, vazios slate com `—:—`); 'Descrição' em card slate; 'Resposta do RH' em card emerald estruturado. Footer dividido: ações primárias (Resolver/Editar/Reabrir) à esquerda, secundárias (Excluir ghost vermelho + Fechar) à direita. Classes Tailwind estáticas (sem interpolação dinâmica) para garantir JIT.
- **Rev. 1754**: **ModuleHub — Tile ORÁCULO agora respeita o toggle das Configurações (estava hardcoded)**. Reportado: mesmo após Rev. 1751, o card roxo ORÁCULO continuava ativo na home com Oráculo 'Desabilitado' nas Configurações. Causa: tile renderizado HARDCODED em `ModuleHub.tsx` L798 (fora do `activeModules.map`), só checava `isAdminMaster`. **Fix sem schema**: condição vira `{isAdminMaster && isModuleEnabled('oraculo') && (...)}` — desabilitar Oráculo nas Configurações some o tile imediatamente, inclusive pra admin_master.
- **Rev. 1753**: **DDS · getSessao — 'Cannot convert undefined or null to object' resolvido (projeção explícita + fim do raw SQL)**. O card de erro da Rev. 1752 expôs o erro REAL que vinha falhando silenciosamente desde a Rev. 1748: clicar em qualquer sessão DDS estourava no servidor. Logs: `[tRPC Error] dds.getSessao: Cannot convert undefined or null to object`. Causas: (a) `db.select().from(ddsSessoes)` sem projeção + spread `{ ...s, funcionarios }` topava metadados do driver; (b) `db.execute(sql...)` pra checar assinaturas retornava formato dependente do driver. **Fix sem schema**: `getSessao` projeta as 18 colunas explicitamente, `temAssinatura` calculado em SQL no mesmo `select` dos funcionários (`sql<boolean>\`(assinatura_img IS NOT NULL AND length(...) > 0)\``), try/catch com console.error detalhado (id/companyId/msg/stack), `funcionarios ?? []` defensivo.
- **Rev. 1752**: **DDS · Aba Sessões — Ações inline (Abrir/Excluir) por linha + multi-seleção pra excluir várias de uma vez + loader/erro visível ao abrir sessão**. Reportado: clicar nas linhas da tabela 'não acontece nada' (sem loading visível) e os botões editar/excluir só apareciam DENTRO do detalhe. **Fix sem schema**: (1) Server `dds.excluirSessoes` nova mutation bulk aceita `ids: number[] (1-200)` com `companyId`, soft-delete em batch. (2) Cliente — ternário do detalhe vira árvore `isLoading→spinner / isError→card vermelho com 'Voltar' / data→SessaoDetalhe` (resolve 'não abre nada'). (3) Cliente — tabela ganha checkbox por linha + select-all no header, banner rose 'N selecionada(s) · Excluir selecionadas' chamando `excluirSessoesMut`, nova coluna Ações à direita com botões 'Abrir' (azul, PenLine) e 'Excluir' (rose, Trash2 com confirm). `e.stopPropagation()` nos clicks pra não conflitar com abertura do detalhe.
- **Rev. 1751**: **ModuleHub — Módulos desabilitados nas Configurações sumem totalmente da home (não aparecem mais como 'Em breve')**. Reportado: módulos desligados em Configurações > Módulos do Sistema (Avaliação, Comunicados Internos, Currículos, Oráculo) continuavam aparecendo na home como cards 'Em breve' na seção 'Em Desenvolvimento'. Causa: `ModuleHub.tsx` L540 fazia `futureModules = [...nãoLançados, ...desabilitadosPorConfig]`. Quebrava regra de ouro 'desabilitado = oculto em TODA tela'. **Fix sem schema/server**: `futureModules` agora é `MODULES.filter(m => !m.active && isModuleEnabled(m.id))` — só módulos com `active: false` (roadmap) E habilitados nas Configurações. Desabilitados por config ficam 100% ocultos. Sidebar (DashboardLayout L1510-1530) já respeitava — paridade completa: home + sidebar + rotas.
- **Rev. 1750**: **DDS · Biblioteca — Barra de progresso bulk com % numérico + ETA estimado**. Pedido após Rev. 1747: a barra mostrava só 'X/Y' sem porcentagem. **Fix em `DDSGuia.tsx`**: bloco da barra vira IIFE — calcula `pct = round((idx/total)*100)` exibido em negrito tabular-nums no canto direito do header, e `eta = restantes * 5s` formatado como `~Mm Ss restantes` abaixo da barra. Container `min-w-[340px]`, barra mais grossa (`h-2`), ocupa largura total. Sem schema/server change.
- **Rev. 1749**: **Configurações > Critérios do Sistema — Toggle 'Alerta de Reservas Preventivas' movido pra cá (era aba dentro de Compras)**. Pedido do usuário: a Rev. 1745 escondeu o toggle numa aba 'Alertas' dentro de `/compras/configuracoes`. Como a tela 'Comando Central' (`/configuracoes` aba 'Critérios do Sistema') já consolida critérios de TODOS os módulos via `ComprasConfigSection`/`FinanceiroConfigSection`, o toggle deve viver lá. **Fix sem schema/server change**: (1) `client/src/pages/configuracoes/ComprasConfigSection.tsx` ganha 4ª sub-seção 'Alerta de Reservas Preventivas' (ícone Bell rose, badge verde 'Ligado'/cinza 'Desligado' no header colapsável) entre 'Comissão de Compradores' e 'Regras de Aprovação'. Reaproveita `getConfigCompras`/`salvarConfigOC` (mesmo campo `alertaReservasAtivo` do banco) — afeta o ReservasAlertModal global imediatamente via `utils.purchase.getConfigCompras.invalidate()` no onSuccess. (2) `client/src/pages/compras/Configuracoes.tsx`: aba 'Alertas', state, useEffect setter e imports `Switch`/`Bell` removidos. Restam 3 abas em Compras: Numeração de OC, Regras de Aprovação, Serviços/Contratos.
- **Rev. 1748**: **DDS · Detalhe da Sessão — toast 'Failed to execute json on Response: Unexpected end of JSON input' resolvido (payload `assinaturaImg` estourava o batch tRPC)**. Reportado (screenshot REVTE-CIVIL/Escritório Central · NR-01, 9/9 presentes): toast vermelho ao tentar excluir/operar. **Causa raiz**: `dds.getSessao` fazia `select()` sem colunas em `dds_sessao_funcionarios`, trazendo a coluna `assinaturaImg` (Rev. 1746, PNG dataURL base64 até 2MB/linha). Com 10+ assinaturas o payload >20MB truncava no httpBatchLink (`response.json()` falhava). **Fix**: (1) `getSessao` passa a projetar colunas leves + flag `temAssinatura: boolean` (`length(assinatura_img)>0`) sem trazer o base64. (2) Novo endpoint `getAssinaturaImg` puxa a imagem sob demanda (authz por innerJoin com `companyId`). (3) `excluirSessao` ganha try/catch + RETURNING id + console.error pra rastreio futuro. (4) `AssinaturaPad` recebe `temAssinaturaPrevia` e dispara `getAssinaturaImg` ao abrir; coluna 'Assinatura' troca a miniatura inline por badge azul 'Assinada · dd/MM/aaaa' clicável. Botão Excluir vira async/await com `mutateAsync`+`voltar?.()` em sucesso e label 'Excluindo...'. Sem schema change.
- **Rev. 1747**: **DDS · Biblioteca — Botão 'Gerar TODOS os roteiros com IA' (bulk + progresso)**. Pedido após Rev. 1746: em vez de clicar tema-por-tema, processar tudo de uma vez. **Client `DDSGuia.tsx`**: 2 novos botões azuis no header — '🤖 Gerar todos os roteiros com IA' (só temas sem `conteudoMd` >=80 chars) e 'Regerar tudo' (sobrescreve todos). `confirm()` mostra contagem + estimativa de tempo (~5s/tema). Loop sequencial reaproveita `gerarRoteiroComIA` + `atualizarTema` (zero server change). Estado `bulkIA` controla barra de progresso ao vivo (Loader2 + 'X/Y' + barra % + contador de falhas em vermelho) com botão 'Cancelar' que para após o item atual. Falhas individuais não interrompem o lote (console.warn por tema). Toast final: success se 0 falhas, warning com contagem se houver. `utils.dds.listTemas.invalidate()` ao fim. Sem schema/server change.
- **Rev. 1746**: **DDS · Lista de Presença — Assinatura digital DESENHADA na tela (canvas touch pra iPad/celular)**. Solicitado pelo usuário (screenshot da Lista de Presença com 11 'pendente' e card 'Enviar para FCsign (em breve)'): coletar assinatura dos funcionários AGORA sem depender da integração FCsign. **Schema**: nova coluna `assinaturaImg` TEXT em `dds_sessao_funcionarios` (PNG dataURL base64). **ColFix** (`server/_core/index.ts` Bloco2 + `COLFIX_VERSION` bumpada pra `v1746-...dds-assinatura-img`). **Server** (`server/routers/dds.ts`): 2 novas mutations — `registrarAssinatura` (valida ownership sessão + funcionário, grava img, tipo='desenhada', assinadoEm=NOW(), força presente=1) e `removerAssinatura`. **Client** (`client/src/pages/sst/DDSGuia.tsx`): novo `AssinaturaPad` (Dialog + canvas HTML5 hi-DPI via `devicePixelRatio`, Pointer Events com `setPointerCapture`, `touch-none` pra bloquear scroll do iOS, botões Limpar/Remover/Salvar). Coluna 'Assinatura' ganha botão azul 'Assinar' (PenLine) ou miniatura clicável da assinatura quando já existe. Card FCsign reescrito com contador `N/total assinaram`. Sem npm install.
- **Rev. 1745**: **Compras · Configurações > Critérios de Alertas — toggle 'Alerta de Reservas Preventivas' (liga/desliga pop-up + banner globais)**. Solicitado após a Rev. 1744 explicar o que era o pop-up 'Reservas Preventivas pendentes' que aparecia no Painel SST (e em qualquer tela). **Schema** (`drizzle/schema.ts`): nova coluna `alertaReservasAtivo` SMALLINT default 1 em `oc_number_config`. **ColFix** (`server/_core/index.ts` Bloco2 DO block + `COLFIX_VERSION` bumpada pra `v1745-...alerta-reservas-ativo` pra forçar execução). **Server** (`purchaseRouter.salvarConfigOC`): aceita `alertaReservasAtivo: boolean` opcional, converte pra 0/1. **Client `compras/Configuracoes.tsx`**: nova aba 'Alertas' (ícone `Bell`) com Switch + texto explicativo + botão âmbar 'Salvar Critérios de Alertas'; `onSuccess` invalida `getConfigCompras` pra refletir imediatamente. **Client `ReservasAlertModal.tsx`**: tanto `ReservasAlertModal` (pop-up global) quanto `ReservasBanner` consultam `purchase.getConfigCompras` e ficam totalmente desabilitados (sem disparar `verificarTravamentoCompras`) quando `alertaReservasAtivo === 0`. Default = 1 (ligado), comportamento atual preservado.
- **Rev. 1744**: **Compras/Cadastros · Toast "Sem acesso a esta empresa." ao salvar nova empresa resolvido (helper `_assertCompanyAccess` quebrado)**. Reportado (screenshot do modal Cadastro de Empresa, bloco Dados Jurídicos/Receita Federal): após salvar nova empresa aparecia toast vermelho "Sem acesso a esta empresa." mesmo logado como admin_master. **Causa raiz** (bug latente desde sempre): `_assertCompanyAccess` em `server/routers/compras.ts` L688 fazia `const allowed = await getCompaniesForUser(...); if (!allowed.includes(companyId))` — `getCompaniesForUser` retorna **array de objetos Company** (rows do banco), NÃO array de IDs, então `allowed.includes(numericId)` era SEMPRE `false`. Sintoma no cadastro: após criar a empresa, o `CompanyContext` seleciona a nova, o `ReservasAlertModal` (montado no DashboardLayout) dispara `compras.verificarTravamentoCompras({companyId: novaId})` e o toast vermelho estoura. Logs do servidor mostravam o erro recorrente. **Fix** em `server/routers/compras.ts` — helper reescrito replicando a regra robusta de `terceiros.ts` Rev. 1702: admin/admin_master bypass; demais usuários checam vínculos REAIS em `user_companies` via `getUserCompanyLinks` (sem o fallback LIMIT 1 do `getCompaniesForUser`); usuário sem nenhum vínculo (controle por grupo/módulo) é liberado. Beneficia também as outras 11 procedures de compras que usam o helper. Sem schema change.
- **Rev. 1743**: **Compras · SC — Bug de números duplicados resolvido (UNIQUE INDEX + gerador robusto) + atalho 'Gerar PDF' por SC**. Reportado pelo usuário (screenshot iPad 13/05/2026): tabela mostrava SC-2026-0176 e SC-2026-0175 aparecendo 2x cada (mesmo número, IDs diferentes). Pediu também atalho pra gerar PDF a partir da solicitação. **Causa**: `criarSolicitacao` (server/routers/compras.ts L2528) e `duplicarSolicitacao` (L10522) geravam número via `COUNT(*)+1` — falhava em (a) race condition em criação simultânea, (b) ao excluir SC antiga o COUNT diminuía e a próxima nova colidia. Schema (`comprasSolicitacoes`) NÃO tinha unique constraint em (companyId, numeroSc), Postgres aceitava duplicatas silenciosamente. **Fix em 4 camadas**: (1) novo helper `gerarProximoNumeroSc(db, companyId, offset)` em compras.ts calcula `MAX(suffix)` do ano corrente filtrado por companyId via regex `^SC-YYYY-\d+$`, retorna prefixo + (max+1+offset) padded 4. (2) `criarSolicitacao` e `duplicarSolicitacao` agora rodam em loop de 8 tentativas com retry — incrementa offset a cada iteração; em unique-violation (`23505`/`uq_compras_solicitacoes_numero`) tenta o próximo número, qualquer outro erro propaga. (3) ColFix em `server/_core/index.ts`: DO block PL/pgSQL idempotente que renumera duplicatas existentes (ROW_NUMBER>1 por company_id+numero_sc) pro próximo suffix livre do mesmo ano, com `RAISE NOTICE` por SC renumerada; em seguida `CREATE UNIQUE INDEX IF NOT EXISTS uq_compras_solicitacoes_numero ON compras_solicitacoes (company_id, numero_sc)`. (4) Atalho PDF: `client/src/pages/compras/Solicitacoes.tsx` ganha função `gerarPdfSC(scId)` que usa `trpcCtx.compras.getSolicitacao.fetch({id})`, monta HTML standalone (cabeçalho com numeroSc/obra/solicitante/data/status + tabela de itens com qty/un/observação/preço meta) e dispara `window.open()` + `print()` (no iOS o usuário escolhe 'Salvar em PDF'). Ícone novo `FileDown` no import lucide-react. Botão pequeno emerald entre Duplicar e Excluir em cada linha + botão grande 'Gerar PDF' no footer do modal de detalhe.
- **Rev. 1742**: **RH · Sidebar — 'Efetivo por Obra' agora aparece em Operacional (cadastro/edição), não só como Dashboard**. Reportado: no módulo RH só existia 'Dashboard Efetivo por Obra' (somente leitura) — pra editar/transferir funcionários o usuário precisava trocar pro módulo Cadastros e voltar. O feature `/obras/efetivo` (componente `ObraEfetivo.tsx`) JÁ estava em `shared/modules.ts` (L132 do módulo rh-dp) e na ACL de `GruposUsuarios.tsx`, só faltava no sidebar visual. **Fix 1 linha** em `client/src/components/DashboardLayout.tsx`: adicionado `{ icon: HardHat, label: 'Efetivo por Obra', path: '/obras/efetivo' }` como primeiro item do bloco 'Operacional' do `menuSectionsRHDP`. O Dashboard segue intacto no bloco 'Dashboards'. Sem schema/server change.
- **Rev. 1741**: **Proj./Doc. Técnicos · Modal Nova Pasta no iPad lançava 'The string did not match the expected pattern'**. Reportado (screenshot iPad 13/05/2026 08:15): criar pasta 'Proposta técnica/PT' em ARQ exibia toast de erro e bloqueava. **Causa**: `<input type="color">` no Safari iOS exige hex EXATAMENTE `#rrggbb` lowercase — defaults do form (`#3B82F6`) e dos atalhos de disciplina usam UPPERCASE, WebKit lança `DOMException: The string did not match the expected pattern` ao montar o input, e o catch genérico exibia como toast. Chrome/Firefox toleram, só iPad quebrava. **Fix em `client/src/pages/gestaodocumentos/index.tsx` (1 linha, sem schema)**: input color normaliza pra lowercase no value E onChange (`(newDiscForm.cor || '#3b82f6').toLowerCase()`).
- **Rev. 1740**: **DDS — Roteiros detalhados em cada tema (biblioteca pré-preenchida + Gerar com IA nas 3 telas)**. Solicitado pelo usuário: instrutor precisa de descrição do que abordar em cada tema (ex.: 'NR-12 — Segurança em Máquinas: o que tratar com a equipe?'). Confirmado: (1) **ambos** = roteiros prontos no banco + botão IA; (2) **3 telas** = Detalhe da sessão + modal Nova Sessão + Biblioteca. **Server (`server/routers/dds.ts`)**: novo `ROTEIROS_DETALHADOS` Map cobrindo TODOS os 33 temas padrão (13 NRs + 12 campanhas Janeiro Branco→Dezembro Vermelho + 8 vacinações PNI/MS) — markdown estruturado em 6 seções fixas (Objetivo / Por que importa / Pontos-chave / Aplicação prática na obra / Perguntas pra equipe / Reforço final), 800-1500 chars cada. Seeds `seedTemasPadrao`/`seedVacinacaoPNI` gravam `conteudoMd` direto. Nova mutation `enriquecerTemasPadrao({sobrescrever})` faz backfill idempotente em temas já cadastrados (atualiza só com conteudoMd <80 chars por padrão). Nova mutation `gerarRoteiroComIA({titulo, descricao?, normaReferencia?, categoria?, obraNome?, funcoesPresentes?[]})` chama `invokeLLM` (Claude→Gemini, GOOGLE_API_KEY já configurada) com systemPrompt CREA-SST exigindo as 6 seções e linguagem de escolaridade média. **Client (`client/src/pages/sst/DDSGuia.tsx`)**: (a) componente `RoteiroMd` — renderer leve markdown sem libs externas; (b) header ganha botão '✨ Enriquecer roteiros dos temas padrão'; (c) Biblioteca: cada card ganha toggle 'Ver roteiro' (verde quando preenchido, cinza vazio) + botão violeta 'Gerar/Regerar com IA' (Loader2 spinner, salva via `atualizarTema`); (d) modal Nova Sessão Bloco 3: botão 'Gerar com IA' contextualizado por obra atual + funções da equipe pré-selecionada (`funcsObraQ` filtrado), textarea 6 linhas font-mono + painel de pré-visualização ao vivo; (e) `SessaoDetalhe`: render bonito via RoteiroMd em card slate, botões 'Gerar com IA' (usa funções dos presentes) + 'Editar/Salvar/Cancelar' inline — só em sessão `aberta`. Sem schema change.
- **Rev. 1739**: **DDS · Detalhe da Sessão — 'Adicionar funcionário' falhava com Zod `nome` undefined**. Reportado pelo usuário (screenshot iPad em 13/05/2026 07:07): após abrir uma sessão DDS criada (REVTE-CIVIL · NR-01), o select 'Adicionar funcionário…' aparecia em branco e clicar em '+ Adicionar' lançava `path:["adicionar",0,"nome"] expected:string received:undefined`. Causa: `SessaoDetalhe` recebe a lista do `trpc.employees.list` (rota global), que devolve `nomeCompleto` (campo do schema, sem alias) — diferente das procedures de `dds.ts` que aliasavam pra `nome`. O cliente lia `e.nome` (undefined) tanto no `<SelectItem>` (label vazio) quanto no payload `marcarPresenca.adicionar[0].nome`. Fix em `client/src/pages/sst/DDSGuia.tsx`, sem schema/server change: 2 ocorrências passam a usar `e.nomeCompleto ?? e.nome` (linha 1262 payload + linha 1314 SelectItem). As outras 2 leituras de `e.nome` no arquivo (busca + card de equipe) consomem `funcsObraQ.data` que JÁ vem com alias `nome`, seguem corretas.
- **Rev. 1738**: **DDS — '0 colaboradores na equipe' resolvido (coluna `employees.nome` → `employees.nomeCompleto`)**. Causa raiz REAL da REVTE-CIVIL e de todos os relatos de equipe vazia no DDS, confirmada por inspeção direta do Neon: REVTE-CIVIL é obra única (id=12, companyId=60002, sem duplicata) com 11 obra_funcionarios ATIVOS no companyId=60002. As Revs. 1731/1733/1735 referenciavam `employees.nome` em vários `select`/`orderBy` mas a coluna real do schema (drizzle/schema.ts L880) é `employees.nomeCompleto`. Postgres lançava `column e.nome does not exist` (erro 42703), o tRPC capturava silenciosamente e o componente renderizava 0 itens — sem toast. A Rev. 1737 (parent+subsidiárias) partiu de hipótese errada e foi inteiramente revertida (architect identificou também gap de cross-tenant ao confiar em `companyIds[]` do client). **Fix mínimo, sem schema change**: 5 ocorrências em `server/routers/dds.ts` trocadas para `employees.nomeCompleto` (`funcionariosDaObra` select+orderBy, `colaboradoresParaTransferir` select+orderBy, `acidentesRecentes` empNome, `criarSessao` pré-carga). Alias TS continua `nome` — cliente não muda. Toda a Rev. 1737 (helpers/inputs `companyIds[]`/inArray) removida.
- **Rev. 1736**: **DDS · Calendário Anual — seletor de ano + ano atual exibe só meses pendentes**. Solicitado: o calendário abria com os 12 meses do ano (poluído com janeiro–abril já passados). Pediu (1) ano atual = só campanhas que faltam, (2) próximos anos com tudo, (3) opção de ver as 12 do ano corrente. Fix em `client/src/pages/sst/DDSGuia.tsx`, sem schema/server change: states `calendarioAno`/`verTodasCampanhas`, toolbar com chips de ano (atual + 2 próximos) e botão âmbar 'Ver todas as campanhas de {ano}' (visível só no ano atual), filter no `.map` do `camp` (`ano>atual || verTodas || m.mes>=mesAtual`), ring emerald do 'mês atual' só quando `calendarioAno===anoAtual`, badge 'sessão(ões) este ano' troca por 'Planejado para {ano}' nos anos futuros (servidor retorna sessões só do ano corrente).
- **Rev. 1734**: **Solicitações de Compra — 9 mini-blocos do card 'Status das Solicitações' agora filtram a tabela**. Solicitado pelo usuário (screenshot iPad do card Rev. 1732): cada bloco (Aguardando aprovação / Aprovadas (sem OC) / Pendente / Em cotação / Em andamento / Entrega parcial / Concluídas / Recusadas / Canceladas) deve filtrar a tabela ao ser clicado. Fix em `client/src/pages/compras/Solicitacoes.tsx` sem schema change: (1) novo state `filtroBreakdown` independente do `filtroStatus` das KPI badges. (2) `breakdownPredicates` reusa a MESMA lógica do `useMemo statusBreakdown` (count e filtro batem pixel-por-pixel). (3) `listaFiltradaObra` ganha etapa adicional aplicando o predicado quando ativo. (4) `<div>` → `<button>` com toggle (clicar no mesmo bloco limpa); ativo ganha `ring-2 ring-inset` da cor do bullet + bg slate suave. (5) ativar breakdown reseta `filtroStatus` pra 'todos' (evita combinação inconsistente). (6) banner âmbar abaixo da grade quando há filtro ativo: 'Filtro ativo: N solicitação(ões)' + botão 'Limpar filtro ✕'. KPI badges abaixo seguem funcionais.
- **Rev. 1733**: **DDS — Sidebar de obras consolidado por nome (corrige '0 colaboradores' em obras duplicadas)**. Reportado: REVTE-CIVIL no modal Nova Sessão DDS mostrava "0 colaboradores na equipe" mas o cadastro > aba Efetivo exibia o time. Causa: existem cadastros DUPLICADOS com mesmo nome (IDs diferentes) — sidebar do DDS listava 1:1 e ao clicar consultava o efetivo de só UMA delas. O cadastro > Efetivo já consolida via `getEfetivoPorObra` (trim+UPPER do nome). Fix em 2 camadas, sem schema change: (1) **Server `dds.ts`**: as 3 queries do modal (`funcionariosDaObra`, `colaboradoresParaTransferir`, `acidentesRecentes`) passam a aceitar `obraIds: number[]` (opcional ao lado do `obraId` legado). Validam ownership de TODAS as ids contra `companyId`, deduplicam funcionários por `employeeId`. `colaboradoresParaTransferir` exclui quem está em QUALQUER duplicata. `acidentesRecentes` usa `inArray`. (2) **Client `DDSGuia.tsx`**: `obrasConsolidadas` (Map por chave canônica) usado no sidebar — cards mostram nome único e micro-label '(N cadastros consolidados)' quando há duplicatas. `sessaoForm.obraIds: number[]` carrega TODAS as ids do grupo ao clicar; `obraId` (canônico = primeiro id) preservado pra `criarSessao`. Badge ⚠️ no sidebar agrega acidentes de todas as duplicatas. Sub-diálogo de transferência envia para o id canônico. Avulsa → `obraIds: []`.
- **Rev. 1732**: **Solicitações de Compra — Card superior 'Status das Solicitações' (visão consolidada granular)**. Solicitado pelo usuário (screenshot iPad de `/compras/solicitacoes`): as 4 KPI badges existentes são macro-status — faltava uma visão por estado de aprovação/cotação/entrega. Fix em `client/src/pages/compras/Solicitacoes.tsx` SEM schema change: novo `useMemo statusBreakdown` calcula 11 métricas (total/ativas/aguardandoAprov/aprovadasSemOC/pendente/emCotacao/emAndamento/entreguesParcial/concluidas/recusadas/canceladas/urgentes) a partir do `listaKpisBase` (mesma fonte das KPI badges, respeita filtro de obra). Card renderizado ANTES das KPI badges com gradient slate, header 'Status das Solicitações ({total} no total · {ativas} ativas)' + subtítulo, badge vermelho pulsante '⚠️ N URGENTE(S)' à direita quando há urgentes ativas, grid responsivo (2/3/5/9 col conforme largura) com 9 mini-blocos divididos por borda — número grande tabular + bullet colorido (opaco quando 0) + label. As 4 KPI badges existentes seguem clicáveis como filtro abaixo, intactas.
- **Rev. 1731**: **DDS — Modal full-screen com obras como eixo principal + transferir colaborador inline + regra obrigatória de acidente D-1 (Lei art. 157 CLT)**. Solicitado pelo usuário em 3 frentes (vide screenshot iPad): (1) modal full-screen ajustado; (2) DDS é por obra → obras precisam aparecer já com o efetivo, com opção de transferir colaborador pra regularizar na hora; (3) **regra obrigatória**: se houver acidente cadastrado na empresa, o DDS do dia seguinte DEVE abordar o assunto com detalhes dos fatos. **Server (`server/routers/dds.ts`)**: 3 novos endpoints — (a) `colaboradoresParaTransferir(companyId, obraId)` lista ativos da empresa fora da obra (subquery `notInArray`); (b) `transferirParaObra(companyId, obraId, employeeId)` cria/reativa registro em `obra_funcionarios` com authz por companyId e bloqueio de status terminais; (c) `acidentesRecentes(companyId, obraId?, diasJanela=7)` retorna acidentes dos últimos 7 dias com flag `obrigatorio=true` quando `dataAcidente === ontem`, LEFT JOIN com employees/obras pra trazer nomes. **Client (`client/src/pages/sst/DDSGuia.tsx`)**: layout 2-painéis full-screen `!max-w-[98vw] h-[95vh]`. Sidebar esquerda (col-span-3): busca + card Avulsa/Escritório + cards de obras com badge pulsante vermelho '⚠️ N' quando há acidente obrigatório. Painel principal (col-span-9): (1) barra-resumo obra alvo + count colaboradores; (2) **banner vermelho 'DDS OBRIGATÓRIO HOJE — Acidente registrado ontem'** citando Lei art. 157 CLT/NR-1, com botão 'Aplicar como tema' que pré-popula título + conteúdo estruturado (data, colaborador, obra, tipo, gravidade, local, parte do corpo, agente causador, dias afastamento, descrição, ação corretiva, pontos a reforçar); (3) banner âmbar suave pra acidentes não-obrigatórios dos últimos 7 dias com chips de atalho; (4) Data/Hora com chips; (5) Tema agrupado por categoria; (6) Título + roteiro colapsável; (7) Instrutor com auto-fill; (8) Local com histórico; (9) **Bloco Equipe da Obra com botão azul 'Transferir colaborador'** que abre sub-diálogo com lista de candidatos, busca por nome/CPF/função, botão 'Transferir →' por linha — após transferência invalida queries e marca como presente automaticamente; (10) Observações. Footer fixo. Sem schema change.
<!-- Rev. ≤ 1730: ver shared/changelog.ts (histórico completo) -->

## Architecture decisions

- **Consolidated Payroll Model**: Parent company (`input.companyId`) owns payroll periods and records. Read queries use `allCompanyIds` IN clauses for subsidiary data, while write queries use `input.companyId` for consolidation.
- **Single Database Source (Neon)**: The system exclusively uses Neon PostgreSQL via `NEON_DATABASE_URL`. `DATABASE_URL` is *never* used as a fallback. All schema changes and fixes are applied directly to Neon.
- **Tenant Isolation**: All data operations (`INSERT`, `SELECT`, `UPDATE`, `DELETE`) consistently filter by `companyId` to ensure strict tenant isolation. Soft-deletes are handled by filtering `deletedAt IS NOT NULL`.
- **EAP Adaptability**: Budget structures (EAP) can vary wildly per project (depth, naming, accumulated values). Cross-module logic (budget × schedule, financial curve, measurements) is designed to be adaptive, automatically detecting leaf items and normalizing values.
- **Specialized AI per Module**: Instead of a generic AI assistant, each module (Planning, Budget, Purchasing, HR, Finance, SST, Measurement) integrates a dedicated AI widget with specific system prompts and context for relevant insights.

## Product

- **HR & DP**: Payroll, time tracking, employee management, benefits.
- **SST (Safety)**: EPIs, ASOs, CIPA, NRs, PGR/PCMSO/LTCAT management with AI-powered document upload.
- **Jurídico**: Legal case management (Labor, Tax, Civil) with rich dashboards, KPIs, and alerts.
- **Terceiros**: Third-party contractors, detailed measurement workflows, PDF generation, financial integration.
- **Parceiros**: Benefits partners management.
- **Orçamento**: Budget creation (Excel import), 3 versions (Sale/Cost/Target), ABC curve, BDI, EAP tree, AI assistant.
- **Financeiro Integrado**: Comprehensive financial module integrating all other modules, auto-DRE, cash flow projection, KPI tracking, COSO approvals.
- **Compras**: Full procurement cycle (SC → Quotation → PO → Warehouse), smart purchasing, budget control, supplier portal, automated alerts.
- **Operacional**: Construction site operations including Daily Log, RDO, Checklists, Concreting, Non-Conformities, Photo Register, Technological Tests, BIM 3D/4D viewer.
- **IntegraSign**: Internal electronic signature module for service contracts.
- **Portal do Prestador de Serviço**: External portal for service contract management and measurements.
- **Frotas**: Fleet and vehicle management (maintenance, fuel, tolls, fines, insurance, tracking, vehicle checklists, Infleet API integration).
- **SMO (Manpower Request)**: Workflow for requesting manpower, financial impact analysis, onboarding checklists, approval process.
- **HE (Overtime Request)**: Workflow for requesting overtime, approval, and financial impact.
- **Currículos**: Resume bank with AI-powered parsing, status tracking, and duplicate detection.
- **Controle de Integrações**: Tracking employee integrations with clients and internal refreshers.
- **Integração de Segurança SST**: Onboarding/training module for safety with public quiz portal and admin dashboard.
- **Telemetria & Analytics**: Tracks platform usage, user engagement, and AI interaction for administrative insights.
- **Dashboard Executivo de Obras**: High-level project KPIs, progress tracking, and health matrix.
- **Proj./Doc. Técnicos**: Document management system for technical project documentation with revision control and ART/RRT management.
- **Medição**: Contract measurement sheets, bulletin generation, direct invoicing (FD).
- **Integração Mas Controle ERP**: Data import from Mas Controle ERP via API or CSV.

## User preferences

- After every completed adjustment, remind the user to click **Publish** to deploy. Deployment config: autoscale, build=`pnpm run build`, run=`node dist/index.js`.
- **Regra de ouro — Datas no padrão brasileiro**: TODA data exibida ao usuário (tabelas, drill-downs, modais, listas, exports visuais) deve estar em **dd/MM/aaaa**. Nunca exibir `YYYY-MM-DD` cru vindo do banco. Padrão de conversão simples: `s.split("-").reverse().join("/")`.
- **Regra de ouro — Paridade Portal × Planejamento**: O Portal do Cliente NUNCA pode divergir do módulo Planejamento (REFIS, Curva S, Avanço Físico, SPI, etc.). O módulo **Planejamento é a fonte única da verdade**. Sempre que houver cálculo replicado no Portal (`PortalPlanejamentoCliente.tsx`), ele deve espelhar EXATAMENTE a fórmula do ERP em `PlanejamentoDetalhe.tsx` — mesmo universo de atividades (folhas com `dataInicio && dataFim`), mesmo denominador, mesma convenção para indiretas (curva prevista linear no realizado). Antes de fechar qualquer ajuste em métrica de planejamento, verificar lado-a-lado os dois lados.

## Gotchas

- **CamelCase in Raw SQL**: Always quote camelCase column names in raw SQL (e.g., `"companyId"`) because PostgreSQL lowercases unquoted identifiers, leading to mismatches with Drizzle schemas.
- **New Columns**: For new database columns, always add them via `ColFix` in `server/_core/index.ts` to ensure they are present before queries run, as `syncSchema` might not execute early enough during startup.
- **Deleting Projects**: Deleting a project triggers a CASCADE delete of ALL child data across 37 tables. No deleted project data is reusable.
- **Budget Structures**: Budgets have VARIABLE structures. Never assume a single EAP structure; cross-module solutions must be adaptive and tested across diverse project structures.
- **New Module Registration**: When creating a new module, it MUST be registered in `server/routers.ts` (`ALL_MODULES`), `client/src/pages/Configuracoes.tsx` (`MODULE_INFO`, `MODULE_PAGES`), and `client/src/pages/ModuleHub.tsx` (`MODULES`) for full visibility and functionality.
- **Soft-Deleted Companies**: All queries, aggregations, listings, and jobs MUST filter out companies with `companies."deletedAt" IS NOT NULL`.

## Pointers

- **Drizzle ORM**: [https://orm.drizzle.team/docs/overview](https://orm.drizzle.team/docs/overview)
- **tRPC**: [https://trpc.io/docs](https://trpc.io/docs)
- **Tailwind CSS**: [https://tailwindcss.com/docs](https://tailwindcss.com/docs)
- **Neon PostgreSQL**: [https://neon.tech/docs/introduction/about](https://neon.tech/docs/introduction/about)
- **Manus OAuth**: _(External documentation not provided, assume internal docs)_
- **Replit Deployment**: [https://docs.replit.com/](https://docs.replit.com/)
- **Infleet API**: [https://api.infleet.com.br/v1/graphql](https://api.infleet.com.br/v1/graphql)
- **BrasilAPI (CNPJ lookup)**: [https://brasilapi.com.br/docs](https://brasilapi.com.br/docs)
