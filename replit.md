# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per módulo
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (`shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> **Convenção (importante)**: este arquivo guarda APENAS as últimas **5 revisões**, em formato curto (1–3 linhas: o quê + por quê).
> Quando entrar uma nova revisão, **remova a mais antiga daqui** — o histórico completo (com causa-raiz, stack traces, nomes de arquivos, etc.) vive em `shared/changelog.ts`.
> Não duplique conteúdo entre os dois arquivos.

- **Rev. 1887**: **Planejamento · Detalhe da Obra · REFIS · BLOCO 5 ganha DRILL-DOWN PAI → FILHO igual à estrutura do cronograma (abrir todos os níveis da EAP p/ identificar gargalos)**. User (16/05/2026, após Rev. 1886, 2 screenshots — cronograma EAP 03.02→03.02.01… vs. REFIS lateral "NAVE NORTE" só nivel+1): "Gostaria de detalhar um pouco mais o REFIS, abrir até a camada que coloquei na lateral... fica mais fácil identificar gargalos. Quero ter a opção de abrir como pais e filhos, igual a estrutura do cronograma." **Causa**: BarChart de etapas só mostrava sub-grupos de `gNivel+1` (filhos diretos). Quem via macro "PROTEÇÕES DE PISO" 4.2%/6.8% no REFIS não conseguia abrir e ver ONDE dentro estava o atraso. `descendentes()` já existia, só faltava recursão + UI. **Mudanças** (todas em `PlanejamentoDetalhe.tsx`): (1) L11485-11505: 2 states + 2 toggles — `drilldownAbertos` (grupos com painel aberto) + `expandedEtapas` (sub-grupos com filhos visíveis); default vazio = tudo recolhido. (2) L11938-11960: nova função recursiva `buildSubgrupos(parent)` no `useMemo grupos` — cada sub-grupo recebe `.children: [...]` recursivo + previsto/realizado/datas. Substituiu `etapas = desc.filter(...).map(...)` (1 nível) por `etapas = buildSubgrupos(g)` (árvore completa). (3) L13490-13614: novo bloco UI entre BarChart e mini-legenda — botão "Detalhar Pais e Filhos (drill-down EAP)" com chevron, ao abrir renderiza tree-table recursiva via `renderRow(e,depth)` (IIFE). Cada linha: chevron expand/recolher se tem filhos, código EAP mono, nome bold se grupo, mini-barra dupla (Previsto azul 60% + Realizado colorido — verde/âmbar/vermelho mesma regra do BarChart), Previsto/Realizado/Desvio tabular. Indentação `12 + depth*18` reflete hierarquia. Header com legenda visual das 4 cores. Mini-barra `hidden md:flex` (mobile só números). (4) version → 1887. **Preservado**: BarChart nivel+1 INTACTO (drill-down é OPCIONAL, fica DEPOIS); mini-legenda etapas atraso>5pp mantida; calc/prevInd/realMap/pesos reusados sem alteração; `descendentes()` sem mudança; collapsedGrupos intacto; export PDF/print não impactado (drill-down nasce recolhido). Zero backend/DB/schema. Reversível em ~3 hunks + version bump. R-001/R-007/R-010 OK.
- **Rev. 1886**: **Planejamento · Programação Semanal LOTUS · EXPORT EXCEL 100% fiel ao MODELO do cliente (escopo: só arquivo exportado, tela inalterada)**. User (16/05/2026, após Rev. 1885, 2 screenshots — export atual vs. PLANILHA MODELO): "(1) Modelo NÃO tem fundo cinza nos textos, só Sáb/Dom; (2) Previsto SEMPRE vem azul na célula superior, embaixo fica preenchido conforme execução; (3) Largura das colunas — note formato de data que o cliente pede (22-abr, 3-nov)." **Causa**: (a) grupos pintavam `FFE7E6E6` em B-P sobrescrevendo estilo do template; (b) `faixasCelula` (compartilhada com tela) pinta TOP vermelho quando previsto passou sem real — útil na tela mas viola regra do cliente (TOP=PLANO sempre azul, BOTTOM=STATUS); (c) `fmtBR` "dd/mm/yyyy" estourava largura e cortava ("4/05/202"). **Mudanças** (todas em `ProgramacaoSemanalLotus.tsx` DENTRO de `handleExportExcel` — zero impacto na tela/cálculos PV/EV): (1) L1283-1294: removido bloco que pintava cinza nas linhas de grupo, mantida só fonte BOLD (única diferenciação no modelo). (2) L1323-1335: override pós-`faixasCelula` específico do export — se TOP era vermelho e BOTTOM vazio, reescreve TOP=azul (PREVISTO) e BOTTOM=vermelho (ATRASADO). Linha de cima sempre = PLANO; linha de baixo concentra todos os status (verde/vermelho/laranja/amarelo). (3) L1270-1282: novo `fmtCurto(s)` parseia "YYYY-MM-DD" → "D-mmm" (ex: "22-abr", "3-nov") aplicado em E/F/G/H. (4) L1144-1161: novo bloco no início de `preencherAba` garante larguras mínimas D=50 (TAREFA), E/F/G/H=10 (datas curtas cabem), I=14 (RESPONSÁVEL) — só AUMENTA, preserva valores maiores do template. Aplicado em TODAS as abas. (5) version → 1886. **Preservado**: cores oficiais (`COR_PREVISTO/REALIZADO/ATRASADO/ANTECIP/NAO_PROG`) inalteradas; espelhamento "se só tem top, ocupa 2 linhas" intacto; renderização da TELA (`faixasCelula` original) inalterada — engenheiro continua vendo VERMELHO no TOP nos dias de previsto não executado como sinal de atenção; cálculos PV/EV, semáforos, KPIs, layout do template, posição dos 3 logos, merges, cabeçalho — tudo intocado. Reversível em ~4 hunks no mesmo arquivo + version bump.
- **Rev. 1885**: **Almoxarifado · Ferramentas de Terceiros · MODAIS ENTRADA/SAÍDA usam CADASTROS de Empresas + Funcionários Terceirizados + "LANÇADO POR" automático com código interno**. User (16/05/2026, após Rev. 1884 base + UX abas + PDF fix): "Para empresa quero que apareça as empresas terceirizadas cadastradas; em responsável os funcionários terceirizados cadastrados; e marcar o usuário ERP automaticamente com código interno". **Causa**: campos `empresa_terceira`/`responsavel_nome` eram TEXT livres (typo, sem filtro/agrupamento real, sem rastreabilidade do terceiro responsável). Cadastros `empresas_terceiras`+`funcionarios_terceiros` e endpoints `terceiros.empresas.list`/`terceiros.funcionarios.list` já existiam — só faltava integrar. `lancado_por_nome` gravava só name; código interno (employees.codigo_interno via join por email) nem persistido nem exibido. **Mudanças**: (1) Schema `server/_core/index.ts` L941-947: 3 ADD COLUMN IF NOT EXISTS — `empresa_terceira_id INT`, `funcionario_terceiro_id INT`, `lancado_por_codigo_interno VARCHAR(20)`. (2) Backend `ferramentasTerceiros.ts`: novo helper `lookupCodigoInterno(db,userId,companyId)` JOIN users+employees por email (falha-aberta, retorna null se admin sem cadastro RH); novo endpoint `meuLancador({companyId})` p/ badge frontend; `criarEntrada` ganhou `empresaTerceiraId`/`funcionarioTerceiroId` opcionais + grava `lancado_por_codigo_interno` derivado server-side (não confia no client); `criarSaida` herda `empresa_terceira_id` do registro pai; `entradasEmAberto` SELECT amplia com `empresa_terceira_id` p/ filtro reativo. (3) Frontend `FerramentasTerceiros.tsx`: novo `LancadorBadge` (chip emerald com código + nome, label "Lançado por", ou italic "(sem código RH)" se null) no header dos 2 modais; ModalEntrada — Input "Empresa" → Select de `terceiros.empresas.list` (filtra status='ativa', mostra nomeFantasia+CNPJ), CNPJ read-only auto-preenchido; Input "Responsável" → Select de `terceiros.funcionarios.list({empresaTerceiraId})` (disabled até escolher empresa), CPF+Telefone read-only auto; `handleEmpresaChange` limpa funcionário/CPF/tel ao trocar empresa; banner amber se 0 empresas cadastradas direcionando "Terceiros → Empresas Terceiras". ModalSaida — deriva `empresa_terceira_id` do registro pai, Select de funcionários filtrado por essa empresa + Input livre como escape (compromisso: SAÍDA precisa funcionar mesmo se funcionário foi demitido); header agora red-600 (consistência com aba SAÍDAS). (4) version → 1885. **Preservado**: `empresa_terceira` continua TEXT (snapshot legível p/ retrocompat de queries antigas/dashboards), fluxo de fotos multi (Rev. 1884) + IA Claude Vision intacto, `quemRecebeu` obrigatório, multi-tenant via `assertSameCompany` em todos endpoints (incluindo novo `meuLancador`), KPIs/listagens inalterados. Persistência do código interno 100% server-side (lookup por email, ignora client). Zero ALTER/DROP/DELETE — só ADD COLUMN IF NOT EXISTS. Reversível em ~10 hunks backend + 1 arquivo frontend + version bump. R-001/R-007/R-010 OK.
- **Rev. 1884**: **Almoxarifado · Ferramentas de Terceiros · MÚLTIPLAS FOTOS por item (até 8) + DETECÇÃO DE PRODUTO POR IA (Claude Vision)**. User (16/05/2026, após Rev. 1883, screenshot iPad "Nova Entrada de Ferramentas de Terceiro" com 1 slot único de foto): "Quero poder tirar mais fotos, e se tiver como a ia detectar o nome do produto automaticamente pela foto pode?". **Causa**: (a) `itemInputSchema` + tabela `ferramentas_terceiros_itens.foto_url` suportavam 1 foto só (etiqueta+ângulo+avarias impossível documentar junto); (b) descrição 100% manual lenta para almoxarife/motorista. `invokeAnthropicVision` já existia em `_core/llm.ts` — só faltava expor. **Mudanças**: (1) Schema `server/_core/index.ts` L937-940: `ADD COLUMN IF NOT EXISTS fotos_urls TEXT[]` idempotente; `foto_url` mantida como CAPA p/ 100% retrocompat. (2) Backend `ferramentasTerceiros.ts`: import `invokeAnthropicVision`; `itemInputSchema` ganhou `fotos:array(max 8).optional()` + `.refine()` exige fotos[] OU legado (retrocompat); `criarEntrada` loop reescrito p/ upload N fotos antes do INSERT (rollback grátis); INSERT grava `foto_url=urls[0]` + `fotos_urls=ARRAY[…]::text[]` (sql.join escape paramétrico); NOVO endpoint `detectarProdutoPorFoto({fotoBase64,fotoMime})` (L465-526) chama Claude Sonnet 4 com prompt pt-BR pedindo JSON estrito `{descricao,marca,modelo,confianca}`, clamp lengths, sempre retorna shape `{ok,…,erro?}` (falha-aberta — não derruba modal). (3) Frontend `FerramentasTerceiros.tsx`: tipo `ItemEntrada.fotos: FotoItem[]` + flag `detectandoIA`; `MAX_FOTOS_POR_ITEM=8`; `adicionarFotoItem/removerFotoItem/detectarProdutoIA`; IA só preenche campos vazios (não sobrescreve); novo `ItemFotosInput` grid 2-col com badge "CAPA" verde, slot "+ FOTO" enquanto < max, contador "N/8", `e.target.value=""` p/ permitir tirar mesma foto em sequência, `capture="environment"` (câmera traseira iPad); botão violet "Detectar com IA" disabled sem fotos, com loading; modal Detalhes ganhou galeria (capa h-24 + thumb-grid 3-col h-7 + tile "+N"); ItemFotoInput original PRESERVADO p/ modal SAÍDA (1 foto basta na devolução). (4) version → 1884. **Preservado**: fluxo SAÍDA intacto, `responsavel/quemRecebeu` obrigatório, KPIs/listagens/multi-tenant inalterados. Endpoint IA `protectedProcedure` + reusa `decodeFoto` (8MB/mime allowlist). Zero ALTER/DROP/DELETE — só `ADD COLUMN IF NOT EXISTS`. Reversível em ~14 hunks. R-001/R-007/R-010 OK.
- **Rev. 1883**: **Frotas · Dash Combustível · DRILL-DOWN clicável em todos os gráficos + explicação visual de "Não informado"**. User (16/05/2026, após Rev. 1882, screenshot do tooltip "Não informado · 7 abast · 4 veíc · 254L · R$ 1.645"): "Quero todos gráficos responsivos se clicar abre a tela com os dados, seguindo as regras de ouro... e cadê o nome da Camila ela abasteceu, e quem é este não identificado?". **Causa**: ranking agrega por campo TEXT livre `motorista` em fleet_fuel_records via `COALESCE(NULLIF(TRIM,''), 'Não informado')` — os 7 lançamentos "Não informado" são abastecimentos com motorista vazio (lançamento manual incompleto ou import Infleet sem driver); Camila provavelmente está nesses 7 OU registrada com grafia diferente. Não havia ferramenta para o user inspecionar/corrigir. **Mudanças**: (1) Backend `frotas.ts` L4591-4689 novo `getFuelDrilldown({companyId,ano,dim,value})` 5 dimensões motorista|posto|tipo|mes|veiculo; sentinela "Não informado" usa `(campo IS NULL OR TRIM=‘')` paridade exata; multi-tenant duplo (ctx guard + WHERE + JOIN); LIMIT 500. Retorna kpi + rows com km/desconto/observacoes. (2) Frontend `CombustivelDashboard.tsx` reescrito ~500L: estado `drill` + `<Dialog>` fullscreen `!fixed !inset-0 !w-screen !h-screen` (Regra de Ouro). Cliques: ComposedChart mensal (gated qtd>0), PieChart tipos + lista lateral, BarChart postos, BarChart motoristas (Cell cinza #94a3b8 específico p/ "Não informado"), linhas da tabela de veículos. (3) UX/educação: subtitle "toque em qualquer gráfico", `<ClickHint>` em cada CardTitle, banner amber acima do ranking explicando "Não informado" + banner extra dentro do modal instruindo como corrigir (editar lançamento ou criar alias em fleet_driver_aliases). Top Notas: "Não informado" agora italic amber. (4) Modal: header gradient emerald, grid 5 KPIs, tabela adaptativa (esconde coluna da dim filtrada), footer com aviso se LIMIT atingido. (5) version → 1883. **Preservado**: getFuelDashboard intacto (endpoint aditivo), KPIs/sort/formatadores, top 15 notas. Zero ALTER/DML — só SELECTs. Reversível em ~2 hunks backend + 1 arquivo frontend + version bump. R-001/R-007/R-010 OK.
