# ERP RH & DP — FC Engenharia

A comprehensive full-stack ERP system for FC Engenharia, managing HR, payroll, projects, finance, procurement, and operational workflows.

## Run & Operate

- **Dev**: `PORT=5000 NODE_ENV=development pnpm dev`
- **Build**: `pnpm build`
- **Prod**: `node dist/index.js`

**Required Env Vars**:
- `NEON_DATABASE_URL` (or `DATABASE_URL`)
- `JWT_SECRET` (random 48-char hex)
- `NODE_ENV=production`
- `SMTP_PASSWORD`
- `GOOGLE_API_KEY`
- `FROTA_API_TOKEN` (for Infleet API)
- `VITE_APP_TITLE`
- `VITE_APP_LOGO`
- `OAUTH_SERVER_URL`
- `VITE_APP_ID`
- `OWNER_OPEN_ID`

## Stack

- **Frontend**: React 19, Tailwind CSS 4, shadcn/ui, Wouter
- **Backend**: Express 4, tRPC 11, Drizzle ORM
- **Database**: PostgreSQL (Neon)
- **Auth**: Manus OAuth (JWT) or local username/password
- **Build**: Vite 7
- **Package Manager**: pnpm

## Where things live

- `client/`: React frontend
- `server/`: Express backend + tRPC routers
  - `server/_core/`: Auth, OAuth, Vite setup, env config
  - `server/routers/`: tRPC routers per módulo
  - `server/db.ts`: Database helpers
- `drizzle/`: Schema and migrations
- `shared/`: Shared types and constants (`shared/version.ts`, `shared/changelog.ts`, `shared/paymentConditions.ts`, `shared/modules.ts`)
- **DB Schema**: `drizzle/schema.ts`
- **API Contracts**: tRPC routers in `server/routers/`
- **Theme/UI**: `client/src/index.css`, `tailwind.config.ts`, `shadcn/ui` components

## Recent changes

> **Convenção OBRIGATÓRIA (não negociável)** — este arquivo guarda APENAS as últimas **5 revisões** em formato detalhado (o quê + por quê + arquivos tocados). Todas as outras revisões DEVEM aparecer como uma única linha: `- ~~Rev. NNNN~~ — ver \`shared/changelog.ts\`.`
>
> **Ao criar uma nova revisão**:
> 1. Adicionar o bloco detalhado da NOVA revisão no TOPO.
> 2. Pegar o bloco mais ANTIGO dos 5 expandidos e SUBSTITUIR ele inteiro por `- ~~Rev. NNNN~~ — ver \`shared/changelog.ts\`.`
> 3. NUNCA deletar parcialmente um bloco (deixando texto órfão sem o marcador `- **Rev. NNNN**:`). Se sobrar parágrafo solto sem prefixo `- `, é bug.
> 4. NUNCA usar marcadores HTML do tipo `<!-- DETALHES REVS ANTIGAS -->` — eles foram banidos na Rev. 1958 (faxina) por causarem confusão.
>
> O histórico completo (causa-raiz, stack traces, nomes de arquivos, comentários longos) vive em `shared/changelog.ts`. Esta convenção é validada visualmente: cada linha do bloco "Recent changes" deve começar com `- ` (hífen+espaço).

- **Rev. 2021**: **SST · DDS · Funcionários TERCEIROS vinculados à obra entram na lista "Equipe da obra" do modal Nova Sessão**. Pedido direto do usuário (imgs IMG_0865 + IMG_0866): "Os funcionários do terceiros poderia tbm parti do DDS, então precisamos que os nomes do funcionários terceiros apareçam aqui tbm quando ele estiver na obra que o DDS está sendo feita". O modal só listava CLT — pedreiros/serventes terceirizados da obra REVTE-CIVIL ficavam fora do controle. **Decisão**: REUSAR `ddsParticipacoesTerceiros` (tabela já criada na Rev. 2004 pra registrar DDS individual de terceiro). NÃO criar tabela nova; NÃO duplicar em `ddsSessaoFuncionarios` (essa tem FK pra employees CLT). Resultado: histórico unificado, seja participação manual ou via sessão coletiva. **Mudança** em 2 arquivos: (A) `server/routers/dds.ts` — imports `funcionariosTerceiros, ddsParticipacoesTerceiros`; `funcionariosDaObra` ganha try/catch defensivo que faz SELECT em `funcionariosTerceiros` WHERE `companyId + obraId IN (ids) + deletedAt IS NULL`, filtra status != inativo/desligado, mapeia pro shape unificado `{tipo:"terceiro", employeeId:null, funcTerceiroId, nome, cpf, funcao, funcaoNaObra:null, status, empresaTerceiraId, fotoUrl}`. CLT existente ganha `tipo:"clt", funcTerceiroId:null`. Retorno = `[...cltFinal, ...tercFinal]`. Se terceiros falhar, retorna só CLT (DDS não pode quebrar). `criarSessao` input ganha `funcTerceiroIds: z.array(z.number()).optional()`; após inserir CLT em `ddsSessaoFuncionarios`, novo try/catch valida terceiros (inArray + eq companyId + isNull deletedAt) e insere em `ddsParticipacoesTerceiros` com `{companyId, funcTerceiroId, dataDds: input.data, tema: input.tituloTema, instrutor, obraId, obraNome, observacoes, createdBy}`. (B) `client/src/pages/sst/DDSGuia.tsx` — `sessaoForm` ganha `funcTerceiroIds:number[]` (state inicial + abrirNovaSessao); `handleSalvarSessao` envia no payload; `totalMarcados = funcionarioIds.length + funcTerceiroIds.length`; `todosSelecionados` trata cada item conforme `e.tipo`; "Selecionar todos" separa CLT de Terceiros e popula 2 arrays; cabeçalho mostra "(N de M … · inclui K terceiro(s))" laranja; cada `<label>` ganha key única `c-{id}`/`t-{id}`, checkbox mexe no array correto, **chip laranja "TERCEIRO"** ao lado do nome (`bg-orange-100 text-orange-800 uppercase`) com tooltip; status badge (Aviso Prévio etc.) só pra CLT. (C) `shared/version.ts` → 2021. **R-001/R-007/R-010 OK**: ZERO SQL/DDL, ZERO mudança de schema — só SELECT/INSERT via Drizzle em tabelas existentes. Reversível em 2 arquivos. **Preservado**: `obraFuncionarios`/`employees`/`ddsSessaoFuncionarios` INTACTOS; `colaboradoresParaTransferir`/`transferirParaObra` INTACTOS (só CLT); tela `FuncionariosTerceiros > aba DDS` da Rev. 2004 INTACTA (mesma tabela); BLOCOS 1-5+7 do modal INTACTOS; Rev. 2020 INTACTA. **Limitações**: "Transferir colaborador" ainda só lista CLT (pra vincular terceiro à obra, usar cadastro de Terceiros); detalhe da sessão (`getSessao`/UI) ainda não lista terceiros participantes — só ficam visíveis na tela do Terceiro. **Follow-up**: detalhe da sessão listar terceiros misturados com CLT (mesmo badge); "Transferir colaborador" aceitar terceiros; histórico em `FuncionariosTerceiros > aba DDS` mostrar origem (manual vs sessão coletiva); validar ASO/NR válidos antes de aceitar terceiro no DDS.
- ~~Rev. 2020~~ — ver `shared/changelog.ts`. **SST · Integração de Segurança · Bugfix: companyId coercion (Zod number)**. Erro reportado pelo usuário (imgs IMG_0864 + IMG_0867, dispositivos diferentes): toasts `[ { "expected": "number", "code": "invalid_type", "path": [ "companyId" ], "message": "Invalid input: expected number, received string" } ]` e "The string did not match the expected pattern." em `/sst/integracao` — cabeçalho+tabs carregavam mas TODAS as queries falhavam (Vídeos vazio, Configurações vazio, KPIs vazios, botão "Criar configuração padrão" da Rev. 2016 estourava). **Causa-raiz**: `useCompany()` (CompanyContext.tsx) tipa `selectedCompanyId: string` (lê de localStorage como `"12"`); `IntegracaoSST.tsx` L47 fazia `const companyId = selectedCompanyId ?? 0` passando string adiante; routers `integracaoSST.*` (criados na Rev. 2007) declaram `z.object({ companyId: z.number(), ... })` → Zod estoura no input. Outras pages SST não tinham esse bug. **Mudança** em 1 arquivo (cirúrgica): `client/src/pages/sst/IntegracaoSST.tsx` L47: `selectedCompanyId ?? 0` → `Number(selectedCompanyId) || 0`. A correção na raiz propaga via prop pra DashboardTab/VideosTab/ConfigTab/PendentesTab/HistoricoTab/SessoesTab — ZERO mudança nas tabs. + `shared/version.ts` → 2020. **R-001/R-007/R-010 OK**: ZERO SQL/DDL, ZERO mudança de schema, ZERO mudança de routers. Reversível em 1 linha. **Preservado**: TUDO (modal Rev. 2016, upload sem limite Rev. 2013, DashboardLayout Rev. 2018, header gradient Rev. 2005, Rev. 2019 INTACTA). **Follow-up**: padronizar `useCompany()` pra expor `companyIdNum` (number já coerced) e evitar reincidência; auditoria geral de páginas que chamam routers `z.number()` sem coerção (provável que outras pages tenham bug latente que só não disparou porque o usuário ainda não clicou).
- ~~Rev. 2019~~ — ver `shared/changelog.ts`. **DP · Fechamento de Ponto · "Atraso Acumulado" clicável com memória de cálculo dia a dia**. Pedido direto do usuário (img IMG_0863, modal "Mais Atrasados" com 47 colab. e 51h08min totais): "Quero poder clicar no atraso acumulado e ver a memória de cálculo de cada um, para analisar melhor". A coluna mostrava só o agregado em vermelho (3:35, 3:03, …) sem auditoria — gestor não conseguia abrir advertência sem ir manualmente no espelho de ponto. **Decisão**: ZERO mudança no motor de cálculo (`payrollEngine`/`getSummary`/`listInconsistencies`) — apenas EXPÕE a memória que já existe (`timeRecords.atrasos` + `employees.jornadaTrabalho` + tolerância Art. 58 §1º CLT) via nova query + modal. **Mudança** em 3 arquivos: (A) `server/routers/fechamentoPonto.ts` (+125L) — novo procedure `getAtrasoDetalhe(companyId, employeeId, dataInicio, dataFim)`: SELECT employees (jornada) + SELECT timeRecords (data/entrada1/atrasos) + `getCriteriaMap` (tolAtraso, padrão 5min) + parse `jornadaTrabalho` JSON → mapa dow→entrada esperada + itera dias calculando `diff = entrada1 - esperada` e só conta se `> tolAtraso`. Fallback pra dias sem jornada (flag `observacao` na linha). Retorna `{nome, tolerancia, entradaPadrao, totalMinutos, dias:[{data, dow, entradaEsperada, entradaReal, minutos, acumulado, observacao}]}`. ZERO SQL bruto, ZERO mutação. (B) `client/src/pages/FechamentoPonto.tsx` (+~150L): novo state `atrasoDetalhe`, nova query `atrasoDetalheQuery`; célula "Atraso Acumulado" nos modais "Mais Atrasados" E "Mais Pontuais" virou `<button>` com `Info` ícone + `title` explicativo (em Pontuais só vira botão quando atraso > 0); novo `<Dialog>` 820px com header gradient red→rose→pink (coerente com modal pai — regra de ouro), corpo com: faixa indigo explicando a fórmula CLT + entrada padrão, cards de resumo (dias com atraso / total / média), **tabela dia a dia** (Data DD/MM · DOW / Esperado / Real / Atraso badge red-100 / Acumulado) com footer red-50 mostrando total. Empty-state amigável quando tolerância absorveu tudo OU jornada não cadastrada. (C) `shared/version.ts` → 2019. **Resultado**: gestor clica no "3:35" do colaborador → vê tabela "16/04 · Qua | esperado 07:00 | real 07:42 | +0h42min | acumulado 0h42min" + linha por linha até bater 3h35min. Pode imprimir/justificar advertência com base em evidência. **Preservado**: `getSummary`/`listInconsistencies`/`getDiasEmployee` INTACTOS; modal `diasDetalhe` INTACTO; avatar+selo CIPA Rev. 2015 INTACTOS; faixa "Como é calculado" Rev. 2006 + feriados Rev. 2014 INTACTAS; outros modais (Conflitos/Inconsistências/Não Identificados/Dixi) INTACTOS; Rev. 2018 INTACTA. **R-001/R-007/R-010 OK**: ZERO SQL bruto, ZERO ALTER/DROP, ZERO mudança de schema — apenas SELECT via Drizzle. Reversível em 3 arquivos. **Limitações**: jornada não cadastrada cai no fallback consolidado com chip âmbar "ver obs."; saídas antecipadas NÃO aparecem (escopo é atraso de entrada). **Follow-up**: exportar CSV do detalhe pra anexar em advertência; aba "Saídas antecipadas" no mesmo modal; link direto pro Espelho de Ponto do dia clicando na linha; aplicar padrão "clicar pra ver memória" em "H. Total" e "% Presença".
- ~~Rev. 2018~~ — ver `shared/changelog.ts`. **SST · Integração de Segurança · Barra lateral (DashboardLayout) restaurada**. Pedido direto do usuário (imgs IMG_0860 + IMG_0861): "Quero que coloque a barra de comando lateral aqui, do módulo SST… arrume isso de vez". Tela `/sst/integracao` abria o conteúdo (header verde + tabs) SEM o sidebar de navegação SST — usuário ficava preso sem conseguir pular pros outros itens (Controle de EPIs, CIPA, PGR, PCMSO, etc.). **Causa-raiz**: `client/src/pages/sst/IntegracaoSST.tsx` nunca foi envolto em `<DashboardLayout>` — outras pages SST (`DDSDashboard`, `DDSGuia`, `RegistroAcidentes`, etc.) usam o padrão `import DashboardLayout from "@/components/DashboardLayout"` + wrapper, mas essa renderizava direto `<div className="min-h-screen ...">` sem o layout. **Mudança** em 2 arquivos: (A) `client/src/pages/sst/IntegracaoSST.tsx` — 3 hunks aditivos, ZERO lógica: +`import DashboardLayout`; envolveu o `<div>` raiz com `<DashboardLayout>...</DashboardLayout>`. Header gradient emerald→teal, tabs sticky, 6 sub-componentes (Dashboard/Vídeos/Config/Pendentes/Histórico/Sessões), modais, mutations — TUDO intacto. (B) `shared/version.ts` → 2018. **Decisão**: mantemos o header gradient interno apesar do DashboardLayout ter topbar próprio — segue regra de ouro (Rev. 2005) consistente com Compras/RH que têm header colorido dentro do layout. **R-001/R-007/R-010 OK**: ZERO SQL, ZERO mudança de schema, puramente layout/wrapper. Reversível em 1 arquivo. **Preservado**: header+tabs INTACTOS; 6 sub-componentes INTACTOS; modal de novo vídeo da Rev. 2016 INTACTO; upload sem limite Rev. 2013 INTACTO; outras pages SST INTACTAS (já tinham DashboardLayout); Rev. 2017 (Documentos Trabalhistas) INTACTA. **Follow-up**: auditar outras pages recém-criadas que possam abrir sem sidebar; padronizar via wrapper de rota no `App.tsx` pra evitar regressão futura.
- ~~Rev. 2017~~ — ver `shared/changelog.ts`. **Terceiros · Aba Documentos · Nova seção "Documentos Trabalhistas" (Ficha de EPI NR-06, OS de SST NR-01, Registro de Funcionário CLT art. 41)**. Pedido direto do usuário (img IMG_0862): "No módulo terceiros preciso incluir campos para subir estes documentos. Abas para Upload de: Ficha de EPI, Ordem de serviço, Registro de funcionário". A aba já tinha 4 seções (Saúde / Treinamentos NR / Integração / Identificação) com motor genérico de upload + cálculo de % de integração — faltava categoria de comprovantes legais trabalhistas exigidos em fiscalização do MTE. **Mudança** em 3 arquivos: (A) `drizzle/schema.ts` (+3 colunas em `funcionariosTerceiros`, todas nullable retrocompat): `fichaEpiUrl varchar(500)`, `ordemServicoUrl varchar(500)`, `registroFuncionarioUrl varchar(500)`. (B) `server/_core/index.ts` (+6L após bloco numero_interno da Rev. 1998): try/catch idempotente com 3 `ALTER TABLE funcionarios_terceiros ADD COLUMN IF NOT EXISTS`. (C) `client/src/pages/terceiros/FuncionariosTerceiros.tsx`: imports +1 ícone lucide (Briefcase); array `secoes` ganha nova entrada ENTRE "Integração de Segurança" e "Identificação" — titulo "Documentos Trabalhistas", icone Briefcase, cor emerald-700, descricao "Comprovantes legais que devem ficar disponíveis pra fiscalização do MTE", 3 docs `obrigatorio: true` com descrições detalhadas referenciando NR-06 / NR-01 / CLT art. 41 e SEM `validadeField` (não vencem — são comprovantes de cadastro/entrega). (D) `shared/version.ts` → 2017. **Resultado**: gestor sobe os 3 docs do mesmo motor (handleUpload existente), % de integração recalcula automaticamente incluindo os novos obrigatórios, painel Status de Integração (Integrado/Parcial/Não Integrado) reflete o gap de compliance imediatamente. Tenants com 50 terceiros já cadastrados passam de 100% pra X% até subirem os novos docs — comportamento esperado (sinaliza gap que estava invisível). **Preservado**: Painel de Status INTACTO (recalcula via `secoes.flatMap(s=>s.docs).filter(d=>d.obrigatorio)`); 4 seções anteriores INTACTAS na ordem; motor `handleUpload(urlField, editingId)` INTACTO; abas Dados Pessoais/DDS INTACTAS; outras leituras INTACTAS (SELECT * funciona com colunas nullable); Rev. 2016 INTACTA. R-001/R-007/R-010 OK (só ADD COLUMN IF NOT EXISTS idempotente, padrão estabelecido das Revs. 1998/2003/2008). Reversível em 3 arquivos. **Follow-up**: tabela filha `terceiros_epis_entregues` pra rastrear CA/validade de cada EPI; OCR/IA pra extrair número da OS e itens da Ficha de EPI; backfill auto pra terceiros de função específica.
- ~~Rev. 2016~~ — ver `shared/changelog.ts`. Pedido direto do usuário (img IMG_0858): "O que é esta configuração de integração? Não aparece nada quando clico e não me deixa cadastrar o vídeo". Tela travava: aviso amarelo cortado, dropdown vazio, botão desabilitado mesmo com arquivo 139.7MB já escolhido. **Causa-raiz**: modelo Config(1)→Modulos(N) obrigatório — sem Config nenhuma, vídeo não pode ser criado. UX punia o usuário forçando trocar de aba pra criar Config manualmente. **Mudança** em 1 arquivo (`client/src/pages/sst/IntegracaoSST.tsx`, frontend-only, ZERO backend): (1) **Auto-seleção** via `useEffect` quando `configs.data.length===1 && !configId && !editingId` → seta o único; hint emerald-700 "✓ Configuração única auto-selecionada." abaixo do Select. (2) **Empty-state INLINE no modal**: quando `configs.length===0 && !editingId`, o Select é substituído por card border-dashed amber com AlertTriangle + texto explicativo ("Posso criar a padrão 'Integração Geral' (nota mínima 70%, validade 12 meses). Você pode ajustar depois na aba Configurações.") + botão "Criar configuração padrão agora" que dispara `integracaoSST.criarConfig.mutate({companyId, titulo:'Integração Geral', notaMinima:70, validadeMeses:12})`; onSuccess refetch + `setConfigId(novaId)` + toast — usuário continua o cadastro do vídeo SEM fechar/reabrir modal. (3) **Card de aviso amarelo EXTERNO** (fora do modal) ganhou o mesmo botão "Criar configuração padrão" — destrava com 1 clique antes de abrir o modal. (4) **Microcopia**: Label do Select ganhou subtítulo "(nota mínima + validade do treinamento)" explicando o que é a config; placeholder muda ("Nenhuma configuração ativa" vs "Selecione a configuração ativa"). (5) `shared/version.ts` → 2016; +`useEffect` no import do React. **Resultado**: usuário sai de "preciso clicar em outra aba, criar config, voltar e tentar de novo" pra "clico 'Criar configuração padrão agora' → continuo o upload no mesmo modal". Pra quem já tem 1 config, o Select já vem preenchido. **Preservado**: backend `integracaoSST.criarConfig` INTACTO (mesma mutation que ConfigTab usa); ConfigTab INTACTA (quem quer custom continua usando); handleSave do vídeo INTACTO; upload da Rev. 2013 INTACTO. R-001/R-007/R-010 OK (ZERO SQL/DDL — só reusa mutation existente com defaults 70/12). Reversível em 1 arquivo. **Follow-up**: aplicar mesma UX a modais de Sessão e Pendentes; renomear inline da config; reativar config inativa via dropdown.
- ~~Rev. 2015~~ — ver `shared/changelog.ts`. Pedido direto do usuário (img IMG_0857, modal "Mais Horas Extras"): "Coloque a fofinha em todos os nomes, pegue no cadastro de cada um, e quando clicar deve ampliar, quero tbm que marque quem é da cipa ou já foi e tá no período de estabilidade". **Mudança** em 2 arquivos: (A) `server/routers/fechamentoPonto.ts` (~50L em `getSummary`, ZERO ALTER): SELECT do leftJoin com `employees` ganhou `fotoUrl` → propagado como `employeeFotoUrl`; após `byEmployee`, novo bloco try/catch faz UMA query batch em `cipa_members` filtrada por `companyFilter(input)` + `inArray(employeeId, allEmployeeIdsArr)` → reduz pra `cipaInfoByEmp` com prioridade (i) `statusMembro='Ativo'` → 'ativo', (ii) `fimEstabilidade >= hoje` → 'estabilidade' (mais futuro vence empate); mapper final injeta `cipaStatus | cipaCargo | cipaFimEstabilidade`. Falha no CIPA NUNCA quebra getSummary (try/catch com console.warn). (B) `client/src/pages/FechamentoPonto.tsx` (~110L aditivas): imports `Avatar/AvatarFallback/AvatarImage` (shadcn) + ícones `HardHat`/`ImageOff`; state `fotoZoom` + helper `getInitials`; `rankings` useMemo propaga foto+cipa pros 4 modais de ranking; **tabela "Resumo por Colaborador"** ganha Avatar `size-9` (ring-2 branco, hover ring-blue + scale-110) clicável + fallback gradient blue→indigo com iniciais + 2 selos novos (CIPA Ativo emerald sólido + HardHat; CIPA Estabilidade âmbar outline + ShieldCheck + data fim + tooltip citando CLT art. 165); **modal de Ranking** mesma estrutura com Avatar `size-8`; **modal de zoom** Dialog max-w-2xl bg gradient slate dark, img max-h-[70vh] ring-4 branco translúcido, placeholder ImageOff orientando cadastro quando sem foto. (C) `shared/version.ts` → 2015. **Resultado**: identificação visual em 79+ colaboradores fica instantânea; gestor vê selo CIPA antes de abrir advertência/rescisão (compliance art. 165). **Preservado**: badges existentes (Inativo/Ajuste/Aviso Prévio/Confiança) INTACTAS; outros modais (Conflitos/Inconsistências/Não Identificados/Dixi) NÃO mudaram nesta rev — esperar feedback. R-001/R-007/R-010 OK (só SELECT). Reversível em 2 arquivos. **Follow-up**: aplicar avatar nas 4 tabelas restantes; tela de cadastro em massa de fotos; bulk import de membros CIPA históricos.
- ~~Rev. 2014~~ — ver `shared/changelog.ts`. Pedido direto do usuário (img IMG_0856_1779031904262): "O ERP precisa saber quando é feriado e desconsiderar (federais, estaduais e municipais) para não gerar falta equivocadamente, e marcar como feriado". Caso reportado: drill-down do Anderson dos Anjos mostrava **21/04 (Tiradentes)** e **01/05 (Dia do Trabalho)** como "Falta provável" — quando na verdade eram feriados nacionais → inflavam faltas e deprimiam % de presença. **Decisão**: ZERO backend — reusar `feriados.listarPeriodo` que já existe e já é consumido por `payrollEngine` e `EspelhoPonto` (mescla feriados do banco + 8 nacionais fixos + 3 móveis derivados da Páscoa via Meeus). **Mudança** em 1 arquivo (`client/src/pages/FechamentoPonto.tsx`, ~70L úteis): (1) Nova `feriadosPeriodoQuery` retorna Set<YYYY-MM-DD> do período de fechamento; (2) 2 queries `feriados.listar` (uma por ano do range, cobre dez/jan) reduzidas em `feriadoNomeMap: Map<data, nome>` pra exibir nomes; (3) `diasUteisNoPeriodo` useMemo agora subtrai feriados Seg-Sex (dep array atualizado); (4) Drill-down: novo `totalFeriados` com chip âmbar 🎉 no resumo; `totalFaltas` exclui feriados; cada dia checa `isFeriado(ds)` → âmbar com "🎉 Feriado · {nome}" no lugar de "Falta provável"; se trabalhou no feriado mantém verde com ring âmbar; hint atualizado; (5) Faixa "Como é calculado" (Rev. 2006): "Feriados ainda não são excluídos (em estudo)" → "feriados (federais, estaduais e municipais) NÃO entram"; (6) `shared/version.ts` → 2014. **Resultado**: Anderson 21/04 e 01/05 viram "🎉 Feriado · Tiradentes" / "🎉 Feriado · Dia do Trabalho" em âmbar; 4 faltas prováveis caem pras reais; % presença sobe; tabela-pai do "Resumo por Colaborador" também herda benefício (mesmo denominador). **Preservado**: backend `getDiasEmployee`/`getSummary` INTACTOS; router `feriados.*` INTACTO; schema INTACTO; outras telas (EspelhoPonto/payrollEngine) INTACTAS; Rev. 2013 INTACTA. R-001/R-007/R-010 OK (frontend-only, zero SQL). Reversível em 1 arquivo. **Limitações**: estaduais/municipais precisam cadastro manual via `feriados.criar` (UI a fazer); `feriados.listar` só filtra por companyId único — em multi-empresa pode faltar nome do feriado de outra empresa mas DATA ainda é excluída via `listarPeriodo` (suporta companyIds); ponto facultativo tratado igual feriado. **Follow-up**: link "Cadastrar feriado" no drill-down; aplicar exclusão em getStats/listInconsistencies (gap da Rev. 2010); suporte companyIds em `feriados.listar`; tela de presets por UF.
- ~~Rev. 2013~~ — ver `shared/changelog.ts`. Pedido direto do usuário: "Quero poder subir vídeo sem limite de tamanho". Rev. 2012 tinha capado em 600MB via multer + memoryStorage e persistia em DB base64 — inviável pra filmes de integração 1-3GB (RAM + Postgres explodem). **Mudança** em 2 arquivos: (A) `server/_core/index.ts` — endpoint `/api/upload/sst-integracao-video` REESCRITO: multer trocado de `memoryStorage` pra `diskStorage` em `os.tmpdir()/sst-video-uploads`; `limits.fileSize` REMOVIDO; após upload, `fs.renameSync` pro destino final em `server/uploads/sst/integracao/videos/` (fallback copyFile+unlink se EXDEV); **NÃO chama mais `storagePut`** (que faria base64 em DB) — arquivo fica em disco, servido pelo middleware estático `/uploads` já existente; URL retornada `/uploads/sst/integracao/videos/...`; se `forgeApiUrl`+`forgeApiKey` configurados, faz upload OPCIONAL pra storage externa em paralelo (não bloqueia se falhar). Limpeza de tmp em todos os erros. (B) `client/src/pages/sst/IntegracaoSST.tsx`: 2 strings cosméticas — "até 600 MB" → "sem limite de tamanho". (C) `shared/version.ts` → 2013. **Resultado**: usuário sobe vídeo de qualquer tamanho (limitado só pelo disco do Repl e rede dele); RAM não infla (disco direto), Postgres não infla (sem base64), storage externa replicada quando configurada. **Preservado**: modal Rev. 2012 INTACTO (só 2 strings); tRPC `criarModulo/atualizarModulo` INTACTOS; `storagePut` INTACTO (outros módulos continuam usando); middleware estático `/uploads` INTACTO; outros uploads (sst-document 150MB, fotos terceiros) INTACTOS. **Trade-offs**: vídeos só em disco local sem storage externa configurada (somem em wipe); replicação externa ainda carrega RAM (Blob de readFileSync); sem antivírus/quota. R-001/R-007/R-010 OK (zero SQL). Reversível em 2 arquivos. **Follow-up**: streaming real pra externa (createReadStream); quota por empresa; transcoding; preview do vídeo no card.
- ~~Rev. 2012~~ — ver `shared/changelog.ts`. Pedido direto do usuário (img IMG_0855_1779031643993): "Já tenho o vídeo, quero fazer upload ele tem 500mega de tamanho, vou precisar fazer upload mas a tela tá travando, quero poder digitar o nome do filme de integração e só... simplique a tela". Problemas: modal complexo com 4 seções coloridas + ZERO mecanismo real de upload (campo `videoTipo=Upload` existia mas só tinha input de texto pra URL). **Mudança** em 2 arquivos: (A) `server/_core/index.ts` (+25L): novo endpoint `POST /api/upload/sst-integracao-video` com multer memoryStorage 600MB, persiste via `storagePut` em `sst/integracao/videos/{companyId}-{ts}-{safeName}`, retorna `{url, key, sizeBytes}`. (B) `client/src/pages/sst/IntegracaoSST.tsx` (~150L no VideosTab): 4 states novos (selectedFile/uploadProgress/uploading/showAdvanced); função `uploadVideoFile` via XMLHttpRequest com `xhr.upload.onprogress` (fetch não suporta); `handlePickFile` auto-preenche título com nome do arquivo; `handleSave` async — faz upload PRIMEIRO e só então chama criarModulo com URL retornada; **body reescrito** de 4 seções coloridas pra layout linear: Configuração compacta, **Nome do vídeo destacado** (h-11 autoFocus), **bloco de upload grande** (border-dashed emerald, UploadCloud, MP4/MOV/WebM/AVI até 600MB), card de arquivo com FileVideo+nome+MB+X, **barra de progresso gradient** durante envio, **"Mais opções" colapsado** (ChevronDown) agrupa URL externa+Tipo+Descrição+Duração+Ordem+Obrigatório (NADA removido, só escondido). Footer: CTA mostra "Enviando vídeo… N%" durante upload. +3 imports lucide (UploadCloud/FileVideo/ChevronUp). (C) `shared/version.ts` → 2012. **Resultado**: usuário vê só Nome+Upload em 1 segundo, escolhe arquivo de 500MB, barra enche, vídeo cadastrado. URL externa continua disponível em "Mais opções". **Preservado**: schema INTACTO (videoUrl/videoTipo já existiam); criarModulo/atualizarModulo tRPC INTACTOS; listagem/preview/edição INTACTOS; outros modais (Config/Sessão/Pendentes/Histórico) INTACTOS. Rev. 2011 (grid 2-col) substituída pela estrutura linear — usuário pediu simplificação. R-001/R-007/R-010 OK. Reversível em 2 arquivos. **Limitações**: multer memoryStorage usa RAM (OK pra equipe pequena); storagePut persiste em DB base64 (~670MB no PG, fallback storage externa já pronto). **Follow-up**: preview do vídeo enviado no card; chunked upload (>1GB); aplicar simplificação aos modais Config/Sessão/Pendentes.
- ~~Rev. 2011~~ — ver `shared/changelog.ts`.
- ~~Rev. 2010~~ — ver `shared/changelog.ts`.
- ~~Rev. 2009~~ — ver `shared/changelog.ts`. Pedido direto do usuário (img IMG_0851_1779031346140): "Melhore isso seguindo nossa regra de ouro". Modal chapado com DialogHeader cinza padrão, campos empilhados sem hierarquia, sem ícones, sem agrupamento — quebrava o padrão estabelecido. **Mudança** em 1 arquivo (`client/src/pages/sst/IntegracaoSST.tsx`, ~150L reescritas no modal de VideosTab): (1) Largura `max-w-lg` → `max-w-2xl`, container `p-0 gap-0 overflow-hidden`. (2) **Header gradient** emerald→teal full-width com chip Film ring-2 + título + subtítulo explicativo. (3) **4 seções numeradas e coloridas** com chip de ícone lucide: "1 · Onde encaixa" (indigo, Settings), "2 · Conteúdo do módulo" (slate, GraduationCap), "3 · Mídia do vídeo" (blue, Video) com **preview rico do YouTube** (overlay Play hover + badge), "4 · Configurações de exibição" (amber, Clock) com toggle Obrigatório estilo card verde + hint da consequência. (4) Container scroll `max-h-[calc(100vh-220px)]` pra não estourar em mobile/tablet. (5) Footer com borda + bg slate-50 + CTA gradient emerald→teal com sombra. (6) `shared/version.ts` → 2009. **Resultado**: modal alinhado à regra de ouro, fluxo em 4 etapas numeradas, CTA destacado, hint reforça consequência da obrigatoriedade. **Preservado**: TODOS os estados/mutations/resetForm/handleSave INTACTOS; outros modais do arquivo (Config/Sessão/Pendentes/Histórico) INTACTOS — só VideosTab mudou. Rev. 2008 INTACTA. Schema INTACTO. R-001/R-007/R-010 OK (CSS+markup-only). Reversível em 1 hunk. **Follow-up**: aplicar regra aos outros modais (Config/Sessão/Pendentes/Histórico); render real do iframe YouTube/Vimeo no preview; suporte real a upload direto.
- ~~Rev. 2008~~ — ver `shared/changelog.ts`.
- ~~Rev. 2007~~ — ver `shared/changelog.ts`.- ~~Rev. 2006~~ — ver `shared/changelog.ts`.- ~~Rev. 2005~~ — ver `shared/changelog.ts`.- ~~Rev. 2004~~ — ver `shared/changelog.ts`.- ~~Rev. 2003~~ — ver `shared/changelog.ts`.- ~~Rev. 2002~~ — ver `shared/changelog.ts`.- ~~Rev. 2001~~ — ver `shared/changelog.ts`.- ~~Rev. 2000~~ — ver `shared/changelog.ts`.- ~~Rev. 1999~~ — ver `shared/changelog.ts`.- ~~Rev. 1998~~ — ver `shared/changelog.ts`._PLACEHOLDER_QUE_NAO_DEVE_EXISTIR_ (a) cadastrar foto pra facilitar identificação visual e (b) ERP gerar automaticamente um número interno `[INICIAIS_EMPRESA]-[SEQ_GLOBAL]` (sequencial único pra TODOS terceiros do tenant, só a sigla muda por empresa). **Mudança** em 4 arquivos: (1) `drizzle/schema.ts` (L3566-3569) — `funcionariosTerceiros` ganhou `numeroInterno varchar(30)` (nullable, retrocompat). (2) `server/_core/index.ts` (+5L, bloco logo após `curriculos.historico_status_json`) — bootstrap idempotente `ALTER TABLE funcionarios_terceiros ADD COLUMN IF NOT EXISTS numero_interno VARCHAR(30)` + `CREATE INDEX IF NOT EXISTS idx_func_terc_numero_interno (company_id, numero_interno)`. (3) `server/routers/terceiros.ts` (`funcionarios.create` ~60L reescritas L480-540) — input ganhou `fotoBase64`/`fotoFileName`/`fotoContentType` opcionais; fluxo: busca empresa → gera sigla (NFD remove acentos + uppercase + `/[^A-Z]/` strip + slice(0,3) + padEnd(3,'X'), ex: "Construtora XPTO Ltda"→"CON") → próximo seq GLOBAL via `MAX(NULLIF(regexp_replace(numero_interno,'^.*-',''),'')::INTEGER) + 1` filtrado por `company_id` → monta `${sigla}-${String(seq).padStart(5,'0')}` (ex: `CON-00001`) → se foto enviada, `storagePut` em `terceiros/funcionarios/_novos/{ts}-{name}` → INSERT com `.returning({id})` (corrigido de pattern MySQL `[result] = ...` pra postgres) → retorna `{id, numeroInterno}`. (4) `client/src/pages/terceiros/FuncionariosTerceiros.tsx`: 2 states novos (`fotoPreview` data URL + `fotoPayload` base64), 2 handlers (`handlePickFotoNovo` via FileReader; `handlePickFotoEdit` faz upload imediato via `uploadDoc`); `openNew`/`openEdit`/`handleSave` adaptados; aba Dados Pessoais ganhou **hero card no topo** (gradient blue→indigo, border dashed) com avatar circular h-24 w-24 ring-4 + Camera placeholder/preview + botão X vermelho pra remover + título + badge `numeroInterno` (em edit) OU "Nº interno será gerado ao salvar" (em new) + botão "Selecionar/Trocar foto"; item da lista ganhou avatar circular h-12 w-12 à esquerda + badge azul mono `BadgeCheck numeroInterno` ao lado do nome. + `shared/version.ts` → 1998. **Preservado**: `update` mutation INTACTA, `uploadDoc` INTACTO, filtros/stats/aba Documentos INTACTOS, outras leituras de `funcionariosTerceiros` (warnings/obrigacoes/portal externo) — coluna nullable, queries SELECT * seguem OK. Rev. 1997 INTACTA. Race em geração de seq aceitável pra cadastro manual low-throughput (migra pra advisory lock se virar problema). R-001/R-007/R-010 OK (só ADD COLUMN/INDEX IF NOT EXISTS). Reversível em 4 arquivos.
- ~~Rev. 1997~~ — ver `shared/changelog.ts`.- ~~Rev. 1996~~ — ver `shared/changelog.ts`.- ~~Rev. 1995~~ — ver `shared/changelog.ts`.
- ~~Rev. 1994~~ — ver `shared/changelog.ts`.
- ~~Rev. 1993~~ — ver `shared/changelog.ts`.
- ~~Rev. 1992~~ — ver `shared/changelog.ts`.
- ~~Rev. 1991~~ — ver `shared/changelog.ts`.
- ~~Rev. 1990~~ — ver `shared/changelog.ts`. **Cotações · UX · Coluna SALDO vinculada visualmente ao Vencedor (pedido direto do usuário)**. Tela `/compras/cotacoes/<id>`: coluna SALDO ficava no extremo direito DEPOIS de todas as colunas de fornecedores, mas seu valor é sempre calculado vs. o Vencedor (primeiro fornecedor). Visualmente parecia colada ao ÚLTIMO fornecedor, dando "impressão de erro". **Mudança** em 1 arquivo (`client/src/pages/compras/Cotacoes.tsx`, 3 hunks, ZERO lógica): (1) `<th>` Saldo recebeu fundo emerald-50/60, borda-l-2 emerald-300, ícone Trophy + subtítulo "vs. <Nome do Vencedor>"; (2) `<td>` por item recebeu bg-emerald-50/30 + border-l-2 emerald-200; (3) `<td>` TOTAL recebeu bg-emerald-50/40 + border-l-2 emerald-300. Badge interna emerald/red (positivo/negativo) INTACTA. version → 1990. **Resultado**: SALDO se destaca da última coluna de fornecedor + subtítulo explícito do vencedor → elimina sensação de "valor perdido no final". **Preservado**: TODA lógica (`hasMeta`/`melhorForn`/`metaTot`/`saldo`/`saldoTotal`) INTACTA — só CSS/markup. Rev. 1989 INTACTA. Schema INTACTO. R-001/R-007/R-010 OK. Reversível em 3 hunks.
- ~~Rev. 1989~~ — ver `shared/changelog.ts`. **Cotações · UX · Header e cell condensados (pedido direto do usuário)**. Tela `/compras/cotacoes/<id>` tinha cada coluna de fornecedor com 5+ blocos verticais empilhados (nome+score, chip Vencedor, botão Anexar+texto+nome arquivo, botão Ler com IA+texto, botão Propostas+texto, botão Editar Preços+texto) em ~200px de largura → wrap, sobreposição, visual confuso. Cell de item embaixo da barra de progresso de saldo orçamentário tinha 4 spans separados em 2 linhas. **Mudança** em 1 arquivo (`client/src/pages/compras/Cotacoes.tsx`, 6 hunks, ZERO lógica): (1) toolbar de ações sem `flex-wrap`, gap reduzido pra 0.5; (2-5) botões Anexar/Ler com IA/Propostas/Editar Preços viraram icon-only h-7 w-7 com tooltip preservando a info que estava no texto; modo edição (Salvar/Desconto/Acréscimo) mantido com texto (ações contextuais). (6) Linha de breakdown de saldo condensada de 4 spans empilhados pra 1 linha truncate com tooltip completo. version → 1989. **Resultado**: largura do header diminui ~50%, saldo passa de 2 linhas pra 1, tela cabe sem scroll horizontal interno em 1366+, hover preserva 100% da info. **Preservado**: TODA a lógica INTACTA — só CSS/markup. Handlers/popovers/mutations/Anexo popover/modo edição/Vencedor chip/score badges/COBERTURA header/TOTAL row/barra de progresso INTACTOS. Rev. 1988 INTACTA. Schema INTACTO. R-001/R-007/R-010 OK. Reversível em 6 hunks.
- ~~Rev. 1988~~ — ver `shared/changelog.ts`. **Lote 1 · Pós-revisão arquitetural · 2 correções de profundidade nos fixes anteriores**. Origem: code review identificou (i) C1 estava incompleto pra OC de MATERIAL — `gerarProximoNumeroOC` no branch "compra" usava COUNT(*) dentro do lock mas não persistia contador → INSERT fora da tx permitia 2 chamadas lerem o mesmo COUNT e duplicarem OC-YYYY-NNNN; e (ii) A2 não entregava observabilidade real — `syncNow` mascarava erros com `.catch(() => {})`, então `triggerFinancialSyncAwaited` nunca propagava falha. **Mudança** em 2 arquivos: (1) `server/routers/compras.ts` — branch material de `gerarProximoNumeroOC` reescrito (~28 linhas) pra usar `ocNumberConfig.proximoNumero` (coluna LEGACY que já existia, ZERO ALTER) como contador persistente dentro do lock; bootstrap inteligente via `Math.max(COUNT+1, proximoNumero)` pra primeira chamada com OCs históricas. Lookup do config movido pra antes do switch (compartilhado). (2) `server/services/financialEventTrigger.ts` (+15 linhas) — nova função privada `syncNowStrict` (idêntica a `syncNow` MENOS os swallows); `triggerFinancialSyncAwaited` agora usa ela; `syncNow` + `triggerFinancialSync` originais INTACTOS (8 callers fire-and-forget preservados). version → 1988. **Lote 1 (C1+C2+A1+A2) agora REALMENTE fechado**. **Preservado**: branch OS/Pacote INTACTO, callers fire-and-forget INTACTOS, schema INTACTO (coluna `proximoNumero` já existia), Revs. 1985-1987 INTACTAS. R-001/R-007/R-010 OK. **Não corrigido (deliberadamente, vai pra Lote 2)**: `existCheck` de contrato em `gerarContratoTerceiroDeOS` fica fora do lock — concorrência extrema pode criar 2 contratos com números diferentes pra MESMA OC (não é dup de numeração, é dup de contrato). Menos crítico, fica pra Lote 2 com C3/C4/A3/A4/A5.
- ~~Rev. 1987~~ — ver `shared/changelog.ts`.
- ~~Rev. 1986~~ — ver `shared/changelog.ts`.
- ~~Rev. 1985~~ — ver `shared/changelog.ts`.
- ~~Rev. 1984~~ — ver `shared/changelog.ts`. **Faxina do `replit.md`** (manutenção, sem mudança de comportamento). O arquivo havia crescido pra ~30k tokens com 7 blocos detalhados espalhados (Rev. 1983, 1979, 1975, 1968, 1967, 1965, 1964, 1963, 1962, 1961) violando a convenção do topo deste bloco — "APENAS últimas 5 detalhadas". Além disso havia duplicidades: Rev. 1965/1964/1963/1962/1961 apareciam DUAS vezes (detalhadas no meio + colapsadas no fim). **Mudança** em 2 arquivos: (1) `replit.md` — bloco "Recent changes" reescrito: Rev. 1984 entra como única entrada detalhada no topo; Rev. 1983 → 1903 todas colapsadas em formato one-liner. Convenção, "User preferences" e linha "Revisões anteriores a 1903" INTACTAS. (2) `shared/version.ts` → 1984. **Resultado**: arquivo reduzido de 152 linhas (~30k tokens) pra ~95 linhas (~6k tokens). Toda informação preservada — basta abrir `shared/changelog.ts` pra ler qualquer rev histórica em detalhe. Nenhum código de aplicação tocado. Nenhum schema alterado. Nenhuma rota tRPC modificada. Sem risco de regressão. **Preservado**: Rev. 1983 e todas anteriores 100% INTACTAS no codebase. R-001/R-007/R-010 OK. Reversível em 2 arquivos.
- ~~Rev. 1983~~ — ver `shared/changelog.ts`.
- ~~Rev. 1982~~ — ver `shared/changelog.ts`.
- ~~Rev. 1981~~ — ver `shared/changelog.ts`.
- ~~Rev. 1980~~ — ver `shared/changelog.ts`.
- ~~Rev. 1979~~ — ver `shared/changelog.ts`.
- ~~Rev. 1978~~ — ver `shared/changelog.ts`.
- ~~Rev. 1977~~ — ver `shared/changelog.ts`.
- ~~Rev. 1976~~ — ver `shared/changelog.ts`.
- ~~Rev. 1975~~ — ver `shared/changelog.ts`.
- ~~Rev. 1974~~ — ver `shared/changelog.ts`.
- ~~Rev. 1973~~ — ver `shared/changelog.ts`.
- ~~Rev. 1972~~ — ver `shared/changelog.ts`.
- ~~Rev. 1971~~ — ver `shared/changelog.ts`.
- ~~Rev. 1970~~ — ver `shared/changelog.ts`.
- ~~Rev. 1969~~ — ver `shared/changelog.ts`.
- ~~Rev. 1968~~ — ver `shared/changelog.ts`.
- ~~Rev. 1967~~ — ver `shared/changelog.ts`.
- ~~Rev. 1966~~ — ver `shared/changelog.ts`.
- ~~Rev. 1965~~ — ver `shared/changelog.ts`.
- ~~Rev. 1964~~ — ver `shared/changelog.ts`.
- ~~Rev. 1963~~ — ver `shared/changelog.ts`.
- ~~Rev. 1962~~ — ver `shared/changelog.ts`.
- ~~Rev. 1961~~ — ver `shared/changelog.ts`.
- ~~Rev. 1960~~ — ver `shared/changelog.ts`.
- ~~Rev. 1959~~ — ver `shared/changelog.ts`.
- ~~Rev. 1958~~ — ver `shared/changelog.ts`.
- ~~Rev. 1957~~ — ver `shared/changelog.ts`.
- ~~Rev. 1956~~ — ver `shared/changelog.ts`.
- ~~Rev. 1955~~ — ver `shared/changelog.ts`.
- ~~Rev. 1954~~ — ver `shared/changelog.ts`.
- ~~Rev. 1953~~ — ver `shared/changelog.ts`.
- ~~Rev. 1952~~ — ver `shared/changelog.ts`.
- ~~Rev. 1951~~ — ver `shared/changelog.ts`.
- ~~Rev. 1950~~ — ver `shared/changelog.ts`.
- ~~Rev. 1949~~ — ver `shared/changelog.ts`.
- ~~Rev. 1948~~ — ver `shared/changelog.ts`.
- ~~Rev. 1947~~ — ver `shared/changelog.ts`.
- ~~Rev. 1946~~ — ver `shared/changelog.ts`.
- ~~Rev. 1945~~ — ver `shared/changelog.ts`.
- ~~Rev. 1944~~ — ver `shared/changelog.ts`.
- ~~Rev. 1943~~ — ver `shared/changelog.ts`.
- ~~Rev. 1942~~ — ver `shared/changelog.ts`.
- ~~Rev. 1941~~ — ver `shared/changelog.ts`.
- ~~Rev. 1940~~ — ver `shared/changelog.ts`.
- ~~Rev. 1939~~ — ver `shared/changelog.ts`.
- ~~Rev. 1938~~ — ver `shared/changelog.ts`.
- ~~Rev. 1937~~ — ver `shared/changelog.ts`.
- ~~Rev. 1936~~ — ver `shared/changelog.ts`.
- ~~Rev. 1935~~ — ver `shared/changelog.ts`.
- ~~Rev. 1934~~ — ver `shared/changelog.ts`.
- ~~Rev. 1933~~ — ver `shared/changelog.ts`.
- ~~Rev. 1932~~ — ver `shared/changelog.ts`.
- ~~Rev. 1931~~ — ver `shared/changelog.ts`.
- ~~Rev. 1930~~ — ver `shared/changelog.ts`.
- ~~Rev. 1929~~ — ver `shared/changelog.ts`.
- ~~Rev. 1928~~ — ver `shared/changelog.ts`.
- ~~Rev. 1927~~ — ver `shared/changelog.ts`.
- ~~Rev. 1926~~ — ver `shared/changelog.ts`.
- ~~Rev. 1925~~ — ver `shared/changelog.ts`.
- ~~Rev. 1924~~ — ver `shared/changelog.ts`.
- ~~Rev. 1923~~ — ver `shared/changelog.ts`.
- ~~Rev. 1922~~ — ver `shared/changelog.ts`.
- ~~Rev. 1921~~ — ver `shared/changelog.ts`.
- ~~Rev. 1920~~ — ver `shared/changelog.ts`.
- ~~Rev. 1919~~ — ver `shared/changelog.ts`.
- ~~Rev. 1918~~ — ver `shared/changelog.ts`.
- ~~Rev. 1917~~ — ver `shared/changelog.ts`.
- ~~Rev. 1916~~ — ver `shared/changelog.ts`.
- ~~Rev. 1915~~ — ver `shared/changelog.ts`.
- ~~Rev. 1914~~ — ver `shared/changelog.ts`.
- ~~Rev. 1913~~ — ver `shared/changelog.ts`.
- ~~Rev. 1912~~ — ver `shared/changelog.ts`.
- ~~Rev. 1911~~ — ver `shared/changelog.ts`.
- ~~Rev. 1910~~ — ver `shared/changelog.ts`.
- ~~Rev. 1909~~ — ver `shared/changelog.ts`.
- ~~Rev. 1908~~ — ver `shared/changelog.ts`.
- ~~Rev. 1907~~ — ver `shared/changelog.ts`.
- ~~Rev. 1906~~ — ver `shared/changelog.ts`.
- ~~Rev. 1905~~ — ver `shared/changelog.ts`.
- ~~Rev. 1904~~ — ver `shared/changelog.ts`.
- ~~Rev. 1903~~ — ver `shared/changelog.ts`.

> Revisões anteriores à 1903: ver `shared/changelog.ts` (histórico completo).

## User preferences

- Idioma de comunicação: pt-BR direto e objetivo.
- Toda revisão DEVE: editar código + bumpar `shared/version.ts` + adicionar entrada NO TOPO de `shared/changelog.ts` + pop oldest do top-5 de `replit.md` (ver convenção acima).
- R-001 / R-007 / R-010: JAMAIS executar `ALTER TABLE`, `DROP`, ou `DELETE` em produção.
